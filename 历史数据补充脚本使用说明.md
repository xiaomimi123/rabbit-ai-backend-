# 历史数据补充脚本使用说明

## 📋 脚本用途

`scripts/backfill-claim-fees.ts` 是一个**一次性运行的数据补充脚本**，用于为历史记录补充 `fee_amount_wei` 字段。

**注意**：这个脚本不是后端服务的一部分，是独立的数据迁移工具。

---

## 🔧 环境变量设置位置

### 方案 1: 在本地运行（推荐用于测试）

**适用场景**：
- 本地开发环境
- 测试环境
- 有数据库访问权限的本地机器

**设置方法**：

#### 方法 A: 创建 `.env` 文件（推荐）

在 `rabbit-ai-backend` 目录下创建 `.env` 文件：

```env
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key
BSC_RPC_URLS=https://bsc-dataseed1.binance.org,https://bsc-dataseed2.binance.org
```

**注意**：`.env` 文件通常会被 `.gitignore` 忽略，不会提交到 Git。

#### 方法 B: 在 PowerShell 中临时设置

```powershell
# 在运行脚本前设置环境变量
$env:SUPABASE_URL='https://your-project.supabase.co'
$env:SUPABASE_SERVICE_KEY='your-service-role-key'
$env:BSC_RPC_URLS='https://bsc-dataseed1.binance.org,https://bsc-dataseed2.binance.org'

# 然后运行脚本
npx tsx scripts/backfill-claim-fees.ts
```

---

### 方案 2: 在服务器上运行（推荐用于生产环境）

**适用场景**：
- 生产环境
- 服务器已有环境变量配置

**设置方法**：

#### 如果使用 Render.com：

1. 进入项目设置
2. 在 "Environment" 部分添加环境变量：
   - `SUPABASE_URL`
   - `SUPABASE_SERVICE_KEY`
   - `BSC_RPC_URLS`

3. 通过 SSH 或控制台连接到服务器，运行脚本：
   ```bash
   cd /path/to/rabbit-ai-backend
   npx tsx scripts/backfill-claim-fees.ts
   ```

#### 如果使用其他服务器：

1. 确保服务器已配置环境变量（与后端服务使用相同的环境变量）
2. SSH 连接到服务器
3. 运行脚本：
   ```bash
   cd /path/to/rabbit-ai-backend
   npx tsx scripts/backfill-claim-fees.ts
   ```

---

## 📝 环境变量说明

### 必需的环境变量

| 变量名 | 说明 | 示例 |
|--------|------|------|
| `SUPABASE_URL` | Supabase 项目 URL | `https://xxx.supabase.co` |
| `SUPABASE_SERVICE_KEY` | Supabase Service Role Key（需要完整权限） | `eyJhbGc...` |
| `BSC_RPC_URLS` | BSC RPC 节点 URL（多个用逗号分隔） | `https://bsc-dataseed1.binance.org,https://bsc-dataseed2.binance.org` |

### 如何获取这些值？

1. **SUPABASE_URL** 和 **SUPABASE_SERVICE_KEY**：
   - 登录 Supabase Dashboard
   - 进入项目设置 → API
   - `SUPABASE_URL` = Project URL
   - `SUPABASE_SERVICE_KEY` = `service_role` key（⚠️ 注意：不是 `anon` key）

2. **BSC_RPC_URLS**：
   - 使用后端服务中已配置的 RPC URL
   - 或者使用公共节点：`https://bsc-dataseed1.binance.org,https://bsc-dataseed2.binance.org`

---

## 🚀 运行步骤

### 1. 确保环境变量已设置

```powershell
# 检查环境变量（PowerShell）
$env:SUPABASE_URL
$env:SUPABASE_SERVICE_KEY
$env:BSC_RPC_URLS
```

### 2. 安装依赖（如果未安装）

```bash
npm install
# 或
npm install dotenv tsx
```

### 3. 运行脚本

```bash
npx tsx scripts/backfill-claim-fees.ts
```

### 4. 查看输出

脚本会显示：
- 找到的记录数量
- 处理进度
- 成功/失败统计
- 最终结果

---

## ⚠️ 注意事项

### 1. 运行位置

- ✅ **可以在本地运行**（如果有数据库访问权限）
- ✅ **可以在服务器上运行**（推荐，因为服务器通常已有环境变量配置）
- ❌ **不需要在后端服务运行时执行**（这是独立脚本）

### 2. 运行时机

- ✅ **建议在低峰期运行**（避免 RPC 速率限制）
- ✅ **可以多次运行**（脚本会自动跳过已补充的记录）
- ✅ **可以随时中断**（已处理的记录不会重复处理）

### 3. 性能考虑

- 脚本会批量处理（每次 10 条）
- 批次间有延迟（2 秒），避免 RPC 速率限制
- 如果记录很多，可能需要较长时间

### 4. 数据安全

- 脚本使用 `ON CONFLICT DO UPDATE`，不会重复更新
- 如果 `fee_amount_wei` 已存在，不会覆盖
- 可以安全地多次运行

---

## 🔍 验证结果

运行脚本后，可以通过以下 SQL 验证：

```sql
-- 检查补充结果
SELECT 
  COUNT(*) as total_claims,
  COUNT(fee_amount_wei) as claims_with_fee,
  COUNT(*) - COUNT(fee_amount_wei) as claims_without_fee
FROM claims;

-- 查看补充的数据示例
SELECT 
  tx_hash,
  address,
  fee_amount_wei,
  ethers.utils.formatEther(fee_amount_wei) as fee_bnb
FROM claims
WHERE fee_amount_wei IS NOT NULL
LIMIT 10;
```

---

## 📊 示例输出

```
🚀 开始补充历史数据：fee_amount_wei
📊 RPC 节点数量: 3
📊 当前使用 RPC: https://bsc-dataseed1.binance.org

📊 找到 31 条需要补充的记录

📦 处理批次 1/4 (1-10)
✅ 交易 0x1234... 手续费: 0.0004 BNB
✅ 交易 0x5678... 手续费: 0.0004 BNB
...

📊 补充完成统计:
✅ 成功: 31 条
❌ 失败: 0 条
⏭️  跳过: 0 条
📊 总计: 31 条

🎉 所有记录已成功补充！
```

---

## 🆘 常见问题

### Q1: 脚本提示 "缺少 Supabase 配置"

**A**: 检查环境变量是否正确设置：
```powershell
# PowerShell
$env:SUPABASE_URL
$env:SUPABASE_SERVICE_KEY
```

### Q2: 脚本提示 "BSC_RPC_URLS 环境变量未配置"

**A**: 设置 RPC URL：
```powershell
$env:BSC_RPC_URLS='https://bsc-dataseed1.binance.org,https://bsc-dataseed2.binance.org'
```

### Q3: RPC 请求失败

**A**: 
- 检查 RPC URL 是否可访问
- 尝试使用其他 RPC 节点
- 脚本会自动重试和切换 RPC

### Q4: 可以在生产服务器上运行吗？

**A**: 可以，但建议：
- 在低峰期运行
- 确保服务器有足够的网络带宽
- 监控 RPC 请求速率

---

## 📌 总结

- **环境变量设置位置**：根据运行位置决定
  - 本地运行：`.env` 文件或 PowerShell 环境变量
  - 服务器运行：服务器环境变量（与后端服务共享）
- **运行位置**：可以在本地或服务器上运行
- **运行时机**：一次性运行，可以随时执行
- **安全性**：脚本是幂等的，可以安全地多次运行

---

**最后更新**: 2026-01-03

