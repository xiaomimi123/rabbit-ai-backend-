# BSC RPC 网络错误问题审查报告

**日期**: 2026-01-09  
**问题类型**: 后端稳定性问题  
**严重程度**: 🟡 中等（影响数据准确性，但不影响核心功能）

---

## 📋 问题概述

服务器后台频繁出现 BSC RPC 节点连接错误，导致：
- 收益计算可能使用默认值（0）
- Indexer 同步可能中断
- 管理员 KPI 数据可能不准确

---

## 🔍 错误日志分析

### 1. **网络检测失败** (最频繁)
```
could not detect network (event="noNetwork", code=NETWORK_ERROR, version=providers/5.8.0)
```

**出现位置**:
- `getAdminKpis` - 查询 claimFee 时
- `getRevenueStats` - 查询 claimFee 时
- `getAdminRevenueWithDateRange` - 查询 claimFee 时
- Indexer - 轮询区块时

**影响**: 
- ✅ 已有降级处理（使用默认值 0）
- ⚠️ 但数据可能不准确

### 2. **RPC 超时**
```
[Earnings] RPC timeout for 0xb6c50a1aa8816e866092c01c05c5fe1ab3489d9b (balanceOf), using default 0
```

**出现位置**:
- `earnings.ts` - 查询用户 RAT 余额时

**影响**:
- ✅ 已有 8 秒超时保护
- ✅ 使用默认值 0，不影响 API 响应
- ⚠️ 但收益计算可能不准确

### 3. **Indexer 超时**
```
[indexer] error timeout (requestBody="{\"method\":\"eth_blockNumber\",\"params\":[],\"id\":161699,\"jsonrpc\":\"2.0\"}", requestMethod="POST", timeout=120000, url="https://bsc-mainnet.nodereal.io/v1/174046b55b1b4cf1bf515a9af8497ecb", code=TIMEOUT, version=web/5.8.0)
```

**出现位置**:
- `indexer/indexer.ts` - 轮询最新区块时

**影响**:
- ⚠️ Indexer 会跳过本次同步，等待下次轮询
- ⚠️ 如果所有 RPC 都超时，可能导致同步延迟

---

## 🔎 根本原因分析

### 1. **RpcPool 实现过于简单**

**当前实现** (`src/infra/rpcPool.ts`):
```typescript
export class RpcPool {
  private idx = 0;
  private providers: ethers.providers.JsonRpcProvider[];

  constructor(urls: string[]) {
    this.providers = urls.map((u) => new ethers.providers.JsonRpcProvider(u));
  }

  current(): ethers.providers.JsonRpcProvider {
    return this.providers[this.idx];
  }

  rotate(): ethers.providers.JsonRpcProvider {
    this.idx = (this.idx + 1) % this.providers.length;
    return this.current();
  }
}
```

**问题**:
- ❌ 没有健康检查（不知道哪个 RPC 可用）
- ❌ 没有超时配置（使用 ethers.js 默认 120 秒）
- ❌ 没有错误统计（不知道哪个 RPC 经常失败）
- ❌ 轮询策略简单（可能一直使用同一个失败的 RPC）

### 2. **JsonRpcProvider 没有配置超时**

**当前代码**:
```typescript
this.providers = urls.map((u) => new ethers.providers.JsonRpcProvider(u));
```

**问题**:
- ❌ 使用默认超时（120 秒），太长了
- ❌ 没有配置连接超时
- ❌ 没有配置请求重试

### 3. **Indexer 错误处理不够健壮**

**当前实现** (`src/indexer/indexer.ts`):
```typescript
while (true) {
  try {
    const provider = providerFactory();
    await runOnce(provider);
  } catch (e) {
    console.error('[indexer] error', (e as any)?.message || e);
    onFatal?.(e); // 只是 rotate RPC，没有其他处理
  }
  await sleep(config.pollIntervalMs);
}
```

**问题**:
- ⚠️ 错误后立即重试，如果所有 RPC 都失败，会频繁报错
- ⚠️ 没有指数退避（exponential backoff）
- ⚠️ 没有错误分类（临时错误 vs 永久错误）

### 4. **RPC 节点可能不稳定**

从日志看，使用的 RPC 节点：
- `https://bsc-mainnet.nodereal.io/v1/174046b55b1b4cf1bf515a9af8497ecb`

**可能原因**:
- RPC 节点负载过高
- 网络连接不稳定
- RPC 节点配置问题

---

## 📊 影响评估

### ✅ 已有保护措施

1. **收益计算** (`earnings.ts`):
   - ✅ 8 秒超时保护
   - ✅ 失败时使用默认值 0
   - ✅ 不影响 API 响应

2. **管理员 KPI** (`admin.ts`):
   - ✅ 10 秒超时保护
   - ✅ 失败时使用默认值
   - ✅ 有缓存机制（60 秒）

3. **Indexer**:
   - ✅ 有 RPC 轮换机制
   - ✅ 错误不会导致服务崩溃

### ⚠️ 潜在问题

1. **数据准确性**:
   - 如果 RPC 频繁失败，收益计算可能不准确
   - 管理员 KPI 可能显示错误数据

2. **同步延迟**:
   - Indexer 如果频繁失败，可能导致链上数据同步延迟

3. **用户体验**:
   - 虽然不影响 API 响应，但数据可能不准确

---

## 🛠️ 修复方案

### 方案 1: 增强 RpcPool（推荐）⭐

**目标**: 添加健康检查、超时配置、错误统计

**实施步骤**:
1. 为 `JsonRpcProvider` 配置更短的超时（30 秒）
2. 添加 RPC 健康检查（定期 ping）
3. 添加错误统计（记录每个 RPC 的失败次数）
4. 智能轮换（优先使用健康的 RPC）

**优点**:
- ✅ 提高系统稳定性
- ✅ 自动切换到健康的 RPC
- ✅ 减少错误日志

**缺点**:
- ⚠️ 需要修改核心代码
- ⚠️ 增加代码复杂度

### 方案 2: 优化 Indexer 错误处理

**目标**: 添加指数退避、错误分类

**实施步骤**:
1. 添加指数退避（错误后等待更长时间）
2. 错误分类（临时错误 vs 永久错误）
3. 如果所有 RPC 都失败，增加等待时间

**优点**:
- ✅ 减少错误日志
- ✅ 避免频繁重试

**缺点**:
- ⚠️ 可能增加同步延迟

### 方案 3: 配置多个备用 RPC 节点

**目标**: 增加 RPC 节点数量，提高可用性

**实施步骤**:
1. 在 `BSC_RPC_URLS` 中添加更多备用节点
2. 使用不同的 RPC 提供商（NodeReal、Binance、Ankr 等）

**优点**:
- ✅ 简单易实施
- ✅ 提高可用性

**缺点**:
- ⚠️ 需要配置多个 RPC 节点（可能有成本）
- ⚠️ 不能解决根本问题（如果所有节点都失败）

---

## 🎯 推荐实施方案

**优先级排序**:
1. **P0（立即实施）**: 增强 RpcPool，添加超时配置和健康检查
2. **P1（本周内）**: 优化 Indexer 错误处理，添加指数退避
3. **P2（可选）**: 配置更多备用 RPC 节点

**预期效果**:
- ✅ 减少 80% 以上的 RPC 错误
- ✅ 提高数据准确性
- ✅ 减少错误日志噪音

---

## 📝 实施检查清单

- [ ] 增强 `RpcPool` 类，添加健康检查
- [ ] 为 `JsonRpcProvider` 配置超时（30 秒）
- [ ] 优化 Indexer 错误处理，添加指数退避
- [ ] 添加 RPC 错误统计和监控
- [ ] 测试修复后的稳定性
- [ ] 更新文档

---

## 🔗 相关文件

- `src/infra/rpcPool.ts` - RPC 连接池
- `src/indexer/indexer.ts` - Indexer 主循环
- `src/services/earnings.ts` - 收益计算（已有超时保护）
- `src/services/admin.ts` - 管理员 KPI（已有超时保护）
- `src/config.ts` - RPC 配置

---

**报告生成时间**: 2026-01-09  
**审查人**: AI Assistant  
**状态**: 🔄 待修复

