# 访问统计调试检查清单

## 🔍 快速检查步骤

### 1. 前端检查（浏览器控制台）

**步骤：**
1. 打开网站
2. 按 F12 打开开发者工具
3. 切换到 Console 标签
4. 清除 sessionStorage：
   ```javascript
   sessionStorage.removeItem('rabbit_visit_recorded');
   sessionStorage.removeItem('rabbit_session_id');
   ```
5. 刷新页面

**应该看到的日志：**
```
[App] Starting to record page visit...
[App] Recording page visit now...
[App] 📤 Sending visit record to: https://your-backend/api/analytics/visit
[App] 📥 Response received: {status: 200, ok: true, ...}
[App] ✅ Page visit recorded successfully: {ok: true, message: "Visit recorded"}
```

**如果看到错误：**
- `CORS policy` → 检查后端 CORS 配置
- `404 Not Found` → 检查 API 路径配置
- `Network Error` → 检查后端服务是否运行
- `Failed to record page visit` → 查看详细错误信息

### 2. 后端检查（服务器日志）

**应该看到的日志：**
```
[Analytics API] Client IP: xxx.xxx.xxx.xxx
[Analytics API] Rate limit check: {allowed: true}
[Analytics API] 📥 Request body: {...}
[Analytics API] ✅ Request validated: {...}
[Analytics API] Recording visit: {...}
[Analytics] Inserting page visit: {...}
[Analytics] ✅ Page visit inserted successfully: {id: 123}
[Analytics API] Visit recorded result: {ok: true, id: 123}
```

**如果看到错误：**
- `Rate limit exceeded` → 等待 1 分钟后重试
- `Failed to insert page visit` → 检查数据库连接和权限
- `Client IP is null` → 检查请求头（可能正常，如果 IP 为 null 也会继续处理）

### 3. 数据库检查（Supabase SQL Editor）

**执行查询：**
```sql
-- 检查记录数量
SELECT COUNT(*) FROM page_visits;

-- 查看最近记录
SELECT * FROM page_visits ORDER BY created_at DESC LIMIT 10;

-- 检查表结构
SELECT column_name, data_type, is_nullable 
FROM information_schema.columns 
WHERE table_name = 'page_visits';
```

### 4. 网络请求检查（浏览器 Network 标签）

**步骤：**
1. 打开 Network 标签
2. 刷新页面
3. 查找 `/api/analytics/visit` 请求
4. 检查：
   - 请求状态码（应该是 200）
   - 请求 URL（应该是正确的后端地址）
   - 请求体（应该包含所有必要字段）
   - 响应体（应该是 `{ok: true, message: "Visit recorded"}`）

## 🐛 常见问题及解决方案

### 问题 1: 前端没有发送请求

**可能原因：**
- sessionStorage 已标记为已记录
- useEffect 依赖问题
- 页面加载失败

**解决方案：**
```javascript
// 清除 sessionStorage
sessionStorage.clear();

// 刷新页面
location.reload();
```

### 问题 2: 后端没有收到请求

**可能原因：**
- API 路由未注册
- CORS 配置错误
- 后端服务未运行

**解决方案：**
1. 检查 `src/server.ts` 中是否调用了 `registerAnalyticsRoutes(app)`
2. 检查后端 CORS 配置
3. 检查后端服务是否正常运行

### 问题 3: 数据库插入失败

**可能原因：**
- 表不存在
- 权限不足
- 数据格式错误

**解决方案：**
1. 确认表已创建（执行 `create_page_visits_table.sql`）
2. 检查 Supabase 服务角色密钥是否正确
3. 查看后端日志中的详细错误信息

### 问题 4: Rate Limit 拦截

**可能原因：**
- 同一 IP 在 1 分钟内多次请求

**解决方案：**
- 等待 1 分钟后重试
- 或修改 Rate Limit 逻辑（如果需要）

## 📊 测试命令

### 使用 curl 测试 API

```bash
curl -X POST https://your-backend-url/api/analytics/visit \
  -H "Content-Type: application/json" \
  -H "CF-Connecting-IP: 8.8.8.8" \
  -d '{
    "pagePath": "/test",
    "walletAddress": null,
    "referrer": null,
    "language": "zh",
    "isMobile": false,
    "sessionId": "test_123"
  }'
```

### 检查数据库记录

```sql
-- 查看所有记录
SELECT * FROM page_visits ORDER BY created_at DESC;

-- 查看今天的记录
SELECT * FROM page_visits 
WHERE created_at >= CURRENT_DATE 
ORDER BY created_at DESC;

-- 查看特定 IP 的记录
SELECT * FROM page_visits 
WHERE ip_address = 'xxx.xxx.xxx.xxx'
ORDER BY created_at DESC;
```

## ✅ 验证清单

- [ ] 前端控制台没有错误
- [ ] 前端发送了请求（Network 标签可见）
- [ ] 后端收到了请求（服务器日志可见）
- [ ] 后端成功插入数据库（服务器日志显示成功）
- [ ] 数据库中有记录（SQL 查询有结果）
- [ ] 管理后台能显示数据（访问统计页面有数据）

## 🆘 如果仍然没有数据

1. **检查所有日志**：前端控制台 + 后端服务器日志
2. **检查网络请求**：浏览器 Network 标签
3. **检查数据库**：Supabase SQL Editor
4. **检查配置**：环境变量、CORS 设置
5. **提供信息**：将所有日志和错误信息提供给开发人员

