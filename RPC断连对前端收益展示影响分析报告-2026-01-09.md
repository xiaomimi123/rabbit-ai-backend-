# RPC 断连对前端收益展示影响分析报告

**日期**: 2026-01-09  
**分析范围**: 用户前端收益展示功能  
**问题**: RPC 断连接是否影响用户前端的收益展示？

---

## 📋 执行摘要

**结论**: ⚠️ **部分影响** - RPC 断连会影响 VIP 等级和日利率的显示，但**不会影响实际收益金额的显示**。

### 影响程度
- ✅ **收益金额（pendingUsdt）**: **不受影响** - 主要来自数据库
- ⚠️ **VIP 等级**: **受影响** - 依赖 RPC 获取链上余额
- ⚠️ **日利率**: **受影响** - 依赖 VIP 等级
- ⚠️ **实时收益计算**: **轻微影响** - 如果 VIP 等级错误，实时增量计算可能不准确

---

## 🔍 数据流分析

### 1. 前端收益展示流程

```
用户打开资产页面
    ↓
loadEarningsData() 执行
    ↓
fetchEarnings(address) - 调用后端 API
    ↓
后端 calculateUserEarnings() 执行
    ↓
├─ RPC 调用: balanceOf() - 获取链上 RAT 余额
│  └─ 如果失败: balance = 0 (降级处理)
│
├─ 数据库查询: users.usdt_total - 获取累计收益
│  └─ 不受 RPC 影响 ✅
│
├─ 数据库查询: users.last_settlement_time - 获取最后结算时间
│  └─ 不受 RPC 影响 ✅
│
└─ 计算收益: 基于数据库数据 + 时间差
   └─ 不受 RPC 影响 ✅
    ↓
返回 { pendingUsdt, dailyRate, currentTier, holdingDays }
    ↓
前端显示收益
```

### 2. 后端收益计算逻辑

**位置**: `rabbit-ai-backend/src/services/earnings.ts`

#### 2.1 RPC 调用（可能失败）

```typescript
// 步骤 1: 从链上读取 RAT 余额
let balance: number = 0;
try {
  const ratContract = new ethers.Contract(config.ratTokenContract, ERC20_ABI, provider);
  balanceWei = await Promise.race([balancePromise, timeoutPromise]); // 8秒超时
  balance = parseFloat(balanceStr);
} catch (error: any) {
  // 🟢 降级处理：使用默认值 0
  console.warn(`[Earnings] RPC timeout for ${addr} (balanceOf), using default 0`);
  balance = 0; // ⚠️ 这里会导致问题
}
```

**影响**:
- ❌ 如果 RPC 失败，`balance = 0`
- ❌ 导致 VIP 等级计算错误（会显示为新手）
- ❌ 导致日利率显示错误（会显示为 2%）

#### 2.2 数据库查询（不受 RPC 影响）

```typescript
// 步骤 2-3: 查询数据库
const { data: userRow } = await supabase
  .from('users')
  .select('usdt_total, last_settlement_time, created_at')
  .eq('address', addr)
  .maybeSingle();

// 步骤 4: 确定 VIP 等级（依赖 balance）
const { dailyRate, tier: currentTier } = getVipTierByBalance(balance);
// ⚠️ 如果 balance = 0，会返回新手等级（tier = 0, dailyRate = 2%）

// 步骤 5: 计算收益（主要依赖数据库数据）
const baseEarnings = Number(userRow?.usdt_total || 0); // ✅ 来自数据库
const timeSinceLastSettlement = (now - lastSettlementTime) / (24 * 3600 * 1000);
const incrementalEarnings = balance * dailyRate * timeSinceLastSettlement / 100;
const pendingUsdt = baseEarnings + incrementalEarnings; // ✅ 主要来自数据库
```

**关键发现**:
- ✅ `baseEarnings` 来自数据库（`usdt_total`），不受 RPC 影响
- ⚠️ `incrementalEarnings` 依赖 `balance` 和 `dailyRate`，如果 RPC 失败会有影响
- ⚠️ 但如果 `balance = 0`，`incrementalEarnings = 0`，所以 `pendingUsdt = baseEarnings`（仍然准确）

---

## 📊 影响分析

### 场景 1: RPC 完全失败（超时/网络错误）

**后端行为**:
- `balance = 0`（降级处理）
- `currentTier = 0`（新手）
- `dailyRate = 2%`（新手利率）
- `pendingUsdt = baseEarnings + 0`（增量收益为 0）

**前端显示**:
- ✅ **收益金额**: 显示 `baseEarnings`（准确，因为增量收益为 0）
- ❌ **VIP 等级**: 显示"新手"（错误，应该是实际等级）
- ❌ **日利率**: 显示"2%"（错误，应该是实际利率）
- ⚠️ **实时收益**: 基于错误的日利率计算，可能不准确

**用户体验**:
- ✅ 能看到正确的收益金额
- ❌ 看不到正确的 VIP 等级
- ❌ 看不到正确的日利率
- ⚠️ 实时收益增量可能不准确（但基准值正确）

### 场景 2: RPC 部分失败（延迟但最终成功）

**后端行为**:
- 8 秒超时保护，如果 8 秒内成功，返回正确余额
- 如果超过 8 秒，使用 `balance = 0`

**前端显示**:
- 取决于 RPC 响应时间
- 如果响应快（< 8秒），显示正确
- 如果响应慢（> 8秒），同场景 1

### 场景 3: 所有 RPC 节点都失败

**后端行为**:
- 所有 RPC 节点都超时或失败
- 使用 `balance = 0`（降级处理）
- 收益计算基于数据库数据

**前端显示**:
- ✅ 收益金额仍然准确（来自数据库）
- ❌ VIP 等级和日利率显示错误

---

## 🛠️ 当前保护措施

### ✅ 已有保护

1. **超时保护**:
   - 8 秒超时，快速失败
   - 避免长时间等待

2. **降级处理**:
   - RPC 失败时使用 `balance = 0`
   - 确保 API 不会崩溃

3. **数据库优先**:
   - 收益计算主要依赖数据库数据
   - RPC 只用于获取当前余额

4. **前端错误处理**:
   - 404 错误时显示 0（正常情况）
   - 其他错误时显示错误状态

### ⚠️ 潜在问题

1. **VIP 等级显示错误**:
   - 如果 RPC 失败，用户看不到正确的 VIP 等级
   - 可能影响用户体验

2. **日利率显示错误**:
   - 如果 RPC 失败，日利率显示为 2%（新手）
   - 可能误导用户

3. **实时收益增量不准确**:
   - 如果 VIP 等级错误，实时增量计算可能不准确
   - 但基准值（来自数据库）仍然准确

---

## 💡 优化建议

### 方案 1: 使用数据库缓存的余额（推荐）⭐

**思路**: 如果 RPC 失败，使用数据库中上次缓存的余额

**实施步骤**:
1. 在 `users` 表中添加 `rat_balance_wei` 字段（已有）
2. 每次成功获取链上余额后，更新数据库
3. 如果 RPC 失败，使用数据库中的余额

**优点**:
- ✅ VIP 等级和日利率显示更准确
- ✅ 实时收益计算更准确
- ✅ 减少对 RPC 的依赖

**缺点**:
- ⚠️ 如果用户通过其他方式获得代币，余额可能不准确
- ⚠️ 需要定期同步链上余额

### 方案 2: 前端缓存上次的 VIP 等级

**思路**: 如果 API 返回的 VIP 等级为 0，使用前端缓存的等级

**实施步骤**:
1. 前端缓存上次成功获取的 VIP 等级
2. 如果 API 返回 `currentTier = 0` 且 `balance = 0`，使用缓存的等级
3. 显示提示："当前无法获取链上余额，显示上次的 VIP 等级"

**优点**:
- ✅ 简单易实施
- ✅ 改善用户体验

**缺点**:
- ⚠️ 如果用户真的降级了，可能显示错误

### 方案 3: 优化 RPC 连接（已实施）

**思路**: 使用我们之前实施的 RPC 连接池优化

**优点**:
- ✅ 减少 RPC 失败的概率
- ✅ 自动切换到备用节点

**缺点**:
- ⚠️ 如果所有节点都失败，仍然会有问题

---

## 📝 结论

### 对用户的影响

1. **收益金额显示**: ✅ **不受影响**
   - 主要来自数据库，RPC 失败时仍然准确

2. **VIP 等级显示**: ❌ **受影响**
   - 依赖 RPC 获取链上余额
   - RPC 失败时显示为"新手"

3. **日利率显示**: ❌ **受影响**
   - 依赖 VIP 等级
   - RPC 失败时显示为"2%"

4. **实时收益计算**: ⚠️ **轻微影响**
   - 基准值准确（来自数据库）
   - 增量值可能不准确（依赖错误的日利率）

### 建议

1. **短期**: 保持现状，收益金额仍然准确
2. **中期**: 实施方案 1（使用数据库缓存的余额）
3. **长期**: 解决 RPC 付费问题，减少失败概率

---

## 🔗 相关文件

- `rabbit-ai-backend/src/services/earnings.ts` - 收益计算逻辑
- `rabbit-ai-frontendxin/views/AssetView.tsx` - 前端收益展示
- `rabbit-ai-frontendxin/api.ts` - 前端 API 调用
- `rabbit-ai-backend/BSC-RPC付费问题说明和解决方案-2026-01-09.md` - RPC 付费问题

---

**报告生成时间**: 2026-01-09  
**状态**: ✅ 分析完成

