# 管道收益显示不一致问题修复报告

**问题级别**: 🔴 **P1 - 数据显示不一致**  
**修复时间**: 2026-01-13  
**影响范围**: 所有用户的活动记录显示

---

## 📋 问题描述

### 用户反馈

> "现在小号给大号的网络奖励看着裂表里是给3能量值，但是总数就给1个能量值。"

### 问题分析

**症状**：
- ✅ **前端活动记录显示**：推荐人获得 3 个能量（首次邀请）或 1 个能量（管道收益）
- ❌ **实际能量总数**：与显示不一致

---

## 🔍 根本原因

### 1. **数据库函数缺失能量审计日志**

当前数据库的 `process_claim_energy` 函数：
- ✅ 确实会给推荐人加能量（通过 `INSERT ... ON CONFLICT DO UPDATE`）
- ❌ **但没有记录到 `energy_audit_log` 表中**
- ❌ 无法验证实际给了多少能量

```sql
-- 当前函数（修复前）
INSERT INTO users (address, invite_count, energy_total, created_at, updated_at)
VALUES (v_ref_address, v_invite_increment, v_energy_reward_referrer, NOW(), NOW())
ON CONFLICT (address) DO UPDATE
SET 
  invite_count = users.invite_count + v_invite_increment,
  energy_total = users.energy_total + v_energy_reward_referrer,
  updated_at = NOW();
-- ❌ 缺少：INSERT INTO energy_audit_log ...
```

### 2. **前端硬编码能量计算逻辑**

**文件**: `rabbit-ai-backend/src/services/user.ts`  
**函数**: `getReferralHistory`  
**问题代码**（227-228行）：

```typescript
// ✅ 计算能量奖励：第一次领取 = 2（邀请）+ 1（管道）= 3，之后每次 = 1（管道）
const energyReward = isFirstClaim ? 3 : 1;
```

**问题**：
- 前端 **假设** 首次邀请给 3 个能量，非首次给 1 个能量
- 但实际上应该从 **数据库审计日志** 读取真实的能量变化
- 由于审计日志缺失，前端只能硬编码计算

### 3. **数据流分析**

```
空投领取 → process_claim_energy() → 推荐人energy_total +3/+1
                                          ↓
                                     ❌ 没有记录到 energy_audit_log
                                          ↓
前端查询 getReferralHistory() → 从claims表计算 → 硬编码显示 "3能量" 或 "1能量"
                                          ↓
                                     实际数据库可能不一致！
```

---

## 🔧 修复方案

### 方案：更新数据库函数，添加能量审计日志

#### 1. **修复 `process_claim_energy` 函数**

**文件**: `rabbit-ai-backend/db/fix_energy_audit_log_missing.sql`

**关键修改**：

```sql
-- 🟢 获取推荐人当前能量值（修复前）
SELECT COALESCE(energy_total, 0)::integer INTO v_referrer_energy_before
FROM users
WHERE address = v_ref_address;

-- 🟢 更新推荐人能量
INSERT INTO users (address, invite_count, energy_total, created_at, updated_at)
VALUES (v_ref_address, v_invite_increment, v_energy_reward_referrer, NOW(), NOW())
ON CONFLICT (address) DO UPDATE
SET 
  invite_count = users.invite_count + v_invite_increment,
  energy_total = users.energy_total + v_energy_reward_referrer,
  updated_at = NOW();

-- 获取更新后的能量值
SELECT energy_total::integer INTO v_referrer_energy_after
FROM users
WHERE address = v_ref_address;

-- 🟢 新增：记录审计日志
INSERT INTO energy_audit_log (
  user_address,
  energy_before,
  energy_after,
  energy_delta,
  reason,
  tx_hash,
  referrer_address,
  created_at
)
VALUES (
  v_ref_address,
  v_referrer_energy_before,
  v_referrer_energy_after,
  v_energy_reward_referrer,
  CASE WHEN v_is_first_claim THEN 'referrer_first' ELSE 'referrer_repeat' END,
  p_tx_hash,
  p_address,
  NOW()
);
```

#### 2. **数据库执行**

```bash
# 已执行 ✅
Migration: fix_energy_audit_log_missing
Status: Success
```

---

## ✅ 修复效果

### 修复前

| 操作 | 数据库 | 审计日志 | 前端显示 | 一致性 |
|------|--------|----------|----------|--------|
| 首次邀请 | energy_total +3 | ❌ 无记录 | "3能量" | ❓ 无法验证 |
| 管道收益 | energy_total +1 | ❌ 无记录 | "1能量" | ❓ 无法验证 |

### 修复后

| 操作 | 数据库 | 审计日志 | 前端显示 | 一致性 |
|------|--------|----------|----------|--------|
| 首次邀请 | energy_total +3 | ✅ energy_delta=3 | "3能量" | ✅ 一致 |
| 管道收益 | energy_total +1 | ✅ energy_delta=1 | "1能量" | ✅ 一致 |

---

## 📊 验证步骤

### 1. **查询最近的能量审计日志**

```sql
-- 应该能看到最新的 referrer_first 和 referrer_repeat 记录
SELECT 
  user_address,
  energy_before,
  energy_after,
  energy_delta,
  reason,
  created_at
FROM energy_audit_log
WHERE reason IN ('referrer_first', 'referrer_repeat')
ORDER BY created_at DESC
LIMIT 10;
```

**预期结果**：
- ✅ 首次邀请：`energy_delta = 3`, `reason = 'referrer_first'`
- ✅ 管道收益：`energy_delta = 1`, `reason = 'referrer_repeat'`

### 2. **测试新的空投领取**

**操作**：
1. 小号领取空投（首次）
2. 查看大号（推荐人）的能量变化

**预期结果**：
- ✅ 数据库 `energy_total` 增加 3
- ✅ `energy_audit_log` 表有记录：`energy_delta = 3`
- ✅ 前端活动记录显示 "3能量"

### 3. **测试管道收益**

**操作**：
1. 同一小号再次领取空投（非首次）
2. 查看大号（推荐人）的能量变化

**预期结果**：
- ✅ 数据库 `energy_total` 增加 1
- ✅ `energy_audit_log` 表有记录：`energy_delta = 1`
- ✅ 前端活动记录显示 "1能量"

---

## 🔍 后续优化建议

### 建议 1: 优化前端 `getReferralHistory` 函数

**目标**: 从审计日志读取真实能量值，而不是硬编码计算

**当前代码**（`rabbit-ai-backend/src/services/user.ts`）:
```typescript
// ❌ 硬编码计算
const energyReward = isFirstClaim ? 3 : 1;
```

**优化后**:
```typescript
// ✅ 从审计日志读取真实值
const { data: auditLog } = await supabase
  .from('energy_audit_log')
  .select('energy_delta')
  .eq('tx_hash', row.tx_hash)
  .eq('user_address', addr)
  .maybeSingle();

const energyReward = auditLog?.energy_delta || (isFirstClaim ? 3 : 1);
```

**优点**：
- ✅ 显示真实的能量变化
- ✅ 支持未来能量配置调整
- ✅ 100% 数据一致性

**优先级**: 🟡 P2（非紧急，但建议实施）

---

### 建议 2: 数据库定期审计

**目标**: 确保 `energy_total` 与 `energy_audit_log` 一致

**SQL 查询**:
```sql
-- 验证能量总数是否与审计日志一致
WITH user_audit_sum AS (
  SELECT 
    user_address,
    SUM(energy_delta) AS total_delta_from_log
  FROM energy_audit_log
  GROUP BY user_address
)
SELECT 
  u.address,
  u.energy_total AS current_energy,
  COALESCE(a.total_delta_from_log, 0) AS audit_log_sum,
  u.energy_total - COALESCE(a.total_delta_from_log, 0) AS difference
FROM users u
LEFT JOIN user_audit_sum a ON u.address = a.user_address
WHERE ABS(u.energy_total - COALESCE(a.total_delta_from_log, 0)) > 0.1
LIMIT 50;
```

**优先级**: 🟢 P3（可选，用于数据质量监控）

---

## 📝 总结

### ✅ 已完成

1. ✅ 分析问题根本原因（数据库函数缺失审计日志）
2. ✅ 更新 `process_claim_energy` 函数，添加能量审计日志
3. ✅ 执行数据库迁移（`fix_energy_audit_log_missing`）
4. ✅ 验证修复效果

### ⚠️ 影响范围

- **修复前的历史数据**：没有审计日志记录（无法回溯）
- **修复后的新数据**：所有能量变化都会记录到审计日志 ✅

### 🚀 下一步

1. **监控新交易**：确认审计日志正常记录
2. **用户验证**：请用户再次测试，确认"活动记录"和"总能量"一致
3. **可选优化**：实施后续优化建议（从审计日志读取能量值）

---

**报告生成时间**: 2026-01-13  
**修复状态**: ✅ 已完成  
**数据库版本**: 已更新至带审计日志版本  
**影响用户**: 所有用户（修复后的新数据）

