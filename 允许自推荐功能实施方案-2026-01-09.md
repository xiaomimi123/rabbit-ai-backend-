# 允许自推荐功能实施方案

**实施日期**: 2026-01-09  
**功能说明**: 允许用户自己推荐自己并获得推荐奖励  
**业务决策**: 允许用户通过自推荐来刷邀请奖励

---

## 📋 功能概述

### 当前状态
- ❌ 系统当前**禁止**自推荐
- ❌ 自推荐时，推荐奖励被过滤，不发放

### 修改后状态
- ✅ 系统**允许**自推荐
- ✅ 自推荐时，用户可以获得推荐奖励（能量值和RAT代币）

---

## 🔧 技术实现

### 1. 数据库函数修改

**文件**: `db/allow_self_referral.sql`

**主要修改**:
1. **移除自推荐检测逻辑**
   ```sql
   -- 修改前：检测到自推荐时，将推荐人设为 NULL
   IF v_ref_address = p_address THEN
     v_ref_address := NULL;  -- 过滤自推荐
   END IF;
   
   -- 修改后：允许自推荐，不再过滤
   IF v_ref_address = p_address THEN
     RAISE NOTICE '[process_claim_energy] ✅ 允许自推荐: % 推荐自己', p_address;
     -- 继续处理推荐奖励
   END IF;
   ```

2. **优化自推荐时的行锁处理**
   ```sql
   -- 自推荐时，推荐人就是自己，只需要锁定一次
   IF v_ref_address = p_address THEN
     v_referrer_energy_before := v_user_energy_before;  -- 使用已锁定的值
   END IF;
   ```

3. **自推荐时的能量值更新**
   ```sql
   -- 自推荐时，推荐人就是自己，直接更新自己的能量值
   IF v_ref_address = p_address THEN
     UPDATE users
     SET 
       invite_count = invite_count + v_invite_increment,
       energy_total = energy_total + v_energy_reward_referrer,  -- 在原有基础上再加推荐奖励
       updated_at = NOW()
     WHERE address = p_address;
   END IF;
   ```

### 2. 返回结果增强

**新增字段**:
```sql
RETURN jsonb_build_object(
  'status', 'success',
  'is_first_claim', v_is_first_claim,
  'energy_reward_user', v_energy_reward_user,
  'energy_reward_referrer', CASE WHEN v_ref_address IS NOT NULL THEN v_energy_reward_referrer ELSE NULL END,
  'is_self_referral', CASE WHEN v_ref_address = p_address THEN true ELSE false END  -- 🟢 新增
);
```

---

## 💰 自推荐奖励计算

### 1. 能量值奖励

**奖励机制说明**:
- **首次邀请奖励**: 2点能量值（邀请奖励）
- **管道收益**: 1点能量值（每次下级领取都获得）
- **首次邀请总计**: 2点（邀请奖励）+ 1点（管道收益）= 3点
- **重复领取**: 只有1点（管道收益），没有邀请奖励

**自推荐时，用户会同时获得**:

| 奖励类型 | 首次领取 | 重复领取 | 说明 |
|---------|---------|---------|------|
| **自己领取奖励** | +1点 | +1点 | 每次领取都获得 |
| **推荐奖励（首次）** | +3点 | - | 2点邀请奖励 + 1点管道收益 |
| **推荐奖励（重复）** | - | +1点 | 只有管道收益 |
| **总计** | **+4点** | **+2点** | 每次自推荐的总能量 |

**详细分解**:
```
用户自推荐首次领取：
- 自己领取：+1点
- 推荐奖励（首次）：
  - 邀请奖励：+2点
  - 管道收益：+1点
  - 小计：+3点
- 总计：+4点能量

用户自推荐重复领取：
- 自己领取：+1点
- 推荐奖励（重复）：
  - 管道收益：+1点（没有邀请奖励）
- 总计：+2点能量
```

### 2. RAT代币奖励

**自推荐时，用户会获得推荐奖励的RAT代币**:
- 推荐奖励金额 = 被推荐人领取金额的百分比（由智能合约决定）
- 自推荐时，用户既是领取人，也是推荐人，会获得推荐奖励

### 3. 邀请人数统计

**自推荐时，`invite_count` 会增加**:
- 首次自推荐：`invite_count` +1
- 重复自推荐：`invite_count` 不变

---

## 📊 影响分析

### 1. 正面影响

#### 1.1 用户体验提升
- ✅ 用户可以自由选择推荐人，包括自己
- ✅ 用户可以通过自推荐获得更多奖励
- ✅ 增加了用户获取能量的途径

#### 1.2 业务灵活性提升
- ✅ 允许用户通过自推荐来刷邀请奖励
- ✅ 增加了系统的灵活性和用户参与度

### 2. 潜在风险

#### 2.1 经济风险
- ⚠️ **能量值快速增加**: 用户可以通过自推荐快速获得大量能量值
  - 首次自推荐：+4点能量
  - 重复自推荐：+2点能量
  - 如果用户频繁自推荐，能量值会快速增长

- ⚠️ **RAT代币快速增加**: 用户可以通过自推荐获得大量RAT代币奖励
  - 每次自推荐都会产生推荐奖励的RAT代币
  - 如果用户频繁自推荐，RAT代币会快速增长

#### 2.2 系统公平性
- ⚠️ **不公平优势**: 允许自推荐可能给某些用户带来不公平优势
  - 有多个钱包的用户可以通过自推荐快速积累能量和代币
  - 可能影响系统的公平性和平衡性

#### 2.3 数据统计
- ⚠️ **邀请人数统计**: 自推荐会导致 `invite_count` 增加
  - 用户自己会出现在自己的下级列表中
  - 邀请人数统计可能不够准确

---

## 🔍 使用场景分析

### 场景1：用户首次自推荐

**操作**:
- 用户使用自己的地址作为推荐人，首次领取空投

**奖励**:
- 自己领取：+1点能量
- 推荐奖励（首次）：+3点能量
- RAT代币：获得推荐奖励的RAT代币
- 邀请人数：+1

**总计**:
- 能量值：+4点
- RAT代币：获得推荐奖励
- 邀请人数：+1

### 场景2：用户重复自推荐

**操作**:
- 用户使用自己的地址作为推荐人，重复领取空投

**奖励**:
- 自己领取：+1点能量
- 推荐奖励（重复）：+1点能量
- RAT代币：获得推荐奖励的RAT代币
- 邀请人数：不变

**总计**:
- 能量值：+2点
- RAT代币：获得推荐奖励
- 邀请人数：不变

### 场景3：用户多次自推荐

**假设**: 用户自推荐10次

**奖励计算**:
```
首次自推荐：
- 自己领取：+1点
- 推荐奖励（首次）：+3点（2点邀请奖励 + 1点管道收益）
- 小计：+4点

重复自推荐（9次）：
- 每次：+1点（自己领取）+ 1点（管道收益）= +2点
- 小计：+2点 × 9 = +18点

总计：+4点 + 18点 = +22点能量
```

**RAT代币**:
- 每次自推荐都会获得推荐奖励的RAT代币
- 10次自推荐 = 10次推荐奖励

---

## 📋 实施步骤

### 步骤1：备份数据库

**操作**:
```sql
-- 备份相关表
CREATE TABLE claims_backup_20260109 AS SELECT * FROM claims;
CREATE TABLE referral_rewards_backup_20260109 AS SELECT * FROM referral_rewards;
CREATE TABLE users_backup_20260109 AS SELECT * FROM users;
```

### 步骤2：执行数据库迁移

**操作**:
```bash
# 执行 SQL 脚本
psql -d your_database -f db/allow_self_referral.sql
```

### 步骤3：验证功能

**验证项**:
1. ✅ 自推荐时，推荐奖励正常发放
2. ✅ 能量值正确增加（自己领取 + 推荐奖励）
3. ✅ RAT代币奖励正常发放
4. ✅ 邀请人数正确统计
5. ✅ 审计日志正确记录

### 步骤4：测试用例

**测试用例1：首次自推荐**
```
输入：
- 用户地址：0x123...
- 推荐人：0x123...（自己）

预期结果：
- 能量值：+4点
  - 自己领取：+1点
  - 推荐奖励（首次）：+3点（2点邀请奖励 + 1点管道收益）
- RAT代币：获得推荐奖励
- 邀请人数：+1
```

**测试用例2：重复自推荐**
```
输入：
- 用户地址：0x123...
- 推荐人：0x123...（自己）

预期结果：
- 能量值：+2点
  - 自己领取：+1点
  - 推荐奖励（重复）：+1点（只有管道收益，没有邀请奖励）
- RAT代币：获得推荐奖励
- 邀请人数：不变
```

---

## ⚠️ 注意事项

### 1. 经济影响

**建议**:
- 监控能量值和RAT代币的发放情况
- 如果发现异常增长，及时调整策略
- 考虑设置自推荐的频率限制或奖励上限

### 2. 系统公平性

**建议**:
- 向用户说明自推荐的功能和规则
- 确保所有用户都有平等的自推荐机会
- 监控是否有用户滥用自推荐功能

### 3. 数据统计

**建议**:
- 在统计邀请人数时，可以考虑排除自推荐
- 或者在展示时明确标注自推荐的情况
- 确保数据统计的准确性和透明度

---

## 📊 修复前后对比

### 修复前（禁止自推荐）

| 项目 | 状态 | 说明 |
|------|------|------|
| 自推荐检测 | ✅ 启用 | 检测到自推荐时，过滤推荐奖励 |
| 推荐奖励 | ❌ 不发放 | 自推荐时不发放推荐奖励 |
| 能量值 | 只获得自己领取的奖励 | 首次+1点，重复+1点 |
| RAT代币 | 不获得推荐奖励 | 自推荐时不获得推荐奖励的RAT代币 |
| 邀请人数 | 不增加 | 自推荐时 `invite_count` 不增加 |

### 修复后（允许自推荐）

| 项目 | 状态 | 说明 |
|------|------|------|
| 自推荐检测 | ❌ 禁用 | 不再检测和过滤自推荐 |
| 推荐奖励 | ✅ 正常发放 | 自推荐时正常发放推荐奖励 |
| 能量值 | 获得自己领取+推荐奖励 | 首次+4点（1点自己+2点邀请+1点管道），重复+2点（1点自己+1点管道） |
| RAT代币 | 获得推荐奖励 | 自推荐时获得推荐奖励的RAT代币 |
| 邀请人数 | 正常增加 | 首次自推荐时 `invite_count` +1 |

---

## 🎯 总结

### 功能特点
- ✅ 允许用户自己推荐自己
- ✅ 自推荐时正常发放推荐奖励（能量值和RAT代币）
- ✅ 自推荐时 `invite_count` 正常增加
- ✅ 所有操作都有审计日志记录
- ✅ 使用行锁确保并发安全

### 业务价值
- ✅ 增加用户获取奖励的途径
- ✅ 提高用户参与度和活跃度
- ✅ 增加系统的灵活性和用户选择

### 风险控制
- ⚠️ 需要监控能量值和RAT代币的发放情况
- ⚠️ 需要确保系统公平性
- ⚠️ 需要准确统计和展示数据

---

**实施人**: AI Assistant  
**实施时间**: 2026-01-09  
**状态**: ✅ **方案已准备，等待执行**

