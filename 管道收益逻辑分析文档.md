# 管道收益逻辑分析文档

## 📋 问题描述

**现象**：下级第三次领取空投奖励时，上级还是获得了 3 点能量值（应该是 1 点）

**预期行为**：
- 首次领取：上级获得 3 点能量（1 管道 + 2 首邀）
- 第二次及以后：上级获得 1 点能量（仅管道）

---

## 🔍 当前数据库函数逻辑（实际部署的版本）

从 Supabase 查询到的实际函数逻辑：

```sql
CREATE OR REPLACE FUNCTION public.process_claim_energy(...)
SECURITY DEFINER
SET search_path TO 'public'
AS $function$
DECLARE
  v_claim_count bigint;
  v_is_first_claim boolean;
  ...
BEGIN
  -- 1. 先插入 claim 记录
  INSERT INTO claims (...) 
  VALUES (...) 
  ON CONFLICT (tx_hash) DO NOTHING;
  
  GET DIAGNOSTICS v_inserted = ROW_COUNT;
  
  IF v_inserted = 0 THEN
    RETURN jsonb_build_object('status', 'skipped', 'reason', 'tx_exists');
  END IF;

  -- 2. 插入后统计该地址的总记录数
  SELECT count(*) INTO v_claim_count 
  FROM claims 
  WHERE address = p_address;
  
  -- 3. 判断逻辑：count = 1 表示首次
  v_is_first_claim := (v_claim_count = 1);

  -- 4. 给推荐人奖励
  IF v_is_first_claim THEN
    v_energy_reward := 3;  -- 首次：3 点（1管道 + 2首邀）
  ELSE
    v_energy_reward := 1;  -- 非首次：1 点（仅管道）
  END IF;
END;
$function$;
```

---

## 📝 代码文件中的函数逻辑（fix_process_claim_energy_block_time.sql）

```sql
-- 1. 先统计（插入前）
SELECT count(*) INTO v_claim_count_before
FROM claims
WHERE address = p_address;

-- 2. 然后插入
INSERT INTO claims (...) 
VALUES (...) 
ON CONFLICT (tx_hash) DO NOTHING;

-- 3. 判断逻辑：count_before = 0 表示首次
v_is_first_claim := (v_claim_count_before = 0);
```

---

## ⚠️ 问题分析

### 问题 1：逻辑顺序不一致 ⚠️ **关键问题**

**数据库实际函数**：先插入，后统计
```sql
-- 1. 先插入
INSERT INTO claims (...) VALUES (...) ON CONFLICT (tx_hash) DO NOTHING;
-- 2. 插入后统计
SELECT count(*) INTO v_claim_count FROM claims WHERE address = p_address;
-- 3. 判断：count = 1 表示首次
v_is_first_claim := (v_claim_count = 1);
```
- ✅ 逻辑正确：第一次 count=1（首次），第二次 count=2（非首次），第三次 count=3（非首次）
- ⚠️ **但是**：如果函数没有正确部署，可能还在使用旧版本

**代码文件函数**：先统计，后插入
```sql
-- 1. 先统计（插入前）
SELECT count(*) INTO v_claim_count_before FROM claims WHERE address = p_address;
-- 2. 然后插入
INSERT INTO claims (...) VALUES (...) ON CONFLICT (tx_hash) DO NOTHING;
-- 3. 判断：count_before = 0 表示首次
v_is_first_claim := (v_claim_count_before = 0);
```
- ✅ 逻辑正确：第一次 count_before=0（首次），第二次 count_before=1（非首次），第三次 count_before=2（非首次）
- ⚠️ **问题**：代码文件中的函数可能没有部署到数据库

### 问题 2：RLS（行级安全策略）影响

如果 `claims` 表启用了 RLS，且函数没有 `SECURITY DEFINER`，那么：
- 函数可能无法看到所有历史记录
- 导致 `count(*)` 结果不准确

**检查**：数据库函数有 `SECURITY DEFINER` ✅，应该不受 RLS 影响

### 问题 3：并发问题

如果两个请求同时处理同一个用户的 claim：
- 请求 A：插入后统计，count = 1（首次）
- 请求 B：插入后统计，count = 2（非首次）

但如果有并发，可能导致：
- 两个请求都看到 count = 1
- 都判断为首次，都给了 3 点能量

---

## 🔧 排查步骤

### 步骤 1：检查数据库中的实际函数

```sql
-- 查询函数定义
SELECT pg_get_functiondef(oid) 
FROM pg_proc 
WHERE proname = 'process_claim_energy';
```

### 步骤 2：检查 claims 表的数据

```sql
-- 查询特定用户的 claim 记录
SELECT 
  tx_hash,
  address,
  referrer,
  created_at,
  block_number
FROM claims
WHERE address = '0x6196a89359fda45c87b4c6f67fd6831f63169ae9'
ORDER BY created_at ASC;
```

### 步骤 3：检查 users 表的能量变化

```sql
-- 查询推荐人的能量变化历史（如果有日志表）
-- 或者查询当前能量值
SELECT 
  address,
  energy_total,
  invite_count,
  updated_at
FROM users
WHERE address = '0xd2d24d6839e26a87af01b621d1b2d348295fc760';
```

### 步骤 4：检查是否有重复处理

```sql
-- 检查是否有重复的 tx_hash
SELECT 
  tx_hash,
  COUNT(*) as count,
  array_agg(address) as addresses
FROM claims
GROUP BY tx_hash
HAVING COUNT(*) > 1;
```

---

## 🎯 可能的原因

### 原因 1：函数未正确部署

代码文件中的函数（先统计后插入）可能没有部署到数据库，数据库中的函数还是旧版本（先插入后统计）。

### 原因 2：并发竞态条件

如果两个请求同时处理，可能导致：
- 两个请求都插入成功
- 但统计时都看到 count = 1（因为另一个还没提交）
- 都判断为首次，都给了 3 点能量

### 原因 3：RLS 策略问题

虽然函数有 `SECURITY DEFINER`，但如果 RLS 策略配置不当，可能还是会影响统计。

### 原因 4：事务隔离级别问题

如果事务隔离级别设置不当，可能导致：
- 一个事务插入后，另一个事务统计时看不到新插入的记录
- 导致统计不准确

---

## ✅ 修复建议

### 方案 1：使用行锁 + 先统计后插入（推荐）

```sql
CREATE OR REPLACE FUNCTION public.process_claim_energy(...)
SECURITY DEFINER
SET search_path TO 'public'
AS $function$
DECLARE
  v_claim_count_before integer;
  v_is_first_claim boolean;
  v_inserted boolean := false;
BEGIN
  -- 1. 先统计（插入前）
  SELECT count(*) INTO v_claim_count_before
  FROM claims
  WHERE address = p_address;
  
  -- 2. 插入 claim 记录
  INSERT INTO claims (...) 
  VALUES (...) 
  ON CONFLICT (tx_hash) DO NOTHING;
  
  -- 3. 检查是否成功插入
  GET DIAGNOSTICS v_inserted = ROW_COUNT;
  
  IF v_inserted = 0 THEN
    RETURN jsonb_build_object('status', 'skipped', 'reason', 'tx_exists');
  END IF;

  -- 4. 判断逻辑：count_before = 0 表示首次
  v_is_first_claim := (v_claim_count_before = 0);

  -- 5. 给推荐人奖励（使用 v_is_first_claim 判断）
  ...
END;
$function$;
```

### 方案 2：使用事务 + 行锁

在函数开始处添加行锁，确保同一时间只有一个请求能处理：

```sql
-- 锁定该用户的所有 claim 记录
SELECT 1 FROM claims WHERE address = p_address FOR UPDATE;
```

---

## 📊 验证方法

### 测试用例 1：首次领取

1. 新用户首次领取
2. 检查推荐人是否获得 3 点能量
3. 检查 `invite_count` 是否 +1

### 测试用例 2：第二次领取

1. 同一用户第二次领取
2. 检查推荐人是否只获得 1 点能量
3. 检查 `invite_count` 是否不变

### 测试用例 3：第三次领取

1. 同一用户第三次领取
2. 检查推荐人是否只获得 1 点能量
3. 检查 `invite_count` 是否不变

---

## 🔍 数据验证

### 用户 `0x6196a89359fda45c87b4c6f67fd6831f63169ae9` 的领取记录：

| 序号 | 交易哈希 | 领取时间 | 预期奖励 | 实际奖励 |
|------|---------|---------|---------|---------|
| 1 | 0x06e8... | 2025-12-29 09:58:44 | 3 点（首次） | ? |
| 2 | 0x3ad6... | 2025-12-30 05:17:40 | 1 点（管道） | ? |
| 3 | 0x4620... | 2025-12-31 11:06:57 | 1 点（管道） | **3 点（错误）** |

### 推荐人 `0xd2d24d6839e26a87af01b621d1b2d348295fc760` 状态：

- **当前能量**：92.5
- **邀请人数**：4
- **最后更新**：2025-12-31 12:33:19

---

## 🎯 根本原因分析

### 原因 1：函数未正确部署（最可能）⚠️

**问题**：代码文件 `fix_process_claim_energy_block_time.sql` 中的函数（先统计后插入）可能没有部署到数据库。

**证据**：
- 数据库中的实际函数是"先插入后统计"（从 Supabase 查询到的）
- 代码文件中的函数是"先统计后插入"
- 两者不一致

**验证方法**：
```sql
-- 检查函数是否有 SECURITY DEFINER
SELECT 
  proname,
  prosecdef,  -- true 表示有 SECURITY DEFINER
  prosrc
FROM pg_proc 
WHERE proname = 'process_claim_energy';
```

### 原因 2：并发竞态条件

如果两个请求同时处理：
- 请求 A：插入后统计，count = 1（首次）→ 给 3 点
- 请求 B：插入后统计，count = 1（首次）→ 给 3 点
- 结果：都给了 3 点，但应该只有第一个给 3 点

### 原因 3：函数逻辑错误

虽然数据库函数逻辑看起来正确（先插入后统计），但可能存在边界情况：
- 如果 `ON CONFLICT DO NOTHING` 导致插入失败，但 `ROW_COUNT` 判断有误
- 如果统计时使用了错误的查询条件

---

## 🔧 修复方案

### 方案 1：重新部署正确的函数（推荐）

使用代码文件中的函数（先统计后插入），并添加 `SECURITY DEFINER`：

```sql
CREATE OR REPLACE FUNCTION public.process_claim_energy(...)
RETURNS jsonb
LANGUAGE plpgsql
SECURITY DEFINER  -- ✅ 关键：确保不受 RLS 影响
SET search_path = public
AS $function$
DECLARE
  v_claim_count_before integer;  -- ✅ 先统计
  v_is_first_claim boolean;
  ...
BEGIN
  -- 1. 先统计（插入前）
  SELECT count(*) INTO v_claim_count_before
  FROM claims
  WHERE address = p_address;
  
  -- 2. 插入 claim 记录
  INSERT INTO claims (...) 
  VALUES (...) 
  ON CONFLICT (tx_hash) DO NOTHING;
  
  -- 3. 检查是否成功插入
  GET DIAGNOSTICS v_inserted = ROW_COUNT;
  
  IF v_inserted = 0 THEN
    RETURN jsonb_build_object('status', 'skipped', 'reason', 'tx_exists');
  END IF;

  -- 4. 判断逻辑：count_before = 0 表示首次
  v_is_first_claim := (v_claim_count_before = 0);

  -- 5. 给推荐人奖励
  ...
END;
$function$;
```

### 方案 2：添加行锁防止并发

```sql
-- 在函数开始处添加行锁
SELECT 1 FROM claims WHERE address = p_address FOR UPDATE;
```

---

## 🔍 下一步行动

1. ✅ 检查数据库中的实际函数定义（已完成）
2. ✅ 检查 claims 表的数据（已完成）
3. ✅ **重新部署正确的函数**（先统计后插入 + SECURITY DEFINER）**已完成**
4. ⏳ 验证函数是否正确部署
5. ⏳ 测试验证：确保第二次及以后只给 1 点能量

---

## ✅ 修复执行记录

**执行时间**：2025-12-31

**修复内容**：
- ✅ 将函数逻辑从"先插入后统计"改为"先统计后插入"
- ✅ 确保函数有 `SECURITY DEFINER`，不受 RLS 影响
- ✅ 添加调试信息 `claim_count_before` 和 `reward_given_to_referrer`
- ✅ **添加 users 表行锁，彻底解决并发问题**（关键修复）

**修复脚本**：
1. `db/fix_process_claim_energy_first_claim_logic.sql` - 修复逻辑顺序
2. `db/fix_process_claim_energy_add_row_lock.sql` - 添加行锁（最终版本）

**执行状态**：✅ 已成功部署到 Supabase

---

## 🔒 并发问题修复说明

### 问题分析

**原始问题**：如果用户第一次领取，`claims` 表中没有记录，无法通过锁 `claims` 表来防止并发。

**错误方案**：
```sql
-- ❌ 错误：如果用户第一次领取，claims 表中没有记录，锁不住任何东西
SELECT 1 FROM claims WHERE address = p_address FOR UPDATE;
```

**正确方案**：
```sql
-- ✅ 正确：锁 users 表，因为用户能领取空投，说明 users 表中一定有记录
-- 先确保用户记录存在
INSERT INTO users (address, energy_total, created_at, updated_at)
VALUES (p_address, 0, now(), now())
ON CONFLICT (address) DO NOTHING;

-- 然后锁定该用户的 users 表行
PERFORM 1 FROM users WHERE address = p_address FOR UPDATE;
```

### 工作原理

1. **请求 A** 进来 → 锁住用户 → 处理中...
2. **请求 B** 进来 → 试图锁用户 → **被阻塞，等待 A 完成**
3. **请求 C** 进来 → 试图锁用户 → **被阻塞，等待 B 完成**

这样确保：
- 请求 B 获得锁时，一定能看到请求 A 刚刚插入的 claim 记录
- `v_claim_count_before` 统计时，会包含请求 A 插入的记录
- 请求 B 判断为"非首次"，只给 1 点能量 ✅

### 修复后的完整流程

```sql
1. 数据清洗与准备
2. 🔒 确保用户存在并锁定 users 表行
3. 🔒 先统计 claims（此时已锁住，数据安全）
4. 插入 claim 记录
5. 判断是否为首次（使用插入前的统计结果）
6. 给用户自己加能量
7. 给推荐人奖励（首次 3 点，非首次 1 点）
8. 返回调试信息
```

