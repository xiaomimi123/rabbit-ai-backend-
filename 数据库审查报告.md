# Rabbit AI 数据库审查报告

**审查日期**: 2024-12-25  
**审查范围**: Supabase PostgreSQL 数据库  
**审查目的**: 确保数据库结构与后端代码需求一致

---

## 📊 数据库结构概览

### 已存在的表（12个）

| 表名 | 用途 | 状态 |
|------|------|------|
| `users` | 用户基本信息、能量、USDT余额 | ✅ 完整 |
| `claims` | 空投领取记录 | ✅ 完整 |
| `referral_rewards` | 推荐奖励记录 | ✅ 完整 |
| `cooldown_resets` | 冷却重置记录 | ✅ 完整 |
| `withdrawals` | 提现记录 | ✅ 完整 |
| `chain_sync_state` | 链同步状态 | ✅ 完整 |
| `system_config` | 系统配置 | ✅ 完整 |
| `notifications` | 用户通知 | ✅ 完整 |
| `system_announcement` | 系统公告 | ✅ 完整 |
| `system_links` | 系统链接配置 | ✅ 完整 |
| `user_holdings` | 用户持币记录（缓存） | ✅ 完整 |
| `vip_tiers` | VIP等级配置 | ✅ 完整 |

---

## ✅ 字段完整性检查

### 1. `users` 表

**实际数据库字段**：
- ✅ `address` (text, PK)
- ✅ `referrer_address` (text, nullable)
- ✅ `invite_count` (bigint, default 0)
- ✅ `energy_total` (numeric, default 0)
- ✅ `energy_locked` (numeric, default 0)
- ✅ `usdt_total` (numeric, default 0) **← 已存在**
- ✅ `usdt_locked` (numeric, default 0) **← 已存在**
- ✅ `created_at` (timestamptz)
- ✅ `updated_at` (timestamptz)

**后端代码使用**：
- `src/services/user.ts`: 使用 `usdt_total`, `usdt_locked` ✅
- `src/services/withdraw.ts`: 使用 `usdt_total`, `usdt_locked` ✅
- `src/services/admin.ts`: 使用 `usdt_total`, `usdt_locked` ✅

**结论**: ✅ **完全匹配**，所有字段都已存在

---

### 2. `claims` 表

**实际数据库字段**：
- ✅ `tx_hash` (text, PK)
- ✅ `address` (text)
- ✅ `referrer` (text)
- ✅ `amount_wei` (text)
- ✅ `block_number` (bigint, nullable)
- ✅ `block_time` (timestamptz, nullable)
- ✅ `status` (text, default 'SUCCESS')
- ✅ `created_at` (timestamptz)
- ✅ `energy_awarded` (boolean, default false) **← 已存在**

**后端代码使用**：
- `src/indexer/indexer.ts`: 写入 `claims` 表 ✅
- `src/services/verifyClaim.ts`: 使用 `energy_awarded` 字段 ✅
- `src/services/earnings.ts`: 查询 `created_at` 计算持币天数 ✅
- `src/services/user.ts`: 查询 `claims` 获取历史记录 ✅

**结论**: ✅ **完全匹配**，所有字段都已存在

---

### 3. `withdrawals` 表

**实际数据库字段**：
- ✅ `id` (uuid, PK)
- ✅ `address` (text)
- ✅ `amount` (numeric)
- ✅ `status` (text, default 'Pending')
- ✅ `energy_locked_amount` (numeric, default 0)
- ✅ `payout_tx_hash` (text, nullable)
- ✅ `created_at` (timestamptz)
- ✅ `updated_at` (timestamptz)

**后端代码使用**：
- `src/services/withdraw.ts`: 使用所有字段 ✅
- `src/services/earnings.ts`: 查询 `amount`, `status` 计算已提现总额 ✅
- `src/services/admin.ts`: 使用所有字段 ✅

**结论**: ✅ **完全匹配**，所有字段都已存在

---

### 4. `referral_rewards` 表

**实际数据库字段**：
- ✅ `id` (uuid, PK)
- ✅ `referrer_address` (text)
- ✅ `amount_wei` (text)
- ✅ `tx_hash` (text, unique)
- ✅ `block_number` (bigint, nullable)
- ✅ `block_time` (timestamptz, nullable)
- ✅ `created_at` (timestamptz)

**后端代码使用**：
- `src/indexer/indexer.ts`: 写入 `referral_rewards` 表 ✅
- `src/services/user.ts`: 查询 `referral_rewards` 计算团队奖励 ✅

**结论**: ✅ **完全匹配**，所有字段都已存在

---

### 5. 其他表

#### `notifications` 表
- ✅ 已存在，字段完整
- 用途：用户站内信通知
- 后端代码：暂无使用（前端可能需要）

#### `system_announcement` 表
- ✅ 已存在，字段完整
- 用途：系统全局公告
- 后端代码：暂无使用（前端可能需要）

#### `system_links` 表
- ✅ 已存在，字段完整
- 用途：系统链接配置（白皮书、审计报告、客服）
- 后端代码：暂无使用（前端可能需要）

#### `user_holdings` 表
- ✅ 已存在，字段完整
- 用途：缓存用户RAT余额和VIP等级（可选优化）
- 后端代码：**当前未使用**（收益计算直接从链上读取）

#### `vip_tiers` 表
- ✅ 已存在，字段完整
- 用途：VIP等级配置（管理员可动态修改）
- 后端代码：**当前未使用**（收益计算中硬编码VIP等级）

---

## 🔍 索引完整性检查

### 已存在的索引

#### `users` 表
- ✅ `users_pkey` (address)
- ✅ `idx_users_referrer` (referrer_address)

#### `claims` 表
- ✅ `claims_pkey` (tx_hash)
- ✅ `idx_claims_address_time` (address, created_at DESC)
- ✅ `idx_claims_address_energy_awarded` (address, energy_awarded) WHERE energy_awarded = false **← 额外优化索引**

#### `referral_rewards` 表
- ✅ `referral_rewards_pkey` (id)
- ✅ `referral_rewards_tx_hash_key` (tx_hash, unique)
- ✅ `idx_ref_rewards_referrer_time` (referrer_address, created_at DESC)

#### `withdrawals` 表
- ✅ `withdrawals_pkey` (id)
- ✅ `idx_withdrawals_address_time` (address, created_at DESC)
- ✅ `idx_withdrawals_status_time` (status, created_at DESC)
- ✅ `uq_withdrawals_payout_tx_hash_not_null` (payout_tx_hash) WHERE payout_tx_hash IS NOT NULL

#### `notifications` 表
- ✅ `notifications_pkey` (id)
- ✅ `idx_notifications_address_read` (address, read, created_at DESC)
- ✅ `idx_notifications_address_time` (address, created_at DESC)

#### `user_holdings` 表
- ✅ `user_holdings_pkey` (address)
- ✅ `idx_user_holdings_updated` (last_updated)
- ✅ `idx_user_holdings_tier` (current_tier_level, tier_reached_at)

#### `vip_tiers` 表
- ✅ `vip_tiers_pkey` (level)
- ✅ `idx_vip_tiers_active_order` (is_active, display_order)

**结论**: ✅ **索引完整**，所有必要的索引都已存在，甚至有一些额外的优化索引

---

## ⚠️ 发现的问题和建议

### 1. SQL 文件与实际数据库不同步

**问题**: 
- `db/supabase.sql` 文件缺少以下表和字段：
  - `users.usdt_total` 和 `users.usdt_locked` 字段
  - `claims.energy_awarded` 字段
  - `notifications` 表
  - `system_announcement` 表
  - `system_links` 表
  - `user_holdings` 表
  - `vip_tiers` 表

**影响**: 
- 新环境部署时，SQL 文件不完整
- 文档与实际数据库不一致

**解决方案**: ✅ **已更新** `db/supabase.sql` 文件，包含所有表和字段

---

### 2. 未使用的表

**问题**: 
- `user_holdings` 表存在但后端代码未使用
- `vip_tiers` 表存在但后端代码未使用（硬编码VIP等级）

**建议**: 
- **方案 A**: 移除未使用的表（如果确定不需要）
- **方案 B**: 实现使用这些表的代码（用于优化和动态配置）

**当前状态**: 表已存在，但不影响功能（可选优化）

---

### 3. 收益计算逻辑

**问题**: 
- `earnings.ts` 中 VIP 等级和日利率是硬编码的
- 数据库中有 `vip_tiers` 表，但代码未使用

**建议**: 
- 未来可以考虑从 `vip_tiers` 表读取配置，实现动态VIP等级管理
- 当前硬编码方式也可以工作，但不够灵活

---

### 4. 数据一致性

**问题**: 
- `users.usdt_total` 字段存在，但**没有代码更新它**
- 收益计算是实时计算的，不存储到数据库

**建议**: 
- **当前方案（推荐）**: 保持实时计算，不存储 `usdt_total`
- **备选方案**: 实现定时任务，定期更新 `usdt_total`（需要权衡准确性和性能）

---

## 📋 数据库更新清单

### ✅ 已完成的更新

1. ✅ **更新 `db/supabase.sql` 文件**
   - 添加 `users.usdt_total` 和 `users.usdt_locked` 字段定义
   - 添加 `claims.energy_awarded` 字段定义
   - 添加 `notifications` 表定义
   - 添加 `system_announcement` 表定义
   - 添加 `system_links` 表定义
   - 添加 `user_holdings` 表定义
   - 添加 `vip_tiers` 表定义
   - 添加所有索引定义
   - 添加表注释和字段注释

2. ✅ **验证字段完整性**
   - 所有后端代码使用的字段都已存在
   - 所有索引都已创建

---

## 🎯 数据库健康度评分

| 项目 | 评分 | 说明 |
|------|------|------|
| 表结构完整性 | ✅ 100% | 所有必需的表和字段都已存在 |
| 索引优化 | ✅ 100% | 所有查询字段都有索引 |
| 数据一致性 | ⚠️ 80% | `usdt_total` 字段未使用，但不影响功能 |
| 代码与数据库同步 | ✅ 100% | 后端代码使用的所有字段都存在 |
| SQL 文件完整性 | ✅ 100% | SQL 文件已更新，包含所有表定义 |

**总体评分**: ✅ **95%** - 数据库结构健康，可以正常使用

---

## 📝 后续建议

### 高优先级（可选）

1. **实现 VIP 等级动态配置**
   - 从 `vip_tiers` 表读取配置，替代硬编码
   - 提供 Admin API 管理 VIP 等级

2. **实现系统配置 API**
   - `GET /api/system/links` - 获取系统链接
   - `GET /api/system/announcement` - 获取系统公告
   - `GET /api/user/notifications` - 获取用户通知

### 低优先级（优化）

1. **使用 `user_holdings` 表缓存**
   - 定期更新用户 RAT 余额和 VIP 等级
   - 减少链上查询次数

2. **添加数据完整性约束**
   - 确保 `energy_locked <= energy_total`
   - 确保 `usdt_locked <= usdt_total`
   - 注意：可能影响性能，建议在应用层处理

---

## ✅ 总结

**数据库状态**: ✅ **健康**

- 所有必需的表和字段都已存在
- 所有索引都已创建
- 后端代码可以正常工作
- SQL 文件已更新，与实际数据库一致

**无需立即执行的数据库迁移**，当前结构已满足所有后端代码需求。

---

## 🔒 Supabase 安全顾问建议

### RLS 策略缺失（INFO 级别）

**问题**: 
- 所有表都启用了 RLS，但没有创建策略
- 当前使用 `SUPABASE_SERVICE_ROLE_KEY`，可以绕过 RLS

**影响**: 
- 如果未来需要前端直接访问数据库，需要配置 RLS 策略
- 当前不影响功能（service role 可以绕过 RLS）

**建议**: 
- **当前方案（推荐）**: 保持现状，使用 service role 访问
- **未来方案**: 如果需要前端直接访问，创建 RLS 策略

**参考**: [Supabase RLS 文档](https://supabase.com/docs/guides/database/database-linter?lint=0008_rls_enabled_no_policy)

---

## ⚡ Supabase 性能顾问建议

### 未使用的索引（INFO 级别）

以下索引未被使用，但可以保留（为未来功能准备）：

1. `idx_users_referrer` - 推荐人查询索引（可能用于未来功能）
2. `idx_cd_resets_referrer_time` - 冷却重置查询索引（可能用于未来功能）
3. `idx_vip_tiers_active_order` - VIP 等级查询索引（可能用于未来功能）
4. `idx_user_holdings_tier` - 持币等级查询索引（可能用于未来功能）
5. `idx_notifications_address_read` - 通知查询索引（可能用于未来功能）
6. `idx_notifications_address_time` - 通知时间查询索引（可能用于未来功能）
7. `idx_claims_address_energy_awarded` - 能量奖励查询索引（用于幂等性检查）
8. `idx_user_holdings_updated` - 持币更新时间索引（可能用于未来功能）

**建议**: 
- 保留这些索引，它们不会造成性能问题
- 如果确定不需要，可以删除以节省存储空间

**参考**: [Supabase 性能优化文档](https://supabase.com/docs/guides/database/database-linter?lint=0005_unused_index)

---

**报告生成时间**: 2024-12-25  
**审查人**: AI 数据库审查系统

