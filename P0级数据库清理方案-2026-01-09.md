# P0级数据库清理方案

**日期**: 2026-01-09  
**修复内容**: 持币生息最低门槛验证（10,000 RAT）  
**清理范围**: 非法收益（脏数据）

---

## 📋 问题分析

### 1. 脏数据定义

**非法收益（脏数据）**：
- 系统记录余额 < 10,000 RAT
- 但 `usdt_total > 0`（存在历史收益）

**原因**：
- 之前的漏洞导致余额 < 10,000 RAT 的用户也产生了收益
- 这些收益违反了业务规则（只有余额 >= 10,000 RAT 才能产生收益）

### 2. 代码层面已修复

✅ **代码层面已经拒绝脏数据**：
- `earnings.ts`：如果余额 < 10,000 或系统记录余额 < 10,000，`validBaseEarnings = 0`
- `withdraw.ts`：如果余额 < 10,000 或系统记录余额 < 10,000，`validBaseEarnings = 0`

**结论**：即使数据库中有脏数据，代码层面也不会使用，系统是安全的。

---

## 🔍 数据库清理方案

### 方案 A：清理脏数据（推荐）

**优点**：
- ✅ 数据一致性：数据库中的数据符合业务规则
- ✅ 便于审计：清理记录保存在审计表中
- ✅ 减少混淆：避免未来维护时的困惑

**缺点**：
- ⚠️ 需要备份数据库
- ⚠️ 不可逆操作（但可以查看审计表）

**执行步骤**：
1. **备份数据库**（必须！）
2. 执行 `db/cleanup_illegal_earnings.sql`
3. 验证清理结果

### 方案 B：保留脏数据（仅审计）

**优点**：
- ✅ 保留历史数据，便于追溯
- ✅ 不需要修改数据库

**缺点**：
- ⚠️ 数据不一致：数据库中有脏数据，但代码层面不使用
- ⚠️ 可能造成混淆

**执行步骤**：
1. 只执行 `db/cleanup_illegal_earnings.sql` 的步骤 1 和步骤 2
2. 不执行步骤 3（清理操作）

---

## 📊 清理脚本说明

### 脚本位置
`rabbit-ai-backend/db/cleanup_illegal_earnings.sql`

### 脚本功能

1. **步骤 1：识别脏数据**
   - 查询所有可能的脏数据用户
   - 基于系统记录余额（claims + referral_rewards）判断
   - 不修改数据，仅查询

2. **步骤 2：创建审计表**
   - 创建 `illegal_earnings_audit` 表
   - 用于记录清理前的数据

3. **步骤 3：清理脏数据**
   - 将脏数据记录到审计表
   - 将 `usdt_total` 清零（如果系统记录余额 < 10,000）

4. **步骤 4：验证清理结果**
   - 查询清理后的脏数据（应该为空）
   - 查询审计表（查看清理记录）

---

## 🚀 执行指南

### 1. 前置准备

**必须操作**：
- ✅ 备份数据库（生产环境）
- ✅ 在测试环境先执行，验证无误
- ✅ 确认代码已部署（代码层面已拒绝脏数据）

### 2. 执行清理

**方式 A：使用 Supabase Dashboard**
1. 登录 Supabase Dashboard
2. 进入 SQL Editor
3. 复制 `db/cleanup_illegal_earnings.sql` 的内容
4. 执行脚本

**方式 B：使用 psql 命令行**
```bash
psql -h <host> -U <user> -d <database> -f db/cleanup_illegal_earnings.sql
```

### 3. 验证结果

**检查项**：
- ✅ 脏数据查询结果为空
- ✅ 审计表中有清理记录
- ✅ 用户 `usdt_total` 已清零（如果系统记录余额 < 10,000）

---

## 📈 预期影响

### 1. 受影响用户

**用户类型**：
- 系统记录余额 < 10,000 RAT
- 但 `usdt_total > 0`

**影响**：
- ⚠️ `usdt_total` 被清零
- ✅ 代码层面已拒绝这些收益，所以不影响提现功能
- ✅ 如果用户当前余额 >= 10,000 RAT（通过外部转账），可以正常产生新收益

### 2. 数据统计

**清理前**：
- 查询脏数据用户数量
- 查询脏数据总金额

**清理后**：
- 脏数据用户数量：0
- 脏数据总金额：0
- 审计表记录：清理记录

---

## ⚠️ 重要提示

### 1. 安全措施

- ✅ **代码层面已拒绝脏数据**：即使不清理，系统也是安全的
- ✅ **清理操作可追溯**：所有清理记录保存在审计表中
- ✅ **清理操作可验证**：可以查询审计表确认清理结果

### 2. 执行建议

**推荐方案**：
1. ✅ 先执行步骤 1（识别脏数据），查看影响范围
2. ✅ 如果影响范围可接受，执行步骤 2 和步骤 3（清理）
3. ✅ 执行步骤 4（验证清理结果）

**不推荐**：
- ❌ 不备份数据库就执行清理
- ❌ 在生产环境直接执行，不先在测试环境验证

### 3. 回滚方案

**如果清理出错**：
- 从审计表恢复数据：
```sql
UPDATE users u
SET usdt_total = (
  SELECT original_usdt_total 
  FROM illegal_earnings_audit 
  WHERE address = u.address 
  ORDER BY cleaned_at DESC 
  LIMIT 1
)
WHERE EXISTS (
  SELECT 1 
  FROM illegal_earnings_audit 
  WHERE address = u.address
);
```

---

## ✅ 总结

### 是否需要清理？

**推荐清理**：
- ✅ 数据一致性
- ✅ 便于审计
- ✅ 减少混淆

**可以不清理**：
- ✅ 代码层面已拒绝脏数据，系统是安全的
- ✅ 如果不想修改数据库，可以只审计不清理

### 执行建议

1. **必须操作**：
   - ✅ 备份数据库
   - ✅ 在测试环境先验证

2. **推荐操作**：
   - ✅ 执行清理脚本
   - ✅ 验证清理结果

3. **可选操作**：
   - ✅ 通知受影响用户（如果需要）

---

**文档维护**：如有更新，请及时更新本文档。

