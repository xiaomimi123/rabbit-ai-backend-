# 持币生息逻辑最终设计文档

**日期**: 2026-01-09  
**目的**: 彻底解决持币生息逻辑的反复修改问题  
**状态**: ✅ 最终版本

---

## 🎯 核心设计原则

### 1. 单一职责原则

**`last_settlement_time` 的唯一含义**：
- **用户首次达到 10,000 RAT 的时间点**
- 或者：用户首次领取时间（如果从未达到 10k）
- **一旦设置，除非特殊情况，不应该被修改**

### 2. 收益计算公式

```
增量收益 = RAT余额 × 代币价格($0.01) × 日利率(%) × 持币天数
持币天数 = (当前时间 - last_settlement_time) / (24 × 3600 × 1000)
```

**关键点**：
- 增量收益基于 **RAT余额**，与 USDT 无关
- `last_settlement_time` 是收益计算的起点，不应该被随意修改

### 3. 业务规则

1. **最低门槛**：只有余额 >= 10,000 RAT 才能产生收益
2. **Lazy Settle**：收益只在提现时固化到 `usdt_total`
3. **外部转账检测**：检测到外部转账时，重置 `last_settlement_time` 为当前时间（防止 Sybil Attack）

---

## 📋 `last_settlement_time` 更新规则

### ✅ 允许更新的情况

| 情况 | 更新逻辑 | 原因 |
|------|---------|------|
| **1. 首次达到10k** | 设置为首次累计余额达到10k的时间 | 初始化收益计算起点 |
| **2. 检测到外部转账** | 设置为当前时间 | 防止 Sybil Attack，避免从账户创建时间开始计算收益 |
| **3. 管理员手动设置** | 设置为管理员指定的时间 | 特殊情况处理 |

### ❌ 不允许更新的情况

| 情况 | 当前状态 | 原因 |
|------|---------|------|
| **1. 管理员赠送USDT** | ✅ 已修复（不更新） | USDT与RAT无关，不应该影响收益计算 |
| **2. 用户提现** | ✅ 已修复（不更新） | 提现不应该改变收益计算起点 |
| **3. 用户领取空投** | ⚠️ **需要重新审视** | 见下方分析 |

---

## 🔍 领取空投时的逻辑分析

### 当前实现：`settle_earnings_on_claim`

**当前逻辑**：
```sql
-- 计算从 last_settlement_time 到领取时间的收益
-- 然后更新 last_settlement_time = 领取时间
UPDATE users
SET usdt_total = v_new_usdt_total,
    last_settlement_time = p_claim_time,  -- ⚠️ 每次领取都更新
    updated_at = now()
WHERE address = p_address;
```

**问题分析**：

**场景1：用户首次达到10k后领取空投**
```
T0: 用户首次达到10k，last_settlement_time = T0
T1: 用户领取空投（余额从10k增加到12k），last_settlement_time = T1
T2: 用户查看收益（T1后1天）

当前行为：
- 增量收益 = 12k × 0.01 × 2% × 1天 = 2.4 USDT
- ✅ 正确：新领取的代币从领取时间开始计算收益
```

**场景2：用户持续领取空投**
```
T0: 用户首次达到10k，last_settlement_time = T0
T1: 用户领取空投，last_settlement_time = T1（更新）
T2: 用户领取空投，last_settlement_time = T2（更新）
T3: 用户查看收益（T2后1天）

当前行为：
- 增量收益 = 当前余额 × 0.01 × 2% × 1天
- ✅ 正确：每次领取都会固化之前的收益，新代币从领取时间开始计算
```

**结论**：
- ✅ **领取空投时更新 `last_settlement_time` 是正确的**
- ✅ 这样可以确保新领取的代币从领取时间开始计算收益
- ✅ 这是 Lazy Settle 机制的一部分

---

## 🎯 最终设计

### 1. `last_settlement_time` 的更新规则（最终版）

| 操作 | 是否更新 | 更新为 | 原因 |
|------|---------|--------|------|
| **首次达到10k** | ✅ 是 | 首次累计余额达到10k的时间 | 初始化收益计算起点 |
| **领取空投** | ✅ 是 | 领取时间 | 固化之前的收益，新代币从领取时间开始计算 |
| **提现** | ❌ 否 | 保持不变 | 提现不应该改变收益计算起点 |
| **管理员赠送USDT** | ❌ 否 | 保持不变 | USDT与RAT无关，不应该影响收益计算 |
| **检测到外部转账** | ✅ 是 | 当前时间 | 防止 Sybil Attack |
| **管理员手动设置** | ✅ 是 | 管理员指定的时间 | 特殊情况处理 |

### 2. 收益计算流程（最终版）

```
1. 读取用户数据
   - RAT余额（从链上读取）
   - usdt_total（已固化的收益）
   - last_settlement_time（收益计算起点）

2. 验证最低门槛
   - 如果余额 < 10,000 RAT：增量收益 = 0
   - 如果系统记录余额 < 10,000 RAT：validBaseEarnings = 0（拒绝脏数据）

3. 计算增量收益
   - daysElapsed = (当前时间 - last_settlement_time) / (24 × 3600 × 1000)
   - incrementalEarnings = balance × 0.01 × dailyRate × daysElapsed
   - 只有余额 >= 10,000 RAT 时才计算

4. 计算总收益
   - grossEarnings = validBaseEarnings + incrementalEarnings
   - validBaseEarnings = baseEarnings（如果余额 >= 10k 或系统记录余额 >= 10k）
   - validBaseEarnings = 0（如果余额 < 10k 或系统记录余额 < 10k）

5. 提现时固化收益（Lazy Settle）
   - newUsdtTotal = grossEarnings - amount
   - 不更新 last_settlement_time（保持收益计算起点）
```

### 3. 关键修复点

#### ✅ 已修复的问题

1. **管理员赠送USDT不更新 `last_settlement_time`** ✅
   - 文件：`src/services/admin.ts`
   - 修复：`updateSettlementTime = false`

2. **提现时不更新 `last_settlement_time`** ✅
   - 文件：`src/services/withdraw.ts`
   - 修复：保留 `originalLastSettlementTime`

3. **最低门槛验证** ✅
   - 文件：`src/services/earnings.ts`, `src/services/withdraw.ts`
   - 修复：只有余额 >= 10,000 RAT 才能产生收益

4. **脏数据过滤** ✅
   - 文件：`src/services/earnings.ts`, `src/services/withdraw.ts`
   - 修复：系统记录余额 < 10,000 时，拒绝历史收益

#### ⚠️ 需要确认的逻辑

1. **领取空投时更新 `last_settlement_time`**
   - 当前实现：每次领取都更新
   - 分析：这是正确的，因为需要固化之前的收益
   - 建议：保持当前实现

---

## 📊 完整的状态机

### `last_settlement_time` 的状态转换

```
初始状态：NULL
    ↓
[首次领取] → 设置为首次领取时间（如果余额 < 10k）
    ↓
[首次达到10k] → 设置为首次累计余额达到10k的时间
    ↓
[领取空投] → 更新为领取时间（固化之前的收益）
    ↓
[提现] → 保持不变 ✅
    ↓
[管理员赠送USDT] → 保持不变 ✅
    ↓
[检测到外部转账] → 更新为当前时间（防止攻击）
```

---

## 🔧 代码实现检查清单

### 1. `earnings.ts` - 收益计算

- ✅ 读取 `last_settlement_time`
- ✅ 检测外部转账（更新 `last_settlement_time`）
- ✅ 初始化首次达到10k的时间
- ✅ 计算增量收益（只有余额 >= 10k）
- ✅ 验证脏数据（系统记录余额 < 10k）

### 2. `withdraw.ts` - 提现处理

- ✅ 读取 `last_settlement_time`
- ✅ 检测外部转账（更新 `last_settlement_time`）
- ✅ 计算增量收益（只有余额 >= 10k）
- ✅ 验证脏数据（系统记录余额 < 10k）
- ✅ 固化收益到 `usdt_total`
- ✅ **不更新 `last_settlement_time`** ✅

### 3. `admin.ts` - 管理员操作

- ✅ 管理员赠送USDT时**不更新 `last_settlement_time`** ✅
- ✅ 管理员可以手动设置 `last_settlement_time`

### 4. `settle_earnings_on_claim` - 领取空投时固化收益

- ✅ 计算从 `last_settlement_time` 到领取时间的收益
- ✅ 固化收益到 `usdt_total`
- ✅ 更新 `last_settlement_time` 为领取时间（这是正确的）

---

## 🎯 为什么之前会反复修改？

### 根本原因

1. **`last_settlement_time` 的含义不清晰**
   - 最初设计时没有明确它的唯一含义
   - 导致多个地方都在更新它，逻辑混乱

2. **缺乏统一的设计文档**
   - 没有明确的状态转换规则
   - 每次修复都是"打补丁"，没有从根本上解决问题

3. **业务逻辑理解不完整**
   - 管理员赠送USDT时不应该更新 `last_settlement_time`（已修复）
   - 提现时不应该更新 `last_settlement_time`（已修复）
   - 但领取空投时应该更新（这是正确的）

### 解决方案

1. ✅ **明确 `last_settlement_time` 的唯一含义**：用户首次达到10k的时间（或首次领取时间）
2. ✅ **明确更新规则**：只有在特定情况下才更新
3. ✅ **统一代码实现**：所有相关代码遵循相同的规则
4. ✅ **完善文档**：记录所有规则和设计决策

---

## ✅ 最终检查清单

### 代码层面

- [x] `earnings.ts`：正确计算收益，不随意更新 `last_settlement_time`
- [x] `withdraw.ts`：提现时不更新 `last_settlement_time`
- [x] `admin.ts`：管理员赠送USDT时不更新 `last_settlement_time`
- [x] `settle_earnings_on_claim`：领取空投时更新 `last_settlement_time`（这是正确的）

### 业务规则

- [x] 最低门槛：只有余额 >= 10,000 RAT 才能产生收益
- [x] 脏数据过滤：系统记录余额 < 10,000 时，拒绝历史收益
- [x] 外部转账检测：检测到外部转账时，重置 `last_settlement_time`
- [x] Lazy Settle：收益只在提现时固化

### 安全措施

- [x] 行锁：防止并发问题
- [x] 原子更新：防止数据丢失
- [x] 最大收益熔断：防止计算错误
- [x] 外部转账检测：防止 Sybil Attack

---

## 📝 总结

### 核心原则

1. **`last_settlement_time` 的唯一含义**：用户首次达到10k的时间（或首次领取时间）
2. **更新规则**：只有在特定情况下才更新（首次达到10k、领取空投、检测到外部转账、管理员手动设置）
3. **不更新规则**：管理员赠送USDT、用户提现时不应该更新

### 当前状态

- ✅ 所有关键问题已修复
- ✅ 代码逻辑已统一
- ✅ 设计文档已完善

### 未来维护

- ✅ 遵循本文档的设计原则
- ✅ 任何修改都要更新本文档
- ✅ 代码审查时参考本文档

---

**文档维护**：这是持币生息逻辑的最终设计文档，任何修改都要参考本文档并更新。

