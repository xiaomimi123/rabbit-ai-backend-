# Rabbit AI 后端技术文档

**版本**: v1.0  
**更新日期**: 2026-01-09  
**维护人员**: 开发团队

---

## 📋 目录

1. [项目概述](#项目概述)
2. [技术栈](#技术栈)
3. [项目结构](#项目结构)
4. [核心模块](#核心模块)
5. [数据库设计](#数据库设计)
6. [安全机制](#安全机制)
7. [配置管理](#配置管理)
8. [部署说明](#部署说明)
9. [开发指南](#开发指南)

---

## 1. 项目概述

### 1.1 项目简介

Rabbit AI Backend 是一个基于 Node.js + Fastify + TypeScript 的 Web3 后端服务，为 Rabbit AI 协议提供完整的后端支持。主要功能包括：

- 🎯 **空投领取验证和处理**：验证链上交易，处理空投领取，分配能量值
- 💰 **用户收益计算和管理**：持币生息收益计算，VIP 等级管理
- 💸 **提现处理和管理**：提现申请、审核、自动放款
- 👥 **团队关系和推荐奖励**：推荐人系统，团队奖励分配
- 📊 **管理员后台 API**：用户管理、财务审核、系统配置
- 🔔 **通知系统**：个人通知、广播通知
- 📈 **访问统计和分析**：访问记录、地理位置统计
- 🤖 **Telegram Bot 集成**：提现通知、系统告警
- 🔄 **区块链事件索引器**：自动同步链上事件

### 1.2 核心特性

- ✅ **高并发安全**：使用数据库行锁和原子更新，防止并发问题
- ✅ **推荐人终身制**：推荐关系一次绑定，终身有效
- ✅ **Lazy Settle 机制**：按需结算收益，提高性能
- ✅ **Sybil Attack 防护**：检测外部转账，防止攻击
- ✅ **动态配置**：能量配置、VIP 配置支持动态调整
- ✅ **完整的审计日志**：所有关键操作都有审计记录

---

## 2. 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Node.js | - | 运行时环境 |
| TypeScript | ^5.8.2 | 类型安全 |
| Fastify | ^5.2.1 | Web 框架（高性能） |
| Supabase | ^2.49.1 | 数据库和认证 |
| Ethers.js | ^5.7.2 | 区块链交互 |
| Zod | ^3.24.1 | 数据验证 |
| Telegram Bot API | ^0.67.0 | Telegram 通知 |
| MaxMind GeoLite2 | ^5.0.3 | IP 地理位置 |
| Sentry | ^8.48.0 | 错误监控 |

---

## 3. 项目结构

```
rabbit-ai-backend/
├── src/
│   ├── api/                    # API 路由和中间件
│   │   ├── routes/            # 路由定义
│   │   │   ├── admin.ts       # 管理员 API
│   │   │   ├── asset.ts       # 资产相关 API
│   │   │   ├── mining.ts      # 空投领取 API
│   │   │   ├── user.ts        # 用户相关 API
│   │   │   ├── analytics.ts   # 访问统计 API
│   │   │   ├── vip.ts         # VIP 配置 API
│   │   │   └── ...
│   │   ├── schemas.ts         # Zod 验证模式
│   │   ├── errors.ts          # 错误处理
│   │   └── adminAuth.ts       # 管理员认证
│   ├── services/              # 业务逻辑服务
│   │   ├── verifyClaim.ts     # 空投领取验证
│   │   ├── withdraw.ts        # 提现处理
│   │   ├── earnings.ts        # 收益计算
│   │   ├── admin.ts           # 管理员服务
│   │   ├── user.ts            # 用户服务
│   │   ├── vip.ts             # VIP 等级服务
│   │   ├── vipConfig.ts       # VIP 配置管理
│   │   ├── energyConfig.ts    # 能量配置管理
│   │   ├── autoPayout.ts      # 自动放款服务
│   │   ├── analytics.ts       # 访问统计
│   │   ├── notifications.ts  # 通知服务
│   │   └── telegram.ts        # Telegram Bot
│   ├── infra/                 # 基础设施
│   │   ├── supabase.ts        # Supabase 客户端
│   │   ├── rpcPool.ts         # RPC 节点池
│   │   └── abis.ts            # 智能合约 ABI
│   ├── indexer/               # 区块链索引器
│   │   └── indexer.ts         # 事件索引逻辑
│   ├── config.ts              # 配置管理
│   ├── index.ts               # 应用入口
│   └── server.ts              # 服务器配置
├── db/                        # 数据库迁移脚本
│   ├── fix_referrer_lifetime_lock.sql
│   ├── create_withdraw_safe_function.sql
│   ├── settle_earnings_on_claim.sql
│   └── ...
├── scripts/                   # 工具脚本
└── package.json
```

---

## 4. 核心模块

### 4.1 空投领取验证（verifyClaim.ts）

**功能**：
- 验证链上交易（交易哈希、发送方、接收方）
- 解析智能合约事件（Claimed、ReferralReward、CooldownReset）
- 调用数据库函数处理领取和能量分配
- 处理推荐奖励和冷却时间重置

**关键特性**：
- ✅ 幂等性保护（重复交易自动跳过）
- ✅ 推荐人地址终身制（使用数据库中的推荐人，忽略前端传入）
- ✅ 动态能量配置（从数据库读取）
- ✅ 收益固化（领取前固化收益）

**数据库函数**：
- `process_claim_energy`：原子操作，处理领取和能量分配

### 4.2 收益计算（earnings.ts）

**功能**：
- 从链上读取 RAT 余额
- 计算持币生息收益
- 确定 VIP 等级和日利率
- 检测外部转账（Sybil Attack 防护）

**核心算法**：
```
增量收益 = Balance × TOKEN_PRICE × (dailyRate / 100) × daysElapsed
实时总收益 = baseEarnings + incrementalEarnings
```

**关键特性**：
- ✅ RPC 超时保护（8秒）
- ✅ 外部转账检测（防止 Sybil Attack）
- ✅ 最大收益熔断限制（防止计算错误）
- ✅ 首次达到 10k 自动初始化结算时间

**详细说明**：参见 [收益计算报告](./后端收益计算报告-2026-01-09.md)

### 4.3 提现处理（withdraw.ts）

**功能**：
- 验证提现金额和能量
- Lazy Settle 收益计算
- 锁定能量和 USDT
- 创建提现记录

**关键特性**：
- ✅ Lazy Settle 机制（提现时计算收益）
- ✅ 外部转账检测
- ✅ 行锁防止并发问题
- ✅ 动态能量配置

**数据库函数**：
- `process_withdraw_safe`：原子操作，处理提现和锁定

### 4.4 管理员服务（admin.ts）

**功能**：
- 用户管理（查看、编辑、能量/USDT 调整）
- 提现审核（通过/拒绝）
- 系统配置（能量配置、VIP 配置）
- 数据统计（KPI、收益统计）

**关键特性**：
- ✅ 管理员密钥验证
- ✅ 原子操作确保数据一致性
- ✅ 操作审计日志

**数据库函数**：
- `admin_adjust_user_energy_safe`：管理员能量调整
- `admin_adjust_user_usdt_safe`：管理员 USDT 调整

### 4.5 自动放款服务（autoPayout.ts）

**功能**：
- 自动处理提现（每 30 秒检查一次）
- 私钥加密存储（AES-256-GCM）
- 交易签名和发送
- 放款日志记录

**关键特性**：
- ✅ 私钥加密存储
- ✅ 阈值限制
- ✅ 余额检查
- ✅ 交易验证

### 4.6 VIP 配置管理（vipConfig.ts）

**功能**：
- 从数据库加载 VIP 配置到内存
- 根据余额确定 VIP 等级和日利率
- 配置缓存（60秒 TTL）

**默认配置**：
- 🌱 新手：10,000 - 49,999 RAT，日利率 2%
- 🌿 进阶：50,000 - 99,999 RAT，日利率 4%
- 🌳 资深：100,000 - 199,999 RAT，日利率 6%
- 💎 核心：200,000+ RAT，日利率 10%

### 4.7 能量配置管理（energyConfig.ts）

**功能**：
- 动态管理能量消耗和奖励规则
- 配置历史记录
- 配置缓存（60秒 TTL）

**配置项**：
- `withdraw_energy_ratio`：提现能量消耗比例（默认 10）
- `claim_self_reward`：用户领取空投奖励（默认 1）
- `claim_referrer_first`：推荐人首次邀请奖励（默认 3）
- `claim_referrer_repeat`：推荐人非首次奖励（默认 1）

---

## 5. 数据库设计

### 5.1 核心表

#### users 表
- `address` (PK)：用户钱包地址
- `referrer_address`：推荐人地址（终身制）
- `energy_total`：总能量值
- `energy_locked`：锁定能量值
- `usdt_total`：已固化收益（USDT）
- `usdt_locked`：锁定 USDT
- `last_settlement_time`：上次结算时间
- `invite_count`：邀请数量
- `created_at`、`updated_at`：时间戳

#### claims 表
- `tx_hash` (PK)：交易哈希
- `address`：用户地址
- `referrer`：推荐人地址（终身制）
- `amount_wei`：领取金额（Wei）
- `fee_amount_wei`：手续费（BNB Wei）
- `block_number`：区块号
- `block_time`：区块时间
- `energy_awarded`：是否已奖励能量
- `status`：状态
- `created_at`：创建时间

#### withdrawals 表
- `id` (PK)：提现 ID
- `address`：用户地址
- `amount`：提现金额（USDT）
- `status`：状态（Pending/Completed/Rejected）
- `energy_locked_amount`：锁定能量值
- `payout_tx_hash`：放款交易哈希
- `created_at`、`updated_at`：时间戳

#### referral_rewards 表
- `tx_hash` (PK)：交易哈希
- `referrer_address`：推荐人地址
- `claimer_address`：被推荐人地址
- `amount_wei`：奖励金额（Wei）
- `block_number`：区块号
- `block_time`：区块时间
- `created_at`：创建时间

### 5.2 数据库函数

#### process_claim_energy
**功能**：处理空投领取和能量分配（原子操作）

**参数**：
- `p_tx_hash`：交易哈希
- `p_address`：用户地址
- `p_referrer`：推荐人地址（仅首次绑定有效）
- `p_amount_wei`：领取金额（Wei）
- `p_block_number`：区块号
- `p_block_time`：区块时间
- `p_fee_amount_wei`：手续费（BNB Wei）
- `p_self_reward`：用户自己领取奖励（能量）
- `p_referrer_first`：推荐人首次邀请奖励（能量）
- `p_referrer_repeat`：推荐人非首次奖励（能量）

**特性**：
- ✅ 推荐人地址终身制（如果用户已有推荐人，忽略传入参数）
- ✅ 行锁防止并发问题
- ✅ 原子更新能量值
- ✅ 能量审计日志

#### process_withdraw_safe
**功能**：安全提现处理（原子操作）

**参数**：
- `p_address`：用户地址
- `p_amount`：提现金额（USDT）
- `p_required_energy`：所需能量
- `p_new_usdt_total`：新的已固化收益
- `p_original_last_settlement_time`：原始结算时间

**特性**：
- ✅ 行锁防止并发问题
- ✅ 原子更新能量和 USDT
- ✅ 验证能量和 USDT 余额

#### settle_earnings_on_claim
**功能**：在领取空投时固化收益（原子操作）

**参数**：
- `p_address`：用户地址
- `p_old_balance`：领取前的余额
- `p_claim_time`：领取时间

**特性**：
- ✅ 原子更新 `usdt_total`
- ✅ 更新 `last_settlement_time`

#### admin_adjust_user_energy_safe
**功能**：管理员安全调整用户能量（原子操作）

**特性**：
- ✅ 行锁防止并发问题
- ✅ 原子更新能量值
- ✅ 操作审计日志

#### admin_adjust_user_usdt_safe
**功能**：管理员安全调整用户 USDT（原子操作）

**特性**：
- ✅ 行锁防止并发问题
- ✅ 原子更新 USDT
- ✅ 操作审计日志

---

## 6. 安全机制

### 6.1 并发安全

**问题**：高并发场景下可能出现数据不一致

**解决方案**：
- ✅ 使用数据库行锁（`FOR UPDATE`）
- ✅ 使用原子更新（`SET field = field + value`）
- ✅ 所有关键操作都封装在数据库函数中

**受保护的操作**：
- 空投领取和能量分配
- 提现处理
- 管理员调整能量/USDT
- 收益结算

### 6.2 推荐人地址终身制

**问题**：前端可能传入不同的推荐人地址，导致奖励分配错误

**解决方案**：
- ✅ 推荐关系一次绑定，终身有效
- ✅ 如果用户已有推荐人，强制使用数据库中的值
- ✅ 只有新用户（没有推荐人）才接受前端传入的参数

### 6.3 Sybil Attack 防护

**问题**：用户通过外部转账获得代币后，系统可能从账户创建时间开始计算收益

**解决方案**：
- ✅ 检测外部转账（链上余额 > 系统记录余额 × 1.1）
- ✅ 如果检测到外部转账，使用当前时间作为起息日
- ✅ 最大收益熔断限制（防止计算错误）

### 6.4 幂等性保护

**问题**：重复请求可能导致重复处理

**解决方案**：
- ✅ 交易哈希唯一性检查
- ✅ 数据库函数内部幂等性保护
- ✅ 重复交易自动跳过

### 6.5 管理员认证

**问题**：管理员 API 需要安全认证

**解决方案**：
- ✅ 使用管理员密钥（`ADMIN_API_KEY`）
- ✅ 所有管理员 API 都需要密钥验证
- ✅ 401 错误自动清除密钥

---

## 7. 配置管理

### 7.1 环境变量

**必需变量**：
- `SUPABASE_URL`：Supabase 项目 URL
- `SUPABASE_SERVICE_ROLE_KEY`：Supabase Service Role Key
- `BSC_RPC_URLS`：BSC RPC 节点 URL（逗号分隔）
- `AIRDROP_CONTRACT`：空投合约地址
- `RAT_TOKEN_CONTRACT`：RAT 代币合约地址

**可选变量**：
- `ADMIN_RPC_URL`：管理员专用 RPC URL
- `USDT_CONTRACT`：USDT 合约地址
- `ADMIN_PAYOUT_ADDRESS`：管理员放款地址
- `ADMIN_API_KEY`：管理员 API 密钥
- `TELEGRAM_BOT_TOKEN`：Telegram Bot Token
- `TELEGRAM_ADMIN_CHAT_ID`：Telegram 管理员 Chat ID
- `SENTRY_DSN`：Sentry DSN（错误监控）
- `MAXMIND_LICENSE_KEY`：MaxMind License Key

**完整列表**：参见 `env.example`

### 7.2 动态配置

**VIP 配置**：
- 存储在 `vip_tiers` 表
- 启动时加载到内存
- 缓存 60 秒，自动刷新

**能量配置**：
- 存储在 `energy_config` 表
- 支持动态调整（无需重启）
- 缓存 60 秒

---

## 8. 部署说明

### 8.1 部署环境

**推荐平台**：Render.com

**环境要求**：
- Node.js 18+
- PostgreSQL（Supabase）
- BSC RPC 节点

### 8.2 部署步骤

1. **配置环境变量**
   - 复制 `env.example` 到 `.env`
   - 填写所有必需的环境变量

2. **执行数据库迁移**
   - 在 Supabase SQL Editor 中执行 `db/` 目录下的 SQL 脚本
   - 按顺序执行（参考脚本文件名）

3. **启动服务**
   ```bash
   npm install
   npm run build
   npm start
   ```

### 8.3 健康检查

**端点**：`GET /health`

**响应**：
```json
{
  "ok": true,
  "timestamp": "2026-01-09T12:00:00.000Z"
}
```

---

## 9. 开发指南

### 9.1 代码规范

- 使用 TypeScript 严格模式
- 使用 ESLint 和 Prettier
- 函数注释使用 JSDoc
- 错误处理使用 `ApiError`

### 9.2 添加新 API

1. 在 `src/api/routes/` 创建路由文件
2. 在 `src/api/schemas.ts` 定义验证模式
3. 在 `src/services/` 实现业务逻辑
4. 在 `src/server.ts` 注册路由

### 9.3 添加新数据库函数

1. 在 `db/` 目录创建 SQL 文件
2. 使用 `SECURITY DEFINER` 和 `SET search_path`
3. 使用行锁（`FOR UPDATE`）和原子更新
4. 添加错误处理和日志

### 9.4 测试

**单元测试**：
```bash
npm test
```

**集成测试**：
- 使用 Postman 或类似工具
- 测试所有 API 端点
- 验证数据库函数

### 9.5 调试

**日志级别**：
- `console.log`：信息日志
- `console.warn`：警告日志
- `console.error`：错误日志

**Sentry 监控**：
- 自动捕获未处理的错误
- 记录错误堆栈和上下文

---

## 10. 常见问题

### 10.1 数据库函数不存在

**错误**：`function process_claim_energy does not exist`

**解决方案**：
1. 检查是否执行了数据库迁移脚本
2. 在 Supabase SQL Editor 中执行 `db/fix_process_claim_energy_block_time.sql`

### 10.2 RPC 调用超时

**错误**：`RPC_TIMEOUT`

**解决方案**：
1. 检查 RPC 节点是否正常
2. 配置多个 RPC 节点（自动轮换）
3. 使用 `ADMIN_RPC_URL` 配置专用节点

### 10.3 收益计算错误

**问题**：用户收益计算不正确

**解决方案**：
1. 检查 `last_settlement_time` 是否正确
2. 检查是否检测到外部转账
3. 查看收益计算日志

---

## 11. 更新日志

### 2026-01-09
- ✅ 实现推荐人地址终身制
- ✅ 修复高并发安全问题
- ✅ 实现 Lazy Settle 机制
- ✅ 添加 Sybil Attack 防护
- ✅ 实现动态能量配置

---

**文档维护**：如有更新，请及时更新本文档。

