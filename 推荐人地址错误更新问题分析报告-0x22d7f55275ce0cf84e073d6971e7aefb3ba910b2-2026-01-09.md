# 推荐人地址错误更新问题分析报告

**分析日期**: 2026-01-09  
**用户地址**: `0x22d7f55275ce0cf84e073d6971e7aefb3ba910b2`  
**问题描述**: 用户反映"5号注册的朋友的奖励，跑到了昨天或前天注册的那个人那里去了"

---

## 🔍 问题发现

### 1. 问题现象

用户反映：
- "我那个朋友好像是5号注册的，另外一个朋友是昨天还是前天注册的，但5号注册那个朋友的奖励，跑到昨天还是前天注册的那个人那里去了"
- "为啥我分享的直推，都到我朋友那个账号上去了"

### 2. 数据验证

**查询结果**:
- 用户 `0x22d7f55275ce0cf84e073d6971e7aefb3ba910b2` 有 **34个下级**
- 所有下级的**首次领取**时，推荐人地址都是正确的（`0x22d7f55275ce0cf84e073d6971e7aefb3ba910b2`）
- 但是，**很多下级在后续领取时，推荐人地址被错误更新**为 `0xfb39b86531e4e48b64281ea4194f1a0cb6fe0d92`

**受影响的下级**（部分列表）:
1. `0x0bc18eb8351356e5a3206daf73b5986deb13c947` - 第3次领取时推荐人变成 `0xfb39b86531e4e48b64281ea4194f1a0cb6fe0d92`
2. `0x1d5192a7bd4fd46d1503d471e4fa90d69f909786` - 第3次领取时推荐人变成 `0xfb39b86531e4e48b64281ea4194f1a0cb6fe0d92`
3. `0x643484d2ee203159bcaef4bb5acf7bb7bf07efdc` - 第9次和第10次领取时推荐人变成 `0xfb39b86531e4e48b64281ea4194f1a0cb6fe0d92`
4. `0x7fd5b2b260ef6ea1931843d150c414a95ac4e298` - 第4次领取时推荐人变成 `0xfb39b86531e4e48b64281ea4194f1a0cb6fe0d92`
5. `0x82a20a3c04a5e0d5de3feb6c9b0393cb8fd6f874` - 第3次和第4次领取时推荐人变成 `0xfb39b86531e4e48b64281ea4194f1a0cb6fe0d92`
6. ... 还有更多

---

## 🔎 根本原因分析

### 1. 代码逻辑问题

**前端逻辑** (`rabbit-ai-frontendxin/views/MiningView.tsx`):
```typescript
// 获取推荐人地址（优先级：localStorage > URL > 默认值）
const getReferrer = (): string => {
  const fromStorage = getReferrerFromStorage();
  if (fromStorage !== '0x0000000000000000000000000000000000000000') {
    return fromStorage;
  }
  const fromUrl = getReferrerFromUrl();
  if (fromUrl !== '0x0000000000000000000000000000000000000000') {
    // 如果 URL 中有 ref，保存到 localStorage
    saveReferrerToStorage(fromUrl);
    return fromUrl;
  }
  return '0x0000000000000000000000000000000000000000';
};
```

**问题**:
- 前端每次领取时都会从 `localStorage` 获取推荐人地址
- 如果用户在不同时间访问页面时，`localStorage` 中的推荐人地址可能被更新（比如用户点击了新的推荐链接）
- 这导致后续领取时使用了错误的推荐人地址

**后端逻辑** (`rabbit-ai-backend/src/services/verifyClaim.ts`):
```typescript
export async function verifyClaim(params: { provider: ethers.providers.Provider; address: string; txHash: string; referrer: string }) {
  // ...
  const validReferrer = params.referrer.toLowerCase();  // ⚠️ 直接使用前端传来的推荐人地址
  
  // 调用数据库函数
  const { data: rpcResult } = await supabase.rpc('process_claim_energy', {
    p_tx_hash: txHash,
    p_address: address,
    p_referrer: validReferrer,  // ⚠️ 每次领取时都使用前端传来的推荐人地址
    // ...
  });
}
```

**问题**:
- 后端每次领取时都使用前端传来的推荐人地址
- 没有检查用户是否已经注册过（首次领取）
- 没有使用首次注册时的推荐人地址

### 2. 数据库函数问题

**数据库函数** (`process_claim_energy`):
```sql
INSERT INTO claims (
  tx_hash, 
  address, 
  referrer,  -- ⚠️ 每次领取时都使用传入的推荐人地址
  -- ...
)
VALUES (
  p_tx_hash, 
  p_address, 
  v_ref_address,  -- ⚠️ 这是从前端传来的推荐人地址
  -- ...
);
```

**问题**:
- 数据库函数每次领取时都使用传入的推荐人地址
- 没有检查用户是否已经注册过（首次领取）
- 没有使用首次注册时的推荐人地址

---

## 💡 解决方案

### 方案1：使用首次注册时的推荐人地址（推荐）

**修改数据库函数**:
```sql
-- 在 process_claim_energy 函数中
-- 1. 检查用户是否已经注册过（首次领取）
SELECT referrer INTO v_existing_referrer
FROM claims
WHERE address = p_address
ORDER BY created_at ASC
LIMIT 1;

-- 2. 如果用户已经注册过，使用首次注册时的推荐人地址
IF v_existing_referrer IS NOT NULL THEN
  v_ref_address := v_existing_referrer;
ELSE
  -- 首次领取，使用传入的推荐人地址
  v_ref_address := lower(p_referrer);
END IF;
```

**优点**:
- ✅ 确保推荐人地址在用户生命周期内保持不变
- ✅ 防止推荐人地址被错误更新
- ✅ 符合业务逻辑（推荐人应该在首次注册时确定）

### 方案2：从 users 表获取推荐人地址

**修改数据库函数**:
```sql
-- 在 process_claim_energy 函数中
-- 1. 从 users 表获取推荐人地址
SELECT referrer_address INTO v_ref_address
FROM users
WHERE address = p_address;

-- 2. 如果 users 表中没有，使用传入的推荐人地址（首次注册）
IF v_ref_address IS NULL OR v_ref_address = '0x0000000000000000000000000000000000000000' THEN
  v_ref_address := lower(p_referrer);
END IF;
```

**优点**:
- ✅ 使用 users 表中的推荐人地址（更权威）
- ✅ 防止推荐人地址被错误更新
- ✅ 符合业务逻辑（推荐人应该在首次注册时确定）

### 方案3：前端不再更新 localStorage 中的推荐人地址

**修改前端逻辑**:
```typescript
// 只在首次访问时设置推荐人地址，后续不再更新
const getReferrer = (): string => {
  const fromStorage = getReferrerFromStorage();
  if (fromStorage !== '0x0000000000000000000000000000000000000000') {
    return fromStorage;  // 如果已经设置过，不再更新
  }
  const fromUrl = getReferrerFromUrl();
  if (fromUrl !== '0x0000000000000000000000000000000000000000') {
    saveReferrerToStorage(fromUrl);  // 只在首次访问时设置
    return fromUrl;
  }
  return '0x0000000000000000000000000000000000000000';
};
```

**缺点**:
- ⚠️ 这只是前端修复，如果用户清除 localStorage 或使用不同设备，问题仍然存在
- ⚠️ 不能修复已经错误的数据

---

## 📊 影响评估

### 1. 受影响的数据

**统计**:
- 受影响的下级用户：**至少 20+ 个**
- 错误的推荐奖励记录：**至少 30+ 条**
- 错误的推荐人地址：主要是 `0xfb39b86531e4e48b64281ea4194f1a0cb6fe0d92`

### 2. 奖励分配错误

**影响**:
- 用户 `0x22d7f55275ce0cf84e073d6971e7aefb3ba910b2` 应该获得的推荐奖励被错误分配给了 `0xfb39b86531e4e48b64281ea4194f1a0cb6fe0d92`
- 这导致用户损失了推荐奖励（能量值和RAT代币）

---

## 🚨 紧急修复建议

### 1. 立即修复数据库函数

**修改 `process_claim_energy` 函数**:
- 检查用户是否已经注册过（首次领取）
- 如果已经注册过，使用首次注册时的推荐人地址
- 如果首次注册，使用传入的推荐人地址

### 2. 修复历史数据

**修复脚本**:
- 找出所有推荐人地址被错误更新的记录
- 将这些记录的推荐人地址更新为首次注册时的推荐人地址
- 重新计算推荐奖励

### 3. 验证修复

**验证步骤**:
- 测试新用户首次领取
- 测试已注册用户后续领取
- 验证推荐人地址不再被错误更新

---

## 📝 总结

### 问题根源

1. **前端问题**: 每次领取时都从 `localStorage` 获取推荐人地址，可能被更新
2. **后端问题**: 每次领取时都使用前端传来的推荐人地址，没有检查用户是否已经注册过
3. **数据库问题**: 每次领取时都使用传入的推荐人地址，没有使用首次注册时的推荐人地址

### 解决方案

**推荐使用方案1或方案2**:
- 在数据库函数中检查用户是否已经注册过
- 如果已经注册过，使用首次注册时的推荐人地址
- 如果首次注册，使用传入的推荐人地址

### 优先级

**P0级问题**:
- 这导致推荐奖励被错误分配
- 影响用户信任和系统公平性
- 需要立即修复

---

**分析人**: AI Assistant  
**分析时间**: 2026-01-09  
**状态**: ⚠️ **P0级问题，需要立即修复**

