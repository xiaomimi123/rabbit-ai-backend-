# 数据库安全审查报告

**日期**: 2026-01-09  
**审查范围**: Supabase 数据库安全配置  
**状态**: 🔴 发现严重安全问题

---

## 📋 执行摘要

### 当前架构分析

**后端访问方式**:
- ✅ 后端使用 `service_role` key 访问 Supabase
- ✅ `service_role` key 可以绕过 RLS 限制
- ✅ 后端服务不受 RLS 影响

**前端访问方式**:
- ✅ 前端不直接访问 Supabase（通过后端 API）
- ⚠️ 如果前端直接访问，会使用 `anon` key

**风险评估**:
- 🔴 **PostgREST API 直接暴露**: 如果 `anon` key 泄露，所有未启用 RLS 的表都可以被访问
- 🔴 **API 端点暴露**: Supabase 的 PostgREST API 默认对所有表开放
- 🟡 **后端安全**: 后端使用 `service_role`，相对安全

### 安全风险概览

| 风险级别 | 数量 | 说明 |
|---------|------|------|
| 🔴 **严重（CRITICAL）** | 9 | RLS 未启用 + 敏感列暴露 |
| 🟠 **高（HIGH）** | 12 | RLS 已启用但无策略 |
| 🟡 **中（MEDIUM）** | 4 | 函数搜索路径可变 |
| ✅ **安全** | 0 | 无 |

### 关键发现

1. **8 个表未启用 RLS**（CRITICAL）
   - 包含敏感数据：加密私钥、管理员操作、用户数据备份
   - 这些表完全暴露在 PostgREST API 中

2. **12 个表已启用 RLS 但无策略**（HIGH）
   - RLS 已启用但没有任何策略，实际上等同于禁用
   - 包括用户表、提现表等核心业务表

3. **敏感列暴露**（CRITICAL）
   - `page_visits.session_id` 未受保护

4. **函数安全配置问题**（MEDIUM）
   - 4 个函数缺少 `search_path` 设置

---

## 🔴 严重安全问题（CRITICAL）

### 1. RLS 未启用的表（8 个）

#### 1.1 `admin_operations` - 管理员操作记录表

**风险级别**: 🔴 CRITICAL

**表信息**:
- RLS 状态: ❌ **DISABLED**
- 行数: 3
- 用途: 记录所有管理员操作（能量/USDT 调整）

**敏感字段**:
- `admin_address`: 管理员地址
- `address`: 被操作用户地址
- `operation_type`: 操作类型
- `amount`: 操作金额
- `amount_before` / `amount_after`: 操作前后金额
- `notes`: 操作备注

**风险**:
- ✅ **完全暴露**: 任何人都可以通过 PostgREST API 查看所有管理员操作
- ✅ **审计泄露**: 管理员操作记录完全暴露
- ✅ **用户隐私**: 被操作用户地址和金额信息泄露

**建议**:
- 🔒 启用 RLS
- 🔒 创建策略：仅管理员可查看（通过 JWT 验证）

---

#### 1.2 `auto_payout_config` - 自动放款配置表

**风险级别**: 🔴 CRITICAL

**表信息**:
- RLS 状态: ❌ **DISABLED**
- 行数: 1
- 用途: 存储自动放款配置（包括加密私钥）

**敏感字段**:
- `private_key_encrypted`: **加密后的私钥**（最敏感）
- `wallet_address`: 钱包地址
- `threshold_usdt`: 自动放款阈值
- `enabled`: 是否启用
- `min_balance_usdt`: 最小余额阈值
- `daily_limit_usdt`: 每日限额

**风险**:
- ✅ **极度危险**: 加密私钥完全暴露（即使加密，也不应暴露）
- ✅ **配置泄露**: 自动放款配置完全暴露
- ✅ **安全漏洞**: 攻击者可以查看钱包地址和配置

**建议**:
- 🔒 **立即启用 RLS**（最高优先级）
- 🔒 创建策略：仅后端服务可访问（使用 service_role key）
- 🔒 禁止通过 PostgREST API 访问（建议使用数据库函数封装）

---

#### 1.3 `auto_payout_logs` - 自动放款日志表

**风险级别**: 🔴 CRITICAL

**表信息**:
- RLS 状态: ❌ **DISABLED**
- 行数: 5
- 用途: 记录所有自动放款操作历史

**敏感字段**:
- `withdrawal_id`: 提现记录 ID
- `amount`: 放款金额
- `tx_hash`: 交易哈希
- `status`: 状态（success/failed/pending）
- `error_message`: 错误信息

**风险**:
- ✅ **财务信息泄露**: 所有自动放款记录完全暴露
- ✅ **用户隐私**: 提现记录关联用户地址
- ✅ **交易信息**: 交易哈希和金额信息泄露

**建议**:
- 🔒 启用 RLS
- 🔒 创建策略：仅管理员可查看

---

#### 1.4 `energy_config` - 能量配置表

**风险级别**: 🔴 CRITICAL

**表信息**:
- RLS 状态: ❌ **DISABLED**
- 行数: 6
- 用途: 存储能量相关的动态配置

**敏感字段**:
- `config_key`: 配置键
- `config_value`: 配置值
- `description`: 配置描述

**风险**:
- ✅ **业务逻辑泄露**: 能量配置完全暴露
- ✅ **系统参数泄露**: 攻击者可以查看所有系统参数

**建议**:
- 🔒 启用 RLS
- 🔒 创建策略：仅后端服务可访问（或仅管理员可查看）

---

#### 1.5 `energy_config_history` - 能量配置历史表

**风险级别**: 🔴 CRITICAL

**表信息**:
- RLS 状态: ❌ **DISABLED**
- 行数: 2
- 用途: 记录能量配置变更历史（用于审计）

**敏感字段**:
- `config_key`: 配置键
- `old_value` / `new_value`: 旧值/新值
- `changed_by`: 变更人
- `change_reason`: 变更原因

**风险**:
- ✅ **审计信息泄露**: 配置变更历史完全暴露
- ✅ **操作人信息泄露**: 变更人地址泄露

**建议**:
- 🔒 启用 RLS
- 🔒 创建策略：仅管理员可查看

---

#### 1.6 `ip_geo_cache` - IP 地理位置缓存表

**风险级别**: 🔴 CRITICAL

**表信息**:
- RLS 状态: ❌ **DISABLED**
- 行数: 540
- 用途: 缓存 IP 地址的地理位置信息

**敏感字段**:
- `ip_address`: IP 地址
- `country`: 国家名称
- `country_code`: 国家代码
- `city`: 城市名称

**风险**:
- ✅ **用户位置泄露**: IP 地址和地理位置信息完全暴露
- ✅ **隐私泄露**: 可能违反 GDPR/隐私法规

**建议**:
- 🔒 启用 RLS
- 🔒 创建策略：仅后端服务可访问（不对外暴露）

---

#### 1.7 `page_visits` - 页面访问记录表

**风险级别**: 🔴 CRITICAL

**表信息**:
- RLS 状态: ❌ **DISABLED**
- 行数: 4,317
- 用途: 记录用户访问前端的相关信息

**敏感字段**:
- `ip_address`: IP 地址
- `wallet_address`: 钱包地址
- `session_id`: **会话 ID**（敏感列）
- `user_agent`: 浏览器信息
- `referrer`: 推荐人地址
- `country` / `city`: 地理位置

**风险**:
- ✅ **极度危险**: 包含敏感列 `session_id`，且 RLS 未启用
- ✅ **用户隐私泄露**: IP 地址、钱包地址、会话 ID 完全暴露
- ✅ **地理位置泄露**: 用户地理位置信息泄露
- ✅ **行为追踪**: 用户访问行为完全暴露

**建议**:
- 🔒 **立即启用 RLS**（最高优先级）
- 🔒 创建策略：仅后端服务可访问（不对外暴露）
- 🔒 考虑脱敏处理敏感列

---

#### 1.8 `users_backup_20260105` - 用户数据备份表

**风险级别**: 🔴 CRITICAL

**表信息**:
- RLS 状态: ❌ **DISABLED**
- 行数: 1
- 用途: 用户数据备份（2026-01-05）

**敏感字段**:
- 包含 `users` 表的所有字段
- 用户地址、推荐关系、能量、USDT 余额等

**风险**:
- ✅ **用户数据泄露**: 备份数据完全暴露
- ✅ **历史数据泄露**: 可能包含已删除或修改的用户数据

**建议**:
- 🔒 启用 RLS
- 🔒 **建议**: 如果不再需要，考虑删除此备份表
- 🔒 如果保留，创建策略：仅管理员可访问

---

## 🟠 高风险问题（HIGH）

### 2. RLS 已启用但无策略的表（12 个）

**问题描述**: 
这些表已启用 RLS，但没有任何策略。在 Supabase 中，如果 RLS 已启用但没有策略，**默认拒绝所有访问**（包括通过 PostgREST API）。

**影响**:
- ✅ 如果后端使用 `service_role` key，可以正常访问
- ❌ 如果前端或 API 使用 `anon` key，将无法访问
- ⚠️ 需要确认当前访问方式

**受影响的表**:
1. `chain_sync_state` - 链同步状态
2. `claims` - 空投领取记录
3. `cooldown_resets` - 冷却重置记录
4. `notifications` - 用户通知
5. `referral_rewards` - 推荐奖励
6. `system_announcement` - 系统公告
7. `system_config` - 系统配置
8. `system_links` - 系统链接
9. `user_holdings` - 用户持仓
10. `users` - 用户表（核心）
11. `vip_tiers` - VIP 等级配置
12. `withdrawals` - 提现记录（核心）

**建议**:
- 🔍 确认当前访问方式（service_role vs anon）
- 🔒 为每个表创建合适的 RLS 策略
- 🔒 根据业务需求设置 SELECT/INSERT/UPDATE/DELETE 策略

---

## 🟡 中风险问题（MEDIUM）

### 3. 函数搜索路径可变（4 个函数）

**问题描述**:
这些函数的 `search_path` 参数未设置，可能导致 SQL 注入风险。

**受影响的函数**:
1. `get_energy_config`
2. `update_energy_config`
3. `sum_fee_amount_wei`
4. `sum_rat_balance_wei`

**风险**:
- ⚠️ SQL 注入风险（如果攻击者可以控制 search_path）
- ⚠️ 函数可能访问意外的 schema

**建议**:
- 🔒 为所有函数设置 `search_path = ''` 或 `search_path = 'public'`
- 🔒 使用 `SECURITY DEFINER` 时特别小心

---

## 📊 详细分析

### 表分类和风险评估

#### 核心业务表（需要严格保护）

| 表名 | RLS 状态 | 策略数量 | 风险级别 | 建议 |
|------|---------|---------|---------|------|
| `users` | ✅ ENABLED | 0 | 🟠 HIGH | 创建用户级策略 |
| `withdrawals` | ✅ ENABLED | 0 | 🟠 HIGH | 创建用户级策略 |
| `claims` | ✅ ENABLED | 0 | 🟠 HIGH | 创建用户级策略 |
| `referral_rewards` | ✅ ENABLED | 0 | 🟠 HIGH | 创建用户级策略 |

#### 管理员表（需要最高级别保护）

| 表名 | RLS 状态 | 策略数量 | 风险级别 | 建议 |
|------|---------|---------|---------|------|
| `admin_operations` | ❌ DISABLED | 0 | 🔴 CRITICAL | **立即启用 RLS** |
| `auto_payout_config` | ❌ DISABLED | 0 | 🔴 CRITICAL | **立即启用 RLS** |
| `auto_payout_logs` | ❌ DISABLED | 0 | 🔴 CRITICAL | **立即启用 RLS** |
| `energy_config` | ❌ DISABLED | 0 | 🔴 CRITICAL | **立即启用 RLS** |
| `energy_config_history` | ❌ DISABLED | 0 | 🔴 CRITICAL | **立即启用 RLS** |

#### 统计/缓存表（需要保护）

| 表名 | RLS 状态 | 策略数量 | 风险级别 | 建议 |
|------|---------|---------|---------|------|
| `page_visits` | ❌ DISABLED | 0 | 🔴 CRITICAL | **立即启用 RLS** |
| `ip_geo_cache` | ❌ DISABLED | 0 | 🔴 CRITICAL | **立即启用 RLS** |

#### 系统配置表（需要保护）

| 表名 | RLS 状态 | 策略数量 | 风险级别 | 建议 |
|------|---------|---------|---------|------|
| `system_config` | ✅ ENABLED | 0 | 🟠 HIGH | 创建策略 |
| `system_announcement` | ✅ ENABLED | 0 | 🟠 HIGH | 创建策略 |
| `system_links` | ✅ ENABLED | 0 | 🟠 HIGH | 创建策略 |
| `vip_tiers` | ✅ ENABLED | 0 | 🟠 HIGH | 创建策略 |

---

## 🎯 修复优先级

### 优先级 P0（立即修复 - 24 小时内）

1. **`auto_payout_config`** - 包含加密私钥，极度危险
2. **`page_visits`** - 包含敏感列 `session_id`，且数据量大
3. **`admin_operations`** - 管理员操作记录泄露
4. **`auto_payout_logs`** - 财务信息泄露

### 优先级 P1（本周内修复）

5. **`energy_config`** - 业务逻辑泄露
6. **`energy_config_history`** - 审计信息泄露
7. **`ip_geo_cache`** - 用户位置信息泄露
8. **`users_backup_20260105`** - 用户数据备份泄露

### 优先级 P2（本月内修复）

9. **所有已启用 RLS 但无策略的表** - 创建合适的策略
10. **函数搜索路径问题** - 修复函数安全配置

---

## 🔧 修复方案

### 方案 A: 渐进式修复（推荐）⭐

**优势**:
- ✅ 不影响现有功能
- ✅ 可以逐步测试和验证
- ✅ 风险可控

**步骤**:
1. **阶段 1**: 修复 P0 优先级表（4 个）
2. **阶段 2**: 修复 P1 优先级表（4 个）
3. **阶段 3**: 为已启用 RLS 的表创建策略
4. **阶段 4**: 修复函数安全问题

---

### 方案 B: 一次性修复

**优势**:
- ✅ 快速完成所有修复

**劣势**:
- ❌ 可能影响现有功能
- ❌ 需要大量测试
- ❌ 风险较高

---

## 📝 修复实施计划

### 阶段 1: P0 优先级修复（立即执行）

#### 1.1 修复 `auto_payout_config`

**策略**:
- 仅后端服务可访问（使用 `service_role` key）
- 禁止通过 PostgREST API 直接访问

**SQL**:
```sql
-- 启用 RLS
ALTER TABLE auto_payout_config ENABLE ROW LEVEL SECURITY;

-- 创建策略：仅 service_role 可访问
-- 注意：service_role 默认绕过 RLS，但显式策略更安全
CREATE POLICY "service_role_only" ON auto_payout_config
  FOR ALL
  TO service_role
  USING (true)
  WITH CHECK (true);
```

---

#### 1.2 修复 `page_visits`

**策略**:
- 仅后端服务可插入和查询
- 禁止通过 PostgREST API 直接访问

**SQL**:
```sql
-- 启用 RLS
ALTER TABLE page_visits ENABLE ROW LEVEL SECURITY;

-- 创建策略：仅 service_role 可访问
CREATE POLICY "service_role_only" ON page_visits
  FOR ALL
  TO service_role
  USING (true)
  WITH CHECK (true);
```

---

#### 1.3 修复 `admin_operations`

**策略**:
- 仅管理员可查看（通过 JWT 验证）

**SQL**:
```sql
-- 启用 RLS
ALTER TABLE admin_operations ENABLE ROW LEVEL SECURITY;

-- 创建策略：仅 service_role 可访问
-- 注意：管理员操作应该通过后端 API，而不是直接访问数据库
CREATE POLICY "service_role_only" ON admin_operations
  FOR ALL
  TO service_role
  USING (true)
  WITH CHECK (true);
```

---

#### 1.4 修复 `auto_payout_logs`

**策略**:
- 仅管理员可查看

**SQL**:
```sql
-- 启用 RLS
ALTER TABLE auto_payout_logs ENABLE ROW LEVEL SECURITY;

-- 创建策略：仅 service_role 可访问
CREATE POLICY "service_role_only" ON auto_payout_logs
  FOR ALL
  TO service_role
  USING (true)
  WITH CHECK (true);
```

---

### 阶段 2: P1 优先级修复

#### 2.1 修复 `energy_config` 和 `energy_config_history`

**策略**:
- 仅后端服务可访问

**SQL**:
```sql
-- energy_config
ALTER TABLE energy_config ENABLE ROW LEVEL SECURITY;
CREATE POLICY "service_role_only" ON energy_config
  FOR ALL
  TO service_role
  USING (true)
  WITH CHECK (true);

-- energy_config_history
ALTER TABLE energy_config_history ENABLE ROW LEVEL SECURITY;
CREATE POLICY "service_role_only" ON energy_config_history
  FOR ALL
  TO service_role
  USING (true)
  WITH CHECK (true);
```

---

#### 2.2 修复 `ip_geo_cache`

**策略**:
- 仅后端服务可访问

**SQL**:
```sql
ALTER TABLE ip_geo_cache ENABLE ROW LEVEL SECURITY;
CREATE POLICY "service_role_only" ON ip_geo_cache
  FOR ALL
  TO service_role
  USING (true)
  WITH CHECK (true);
```

---

#### 2.3 处理 `users_backup_20260105`

**建议**:
- 如果不再需要，直接删除
- 如果需要保留，启用 RLS 并创建策略

**SQL**:
```sql
-- 选项 1: 删除备份表（推荐）
DROP TABLE IF EXISTS users_backup_20260105;

-- 选项 2: 启用 RLS 并创建策略（如果需要保留）
ALTER TABLE users_backup_20260105 ENABLE ROW LEVEL SECURITY;
CREATE POLICY "service_role_only" ON users_backup_20260105
  FOR ALL
  TO service_role
  USING (true)
  WITH CHECK (true);
```

---

### 阶段 3: 为已启用 RLS 的表创建策略

#### 3.1 用户相关表策略

**`users` 表**:
```sql
-- 后端服务可以访问所有数据（因为前端不直接访问，所以只需要 service_role 策略）
CREATE POLICY "service_role_all" ON users
  FOR ALL
  TO service_role
  USING (true)
  WITH CHECK (true);
```

**`withdrawals` 表**:
```sql
-- 后端服务可以访问所有数据
CREATE POLICY "service_role_all" ON withdrawals
  FOR ALL
  TO service_role
  USING (true)
  WITH CHECK (true);
```

**`claims` 表**:
```sql
-- 后端服务可以访问所有数据
CREATE POLICY "service_role_all" ON claims
  FOR ALL
  TO service_role
  USING (true)
  WITH CHECK (true);
```

**`referral_rewards` 表**:
```sql
-- 后端服务可以访问所有数据
CREATE POLICY "service_role_all" ON referral_rewards
  FOR ALL
  TO service_role
  USING (true)
  WITH CHECK (true);
```

**注意**: 由于前端不直接访问 Supabase，所有表只需要 `service_role` 策略即可。如果未来需要前端直接访问，再添加用户级策略。

---

#### 3.2 系统配置表策略

**`system_config` 表**:
```sql
-- 仅后端服务可访问
CREATE POLICY "service_role_only" ON system_config
  FOR ALL
  TO service_role
  USING (true)
  WITH CHECK (true);
```

**`system_announcement` 表**:
```sql
-- 仅后端服务可访问（公告通过后端 API 提供）
CREATE POLICY "service_role_only" ON system_announcement
  FOR ALL
  TO service_role
  USING (true)
  WITH CHECK (true);
```

**`vip_tiers` 表**:
```sql
-- 仅后端服务可访问（VIP 配置通过后端 API 提供）
CREATE POLICY "service_role_only" ON vip_tiers
  FOR ALL
  TO service_role
  USING (true)
  WITH CHECK (true);
```

**其他表**:
```sql
-- chain_sync_state
CREATE POLICY "service_role_only" ON chain_sync_state
  FOR ALL
  TO service_role
  USING (true)
  WITH CHECK (true);

-- cooldown_resets
CREATE POLICY "service_role_only" ON cooldown_resets
  FOR ALL
  TO service_role
  USING (true)
  WITH CHECK (true);

-- notifications
CREATE POLICY "service_role_only" ON notifications
  FOR ALL
  TO service_role
  USING (true)
  WITH CHECK (true);

-- system_links
CREATE POLICY "service_role_only" ON system_links
  FOR ALL
  TO service_role
  USING (true)
  WITH CHECK (true);

-- user_holdings
CREATE POLICY "service_role_only" ON user_holdings
  FOR ALL
  TO service_role
  USING (true)
  WITH CHECK (true);
```

---

### 阶段 4: 修复函数安全问题

**修复函数 `search_path`**:
```sql
-- get_energy_config
ALTER FUNCTION get_energy_config(text) 
  SET search_path = 'public';

-- update_energy_config
ALTER FUNCTION update_energy_config(text, numeric, text, text) 
  SET search_path = 'public';

-- sum_fee_amount_wei
ALTER FUNCTION sum_fee_amount_wei() 
  SET search_path = 'public';

-- sum_rat_balance_wei
ALTER FUNCTION sum_rat_balance_wei() 
  SET search_path = 'public';
```

---

## ⚠️ 注意事项

### 1. 不影响现有功能

**关键点**:
- ✅ **后端使用 `service_role` key**（已确认）
  - 文件: `src/infra/supabase.ts`
  - 代码: `createClient(config.supabaseUrl, config.supabaseServiceRoleKey)`
  - 影响: 后端服务**完全不受 RLS 限制**，可以正常访问所有表
- ✅ **前端不直接访问 Supabase**
  - 前端通过后端 API 访问数据
  - 不直接使用 Supabase client
- ⚠️ **PostgREST API 直接暴露风险**
  - Supabase 的 PostgREST API 默认对所有表开放
  - 如果 `anon` key 泄露，未启用 RLS 的表可以被直接访问
  - 即使前端不直接访问，API 端点仍然存在

**验证步骤**:
1. ✅ 后端使用 `service_role` key（已确认）
2. ✅ 前端不直接访问 Supabase（已确认）
3. ⚠️ 需要测试修复后的后端访问是否正常（应该正常，因为使用 service_role）

---

### 2. 测试计划

**修复前**:
- [ ] 记录当前所有表的访问方式
- [ ] 测试所有 API 端点
- [ ] 确认前端功能正常

**修复后**:
- [ ] 验证后端服务可以正常访问
- [ ] 验证前端功能正常
- [ ] 验证管理员功能正常
- [ ] 验证用户功能正常

---

### 3. 回滚计划

**如果修复导致问题**:
1. 立即禁用 RLS（临时）
2. 检查错误日志
3. 修复策略问题
4. 重新启用 RLS

**回滚 SQL**:
```sql
-- 临时禁用 RLS（紧急情况）
ALTER TABLE <table_name> DISABLE ROW LEVEL SECURITY;
```

---

## 📊 修复后预期效果

### 安全提升

- ✅ **数据保护**: 所有敏感数据受 RLS 保护
- ✅ **访问控制**: 明确的访问权限控制
- ✅ **合规性**: 符合数据保护法规要求

### 功能影响

- ✅ **后端服务**: 不受影响（使用 service_role）
- ✅ **前端功能**: 需要合适的策略支持
- ✅ **管理员功能**: 需要管理员认证策略

---

## ✅ 修复检查清单

### 准备阶段

- [ ] 备份数据库
- [ ] 记录当前访问方式
- [ ] 测试所有功能
- [ ] 准备回滚方案

### 修复阶段

- [ ] 修复 P0 优先级表（4 个）
- [ ] 修复 P1 优先级表（4 个）
- [ ] 为已启用 RLS 的表创建策略（12 个）
- [ ] 修复函数安全问题（4 个）

### 验证阶段

- [ ] 验证后端服务正常
- [ ] 验证前端功能正常
- [ ] 验证管理员功能正常
- [ ] 验证用户功能正常
- [ ] 运行安全扫描确认修复

---

## 📝 总结

### 当前状态

- 🔴 **8 个表未启用 RLS**（CRITICAL）
- 🟠 **12 个表已启用 RLS 但无策略**（HIGH）
- 🟡 **4 个函数安全配置问题**（MEDIUM）

### 修复建议

1. **立即修复** P0 优先级表（4 个）
2. **本周内修复** P1 优先级表（4 个）
3. **本月内修复** 所有 RLS 策略和函数安全问题

### 风险评估

- **修复风险**: 🟡 中等（需要充分测试）
- **不修复风险**: 🔴 极高（数据完全暴露）

---

---

## 🔍 重要说明

### 关于 RLS 的实际影响

**好消息**:
- ✅ 后端使用 `service_role` key，**完全不受 RLS 限制**
- ✅ 前端不直接访问 Supabase，通过后端 API
- ✅ 修复 RLS 后，**后端功能不会受影响**

**为什么仍然需要修复**:
- 🔴 **PostgREST API 直接暴露风险**: Supabase 的 PostgREST API 默认对所有表开放
- 🔴 **anon key 泄露风险**: 如果 `anon` key 泄露，未启用 RLS 的表可以被直接访问
- 🔴 **深度防御**: 即使后端安全，数据库层面也应该有保护

**修复策略**:
- 所有表启用 RLS
- 创建 `service_role` 策略（允许后端访问）
- 不创建 `anon` 策略（禁止直接 API 访问）

---

## 📊 修复影响评估

### 对后端的影响

- ✅ **无影响**: 后端使用 `service_role` key，可以绕过 RLS
- ✅ **无需修改代码**: 后端代码不需要任何修改
- ✅ **功能正常**: 所有后端功能继续正常工作

### 对前端的影响

- ✅ **无影响**: 前端不直接访问 Supabase
- ✅ **无需修改代码**: 前端代码不需要任何修改
- ✅ **功能正常**: 所有前端功能继续正常工作

### 对 PostgREST API 的影响

- ✅ **增强安全**: 未启用 RLS 的表将无法通过 PostgREST API 直接访问
- ✅ **防止泄露**: 即使 `anon` key 泄露，也无法访问敏感数据
- ✅ **深度防御**: 多一层安全保护

---

## 🎯 推荐修复方案

### 方案 A: 渐进式修复（推荐）⭐

**优势**:
- ✅ 不影响现有功能（后端使用 service_role）
- ✅ 可以逐步测试和验证
- ✅ 风险可控

**步骤**:
1. **阶段 1（今天）**: 修复 P0 优先级表（4 个）
   - `auto_payout_config`
   - `page_visits`
   - `admin_operations`
   - `auto_payout_logs`

2. **阶段 2（本周）**: 修复 P1 优先级表（4 个）
   - `energy_config`
   - `energy_config_history`
   - `ip_geo_cache`
   - `users_backup_20260105`（或删除）

3. **阶段 3（本月）**: 为已启用 RLS 的表创建策略（12 个）

4. **阶段 4（本月）**: 修复函数安全问题（4 个）

---

### 方案 B: 一次性修复

**优势**:
- ✅ 快速完成所有修复

**劣势**:
- ❌ 需要大量测试
- ❌ 风险较高（虽然理论上无影响）

---

## 📝 快速修复脚本

### 一键修复所有表（推荐使用）

**文件**: `db/fix_rls_security.sql`

```sql
-- ============================================
-- 数据库安全修复脚本
-- 日期: 2026-01-09
-- 说明: 为所有表启用 RLS 并创建 service_role 策略
-- 影响: 不影响后端功能（后端使用 service_role key）
-- ============================================

-- ============================================
-- P0 优先级表（立即修复）
-- ============================================

-- auto_payout_config（最危险：包含加密私钥）
ALTER TABLE auto_payout_config ENABLE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS "service_role_only" ON auto_payout_config;
CREATE POLICY "service_role_only" ON auto_payout_config
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- page_visits（包含敏感列 session_id）
ALTER TABLE page_visits ENABLE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS "service_role_only" ON page_visits;
CREATE POLICY "service_role_only" ON page_visits
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- admin_operations（管理员操作记录）
ALTER TABLE admin_operations ENABLE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS "service_role_only" ON admin_operations;
CREATE POLICY "service_role_only" ON admin_operations
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- auto_payout_logs（自动放款日志）
ALTER TABLE auto_payout_logs ENABLE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS "service_role_only" ON auto_payout_logs;
CREATE POLICY "service_role_only" ON auto_payout_logs
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- ============================================
-- P1 优先级表（本周修复）
-- ============================================

-- energy_config
ALTER TABLE energy_config ENABLE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS "service_role_only" ON energy_config;
CREATE POLICY "service_role_only" ON energy_config
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- energy_config_history
ALTER TABLE energy_config_history ENABLE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS "service_role_only" ON energy_config_history;
CREATE POLICY "service_role_only" ON energy_config_history
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- ip_geo_cache
ALTER TABLE ip_geo_cache ENABLE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS "service_role_only" ON ip_geo_cache;
CREATE POLICY "service_role_only" ON ip_geo_cache
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- users_backup_20260105（建议删除，如果需要保留则启用 RLS）
-- 选项 1: 删除备份表（推荐）
-- DROP TABLE IF EXISTS users_backup_20260105;

-- 选项 2: 启用 RLS（如果需要保留）
ALTER TABLE users_backup_20260105 ENABLE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS "service_role_only" ON users_backup_20260105;
CREATE POLICY "service_role_only" ON users_backup_20260105
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- ============================================
-- 为已启用 RLS 但无策略的表创建策略
-- ============================================

-- chain_sync_state
DROP POLICY IF EXISTS "service_role_only" ON chain_sync_state;
CREATE POLICY "service_role_only" ON chain_sync_state
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- claims
DROP POLICY IF EXISTS "service_role_only" ON claims;
CREATE POLICY "service_role_only" ON claims
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- cooldown_resets
DROP POLICY IF EXISTS "service_role_only" ON cooldown_resets;
CREATE POLICY "service_role_only" ON cooldown_resets
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- notifications
DROP POLICY IF EXISTS "service_role_only" ON notifications;
CREATE POLICY "service_role_only" ON notifications
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- referral_rewards
DROP POLICY IF EXISTS "service_role_only" ON referral_rewards;
CREATE POLICY "service_role_only" ON referral_rewards
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- system_announcement
DROP POLICY IF EXISTS "service_role_only" ON system_announcement;
CREATE POLICY "service_role_only" ON system_announcement
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- system_config
DROP POLICY IF EXISTS "service_role_only" ON system_config;
CREATE POLICY "service_role_only" ON system_config
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- system_links
DROP POLICY IF EXISTS "service_role_only" ON system_links;
CREATE POLICY "service_role_only" ON system_links
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- user_holdings
DROP POLICY IF EXISTS "service_role_only" ON user_holdings;
CREATE POLICY "service_role_only" ON user_holdings
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- users
DROP POLICY IF EXISTS "service_role_only" ON users;
CREATE POLICY "service_role_only" ON users
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- vip_tiers
DROP POLICY IF EXISTS "service_role_only" ON vip_tiers;
CREATE POLICY "service_role_only" ON vip_tiers
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- withdrawals
DROP POLICY IF EXISTS "service_role_only" ON withdrawals;
CREATE POLICY "service_role_only" ON withdrawals
  FOR ALL TO service_role USING (true) WITH CHECK (true);

-- ============================================
-- 修复函数安全问题
-- ============================================

ALTER FUNCTION get_energy_config(text) SET search_path = 'public';
ALTER FUNCTION update_energy_config(text, numeric, text, text) SET search_path = 'public';
ALTER FUNCTION sum_fee_amount_wei() SET search_path = 'public';
ALTER FUNCTION sum_rat_balance_wei() SET search_path = 'public';

-- ============================================
-- 验证修复
-- ============================================

-- 检查所有表的 RLS 状态
SELECT 
  tablename,
  CASE WHEN rowsecurity THEN 'ENABLED' ELSE 'DISABLED' END as rls_status
FROM pg_tables 
WHERE schemaname = 'public'
ORDER BY tablename;

-- 检查所有策略
SELECT 
  tablename,
  policyname,
  cmd,
  roles
FROM pg_policies
WHERE schemaname = 'public'
ORDER BY tablename, policyname;
```

---

**报告生成时间**: 2026-01-09  
**审查人**: AI Assistant  
**状态**: 📋 待选择修复方案并开始实施

