# 系统错误通知功能审查报告

**日期**: 2026-01-09  
**功能**: 后台系统错误通知（Telegram Bot）  
**状态**: 📋 待实施

---

## 📋 当前系统错误处理审查

### 1. RPC 错误处理机制 ✅

**文件**: `src/infra/rpcPool.ts`

**现有功能**:
- ✅ 健康检查：每 60 秒检查一次所有 RPC 节点
- ✅ 智能轮换：优先使用健康的 RPC
- ✅ 失败统计：记录每个 RPC 的失败次数
- ✅ 超时配置：30 秒超时（比默认 120 秒快）
- ✅ 自动恢复：不健康的节点在冷却期后自动重试

**错误检测**:
- `getBlockNumber()` 调用失败 → 标记为不健康
- 连续失败 3 次 → 标记为不健康
- 5 分钟后重试不健康的节点

**当前问题**:
- ❌ **没有通知机制**：RPC 节点失败时只记录日志，不发送告警
- ❌ **没有汇总通知**：所有 RPC 节点都不可用时，没有紧急告警

---

### 2. Indexer 错误处理 ✅

**文件**: `src/indexer/indexer.ts`

**现有功能**:
- ✅ 错误捕获和日志记录
- ✅ RPC 轮换机制（调用 `rpcPool.rotate()`）
- ✅ 指数退避和重试机制

**错误类型**:
- RPC 超时错误
- RPC 限流错误（-32005）
- 网络连接错误

**当前问题**:
- ❌ **没有通知机制**：Indexer 错误只记录日志，不发送告警
- ❌ **没有严重性分级**：所有错误都同等处理

---

### 3. 数据库错误处理 ✅

**文件**: `src/infra/supabase.js`, `src/services/*.ts`

**现有功能**:
- ✅ 错误捕获和日志记录
- ✅ 启动时检查数据库函数是否存在

**错误类型**:
- 数据库连接错误
- 函数不存在错误
- 查询超时错误

**当前问题**:
- ❌ **没有通知机制**：数据库错误只记录日志，不发送告警
- ❌ **启动时致命错误**：数据库函数不存在时只记录日志，不发送告警

---

### 4. 服务启动错误处理 ✅

**文件**: `src/index.ts`

**现有功能**:
- ✅ 启动时检查数据库函数
- ✅ 启动时初始化 Telegram Bot
- ✅ 启动时初始化自动放款服务

**错误类型**:
- 数据库函数不存在（致命错误）
- Telegram Bot 初始化失败（非致命）
- 自动放款服务初始化失败（非致命）

**当前问题**:
- ❌ **致命错误没有通知**：数据库函数不存在时只记录日志，不发送告警
- ❌ **服务启动失败没有通知**：关键服务启动失败时没有告警

---

## 💡 建议监控的错误类型

### 优先级 P0（高优先级 - 立即实施）

#### 1. RPC 节点全部不可用告警 🚨

**触发条件**:
- 所有 RPC 节点都被标记为不健康
- 健康检查连续失败（所有节点）

**推送内容**:
- 🚨 **系统告警：所有 RPC 节点不可用**
- 📊 节点状态汇总
- ⚠️ 影响范围（Indexer、收益计算、提现等）
- 🕒 告警时间
- 💡 建议操作（检查 RPC 配置、联系服务商等）

**业务价值**:
- ✅ 及时发现 RPC 服务中断
- ✅ 快速响应，减少服务中断时间
- ✅ 防止用户操作失败

**代码位置**: `src/infra/rpcPool.ts` - `checkAllNodesHealth()` 方法

---

#### 2. RPC 节点连续失败告警 ⚠️

**触发条件**:
- 单个 RPC 节点连续失败超过阈值（如 5 次）
- 节点被标记为不健康

**推送内容**:
- ⚠️ **RPC 节点告警**
- 🔗 节点 URL（脱敏处理）
- 📊 失败次数
- 🕒 最后成功时间
- 📌 状态：不健康

**业务价值**:
- ✅ 监控单个节点健康状态
- ✅ 及时发现节点问题
- ✅ 便于更换或修复节点

**代码位置**: `src/infra/rpcPool.ts` - `markFailure()` 方法

---

#### 3. Indexer 严重错误告警 🚨

**触发条件**:
- Indexer 连续失败超过阈值（如 10 次）
- Indexer 完全停止工作

**推送内容**:
- 🚨 **Indexer 严重错误告警**
- ⚠️ 错误类型（超时/限流/网络错误）
- 📊 失败次数
- 🕒 最后成功时间
- ⚠️ 影响：区块链数据同步中断

**业务价值**:
- ✅ 及时发现区块链数据同步问题
- ✅ 防止数据不一致
- ✅ 快速恢复服务

**代码位置**: `src/indexer/indexer.ts` - 错误处理逻辑

---

### 优先级 P1（中优先级 - 建议实施）

#### 4. 数据库连接错误告警 💾

**触发条件**:
- 数据库连接失败
- 数据库查询超时
- 数据库函数不存在（启动时）

**推送内容**:
- 💾 **数据库错误告警**
- ⚠️ 错误类型（连接失败/查询超时/函数不存在）
- 📝 错误详情
- 🕒 错误时间
- ⚠️ 影响范围

**业务价值**:
- ✅ 及时发现数据库问题
- ✅ 快速响应数据库故障
- ✅ 防止数据丢失

**代码位置**: 
- `src/infra/supabase.js` - 连接错误处理
- `src/index.ts` - 启动时检查

---

#### 5. 自动放款服务错误告警 🤖

**触发条件**:
- 自动放款服务初始化失败
- 自动放款服务连续失败（如 5 次）

**推送内容**:
- 🤖 **自动放款服务错误告警**
- ⚠️ 错误类型（初始化失败/处理失败）
- 📝 错误详情
- 🕒 错误时间
- ⚠️ 影响：小额提现无法自动处理

**业务价值**:
- ✅ 及时发现自动放款问题
- ✅ 快速恢复自动放款功能
- ✅ 防止用户提现延迟

**代码位置**: `src/services/autoPayout.ts` - 错误处理逻辑

---

#### 6. 服务启动失败告警 🚀

**触发条件**:
- 关键服务启动失败（数据库函数不存在等）
- 服务启动超时

**推送内容**:
- 🚀 **服务启动失败告警**
- ⚠️ 失败的服务名称
- 📝 错误详情
- 🕒 启动时间
- ⚠️ 影响：服务无法正常运行

**业务价值**:
- ✅ 及时发现服务启动问题
- ✅ 快速修复启动错误
- ✅ 防止服务无法启动

**代码位置**: `src/index.ts` - 启动逻辑

---

### 优先级 P2（低优先级 - 可选实施）

#### 7. 系统资源告警 📊

**触发条件**:
- 内存使用率超过阈值（如 90%）
- CPU 使用率超过阈值（如 90%）
- 磁盘空间不足

**推送内容**:
- 📊 **系统资源告警**
- 💾 内存使用率
- ⚡ CPU 使用率
- 💿 磁盘使用率
- 🕒 告警时间

**业务价值**:
- ✅ 监控系统资源使用情况
- ✅ 防止系统崩溃
- ✅ 及时扩容或优化

**代码位置**: 需要新增系统监控模块

---

#### 8. API 错误率告警 📈

**触发条件**:
- API 错误率超过阈值（如 10%）
- 连续错误请求超过阈值

**推送内容**:
- 📈 **API 错误率告警**
- 📊 错误率统计
- ⚠️ 主要错误类型
- 🕒 统计时间段

**业务价值**:
- ✅ 监控 API 健康状态
- ✅ 及时发现 API 问题
- ✅ 快速修复 API 错误

**代码位置**: 需要新增 API 监控中间件

---

## 🎯 实施方案

### 方案 A: 集中式错误监控服务（推荐）⭐

**架构设计**:
```
错误发生 → 错误监控服务 → 判断严重性 → 发送通知
```

**优势**:
- ✅ 统一错误处理逻辑
- ✅ 易于维护和扩展
- ✅ 支持错误去重和限流
- ✅ 支持错误分级

**实施步骤**:
1. 创建 `src/services/errorMonitor.ts` - 错误监控服务
2. 在各个模块中集成错误监控
3. 添加 Telegram 通知函数
4. 实现去重和限流机制

---

### 方案 B: 分散式错误通知（简单）

**架构设计**:
```
错误发生 → 直接调用 Telegram 通知函数
```

**优势**:
- ✅ 实施简单
- ✅ 不需要额外服务

**劣势**:
- ❌ 错误处理逻辑分散
- ❌ 难以统一管理
- ❌ 容易重复通知

---

## 🔧 技术实现建议

### 1. 错误监控服务设计

**文件**: `src/services/errorMonitor.ts`

**核心功能**:
```typescript
class ErrorMonitor {
  // 记录错误
  recordError(type: ErrorType, error: Error, context?: any): void;
  
  // 检查是否需要发送告警
  shouldAlert(type: ErrorType): boolean;
  
  // 发送告警
  sendAlert(type: ErrorType, error: Error, context?: any): Promise<void>;
  
  // 错误去重
  isDuplicate(type: ErrorType, error: Error): boolean;
}
```

**错误类型枚举**:
```typescript
enum ErrorType {
  RPC_ALL_NODES_DOWN = 'rpc_all_nodes_down',
  RPC_NODE_FAILED = 'rpc_node_failed',
  INDEXER_SEVERE_ERROR = 'indexer_severe_error',
  DATABASE_CONNECTION_ERROR = 'database_connection_error',
  DATABASE_FUNCTION_MISSING = 'database_function_missing',
  AUTO_PAYOUT_SERVICE_ERROR = 'auto_payout_service_error',
  SERVICE_STARTUP_FAILED = 'service_startup_failed',
}
```

---

### 2. 错误严重性分级

**严重性级别**:
- 🔴 **严重（Critical）**: 服务完全不可用，需要立即处理
  - 所有 RPC 节点不可用
  - 数据库连接失败
  - 服务启动失败
  
- 🟠 **高（High）**: 服务部分不可用，需要尽快处理
  - Indexer 严重错误
  - 自动放款服务错误
  - 单个 RPC 节点连续失败
  
- 🟡 **中（Medium）**: 服务降级，需要关注
  - 单个 RPC 节点失败
  - API 错误率上升
  
- 🟢 **低（Low）**: 轻微问题，可以延迟处理
  - 系统资源使用率高
  - 非关键服务错误

---

### 3. 错误去重和限流机制

**去重策略**:
- 相同类型的错误在短时间内（如 5 分钟）只发送一次
- 使用 `system_config` 表存储上次告警时间

**限流策略**:
- 严重错误：立即发送，无限制
- 高优先级错误：每小时最多 3 次
- 中优先级错误：每小时最多 1 次
- 低优先级错误：每天最多 1 次

**存储结构**:
```typescript
// system_config 表
{
  key: 'error_alert_last_sent_rpc_all_nodes_down',
  value: timestamp,
  updated_at: timestamp
}
```

---

### 4. Telegram 通知函数设计

**文件**: `src/services/telegram.ts`

**新增函数**:
```typescript
// 发送系统错误告警
export async function sendSystemErrorAlert(data: {
  severity: 'critical' | 'high' | 'medium' | 'low';
  errorType: string;
  errorMessage: string;
  context?: any;
  timestamp: string;
}): Promise<void>
```

**消息格式**:
```
🚨 <b>系统错误告警</b>

⚠️ 严重性: <b>严重</b>
📋 错误类型: RPC 节点全部不可用
📝 错误详情: 所有 RPC 节点健康检查失败
🕒 告警时间: 2026-01-09 15:30:25

📊 <b>影响范围</b>
• Indexer 数据同步中断
• 收益计算功能不可用
• 提现功能可能受影响

💡 <b>建议操作</b>
1. 检查 RPC 服务商状态
2. 验证 RPC URL 配置
3. 联系 RPC 服务商支持

请立即处理！
```

---

## 📊 集成点分析

### 1. RPC Pool 集成

**文件**: `src/infra/rpcPool.ts`

**集成点**:
- `checkAllNodesHealth()`: 检测到所有节点不可用时发送告警
- `markFailure()`: 节点连续失败时发送告警

**实现示例**:
```typescript
// 在 checkAllNodesHealth() 中
const healthyNodes = this.nodes.filter(n => n.isHealthy);
if (healthyNodes.length === 0) {
  // 所有节点不可用，发送严重告警
  setImmediate(async () => {
    const { sendSystemErrorAlert } = await import('../services/telegram.js');
    await sendSystemErrorAlert({
      severity: 'critical',
      errorType: 'RPC_ALL_NODES_DOWN',
      errorMessage: '所有 RPC 节点健康检查失败',
      context: { nodeCount: this.nodes.length },
      timestamp: new Date().toISOString(),
    });
  });
}
```

---

### 2. Indexer 集成

**文件**: `src/indexer/indexer.ts`

**集成点**:
- 错误处理逻辑：检测到严重错误时发送告警

**实现示例**:
```typescript
// 在错误处理中
let consecutiveFailures = 0;
const MAX_CONSECUTIVE_FAILURES = 10;

try {
  // ... indexer 逻辑
  consecutiveFailures = 0; // 重置失败计数
} catch (error) {
  consecutiveFailures++;
  
  if (consecutiveFailures >= MAX_CONSECUTIVE_FAILURES) {
    // 发送严重告警
    setImmediate(async () => {
      const { sendSystemErrorAlert } = await import('../services/telegram.js');
      await sendSystemErrorAlert({
        severity: 'critical',
        errorType: 'INDEXER_SEVERE_ERROR',
        errorMessage: `Indexer 连续失败 ${consecutiveFailures} 次`,
        context: { error: error.message },
        timestamp: new Date().toISOString(),
      });
    });
  }
}
```

---

### 3. 数据库错误集成

**文件**: `src/index.ts`, `src/infra/supabase.js`

**集成点**:
- 启动时检查数据库函数
- 数据库连接错误处理

**实现示例**:
```typescript
// 在启动时检查数据库函数
try {
  const { data, error } = await supabase.rpc('process_claim_energy', {...});
  if (error) {
    const errorMsg = String(error.message || '').toLowerCase();
    if (errorMsg.includes('function') && errorMsg.includes('does not exist')) {
      // 发送严重告警
      setImmediate(async () => {
        const { sendSystemErrorAlert } = await import('./services/telegram.js');
        await sendSystemErrorAlert({
          severity: 'critical',
          errorType: 'DATABASE_FUNCTION_MISSING',
          errorMessage: '数据库函数 process_claim_energy 不存在',
          context: { functionName: 'process_claim_energy' },
          timestamp: new Date().toISOString(),
        });
      });
    }
  }
} catch (e) {
  // 处理其他错误
}
```

---

### 4. 自动放款服务集成

**文件**: `src/services/autoPayout.ts`

**集成点**:
- 服务初始化失败
- 连续处理失败

**实现示例**:
```typescript
// 在 processPendingWithdrawals() 中
let consecutiveFailures = 0;
const MAX_CONSECUTIVE_FAILURES = 5;

try {
  // ... 处理逻辑
  consecutiveFailures = 0; // 重置失败计数
} catch (error) {
  consecutiveFailures++;
  
  if (consecutiveFailures >= MAX_CONSECUTIVE_FAILURES) {
    // 发送高优先级告警
    setImmediate(async () => {
      const { sendSystemErrorAlert } = await import('./telegram.js');
      await sendSystemErrorAlert({
        severity: 'high',
        errorType: 'AUTO_PAYOUT_SERVICE_ERROR',
        errorMessage: `自动放款服务连续失败 ${consecutiveFailures} 次`,
        context: { error: error.message },
        timestamp: new Date().toISOString(),
      });
    });
  }
}
```

---

## 📝 实施计划

### 阶段 1: 核心功能（P0）

1. **创建错误监控服务**
   - 创建 `src/services/errorMonitor.ts`
   - 实现错误记录和去重逻辑
   - 实现错误分级和限流

2. **添加 Telegram 通知函数**
   - 在 `src/services/telegram.ts` 中添加 `sendSystemErrorAlert`
   - 实现不同严重性的消息格式

3. **集成 RPC Pool 告警**
   - 在 `rpcPool.ts` 中集成所有节点不可用告警
   - 在 `rpcPool.ts` 中集成单个节点失败告警

4. **集成 Indexer 告警**
   - 在 `indexer.ts` 中集成严重错误告警

### 阶段 2: 扩展功能（P1）

5. **集成数据库错误告警**
   - 在启动时检查数据库函数
   - 在数据库连接错误时发送告警

6. **集成自动放款服务告警**
   - 在自动放款服务中集成错误告警

7. **集成服务启动失败告警**
   - 在服务启动失败时发送告警

### 阶段 3: 高级功能（P2）

8. **系统资源监控**
   - 添加系统资源监控模块
   - 集成资源告警

9. **API 错误率监控**
   - 添加 API 监控中间件
   - 集成错误率告警

---

## ✅ 实施检查清单

### 代码修改

- [ ] 创建 `src/services/errorMonitor.ts`
- [ ] 在 `src/services/telegram.ts` 中添加 `sendSystemErrorAlert`
- [ ] 在 `src/infra/rpcPool.ts` 中集成告警
- [ ] 在 `src/indexer/indexer.ts` 中集成告警
- [ ] 在 `src/index.ts` 中集成启动错误告警
- [ ] 在 `src/services/autoPayout.ts` 中集成告警
- [ ] 添加错误去重和限流机制

### 测试验证

- [ ] 测试 RPC 节点全部不可用告警
- [ ] 测试单个 RPC 节点失败告警
- [ ] 测试 Indexer 严重错误告警
- [ ] 测试数据库错误告警
- [ ] 测试自动放款服务错误告警
- [ ] 测试错误去重机制
- [ ] 测试错误限流机制
- [ ] 验证消息格式正确

---

## 📊 预期效果

### 运营效率提升

- ✅ **实时监控**: 所有系统错误都有实时告警
- ✅ **快速响应**: 及时发现和处理系统问题
- ✅ **减少损失**: 快速恢复服务，减少用户影响

### 风险控制

- ✅ **服务可用性**: 及时发现服务中断
- ✅ **数据一致性**: 及时发现数据同步问题
- ✅ **系统稳定性**: 监控系统资源使用情况

---

## 🎨 消息格式示例

### RPC 节点全部不可用告警

```
🚨 <b>系统错误告警</b>

⚠️ 严重性: <b>严重</b>
📋 错误类型: RPC 节点全部不可用
📝 错误详情: 所有 RPC 节点健康检查失败
🕒 告警时间: 2026-01-09 15:30:25

📊 <b>节点状态</b>
• 节点总数: 3
• 健康节点: 0
• 不健康节点: 3

📊 <b>影响范围</b>
• Indexer 数据同步中断
• 收益计算功能不可用
• 提现功能可能受影响

💡 <b>建议操作</b>
1. 检查 RPC 服务商状态
2. 验证 RPC URL 配置
3. 联系 RPC 服务商支持

请立即处理！
```

### Indexer 严重错误告警

```
🚨 <b>系统错误告警</b>

⚠️ 严重性: <b>严重</b>
📋 错误类型: Indexer 严重错误
📝 错误详情: Indexer 连续失败 10 次
🕒 告警时间: 2026-01-09 15:30:25

📊 <b>错误统计</b>
• 连续失败次数: 10
• 最后成功时间: 2026-01-09 14:30:25
• 错误类型: RPC 超时

📊 <b>影响范围</b>
• 区块链数据同步中断
• 用户操作可能无法及时同步
• 数据可能不一致

💡 <b>建议操作</b>
1. 检查 RPC 节点状态
2. 检查 Indexer 日志
3. 重启 Indexer 服务

请立即处理！
```

---

## 📝 注意事项

### 1. 错误去重

- 相同类型的错误在短时间内只发送一次
- 使用 `system_config` 表存储上次告警时间
- 根据错误严重性设置不同的去重时间窗口

### 2. 错误限流

- 严重错误：立即发送，无限制
- 高优先级错误：每小时最多 3 次
- 中优先级错误：每小时最多 1 次
- 低优先级错误：每天最多 1 次

### 3. 异步发送

- 所有告警都使用 `setImmediate` 异步发送
- 告警失败不影响主业务流程
- 告警错误会记录日志但不抛出异常

### 4. 消息格式

- 使用 HTML 格式（`parse_mode: 'HTML'`）
- 支持 `<b>`、`<code>` 等标签
- 禁用网页预览（`disable_web_page_preview: true`）
- 根据严重性使用不同的 emoji 和颜色

---

**报告生成时间**: 2026-01-09  
**状态**: 📋 待选择实施方案并开始实施

