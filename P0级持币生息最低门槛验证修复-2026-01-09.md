# P0级持币生息最低门槛验证修复

**日期**: 2026-01-09  
**问题级别**: P0（致命逻辑错误）  
**状态**: ✅ 已修复

---

## 📋 问题描述

### 致命错误

**业务规则**：
- 持币生息的最低门槛是 **10,000 RAT**
- 只有余额 >= 10,000 RAT 才能产生收益
- 余额 < 10,000 RAT 的用户不应该产生任何收益

**问题**：
- 代码中**没有验证余额是否 >= 10,000 RAT** 就开始计算收益
- 导致余额 < 10,000 RAT 的用户仍然可以显示和提现"非法收益"（脏数据）
- 这是历史漏洞导致的脏数据，应该被拒绝

### 实际案例

**用户 `0x4d570cab728574e161509de1f5dab53af78ad5c1`**：
- 系统记录余额：2,575.30 RAT（**低于 10,000 RAT**）
- 已固化收益：50 USDT（**这是非法收益/脏数据**）
- 当前链上余额：10,416.60 RAT（通过外部转账达到）

**问题分析**：
1. 系统记录余额 2,575.30 RAT < 10,000 RAT，不应该产生任何收益
2. 但数据库中存在 50 USDT 的"已固化收益"
3. 这是历史漏洞导致的脏数据
4. 系统应该拒绝显示和提现这些非法收益

---

## 🔍 根本原因

### 代码缺陷

**`earnings.ts`**：
- ❌ 没有检查 `balance >= 10000` 就开始计算增量收益
- ❌ 没有验证 `baseEarnings` 的合法性（余额 < 10000 时，baseEarnings 应该是 0）

**`withdraw.ts`**：
- ❌ 没有检查 `balance >= 10000` 就开始计算增量收益
- ❌ 没有验证 `baseEarnings` 的合法性
- ❌ 允许提现非法收益（脏数据）

### 业务逻辑错误

**正确的逻辑应该是**：
```
IF balance < 10,000 RAT:
  incrementalEarnings = 0
  validBaseEarnings = 0  // 即使数据库中有值，也视为 0（脏数据）
  realTimeEarnings = 0
ELSE:
  incrementalEarnings = balance × TOKEN_PRICE × (dailyRate / 100) × daysElapsed
  validBaseEarnings = baseEarnings  // 如果余额 >= 10,000，才认可历史收益
  realTimeEarnings = validBaseEarnings + incrementalEarnings
```

---

## 🔧 修复方案

### 1. 修复 `earnings.ts`

**添加最低门槛验证**：
```typescript
// 🔒 P0级修复：持币生息最低门槛验证
const MIN_BALANCE_FOR_EARNINGS = 10000;

// 只有余额 >= 10,000 RAT 才能计算增量收益
if (balance >= MIN_BALANCE_FOR_EARNINGS) {
  incrementalEarnings = balance * TOKEN_PRICE * (dailyRate / 100) * daysElapsed;
} else {
  incrementalEarnings = 0;
  console.warn(`[Earnings] ⚠️ 用户余额 ${balance.toFixed(2)} RAT < ${MIN_BALANCE_FOR_EARNINGS} RAT，不计算增量收益`);
}

// 🔒 P0级修复：验证 baseEarnings 的合法性
let validBaseEarnings = baseEarnings;
if (balance < MIN_BALANCE_FOR_EARNINGS && baseEarnings > 0) {
  console.warn(`[Earnings] ⚠️ 检测到非法收益（脏数据）: 用户 ${addr}`);
  console.warn(`   当前余额: ${balance.toFixed(2)} RAT < ${MIN_BALANCE_FOR_EARNINGS} RAT`);
  console.warn(`   历史收益（脏数据）: ${baseEarnings.toFixed(6)} USDT`);
  console.warn(`   🔒 安全措施: 将 baseEarnings 视为 0，不显示非法收益`);
  validBaseEarnings = 0;
}

const grossEarnings = validBaseEarnings + incrementalEarnings;
```

### 2. 修复 `withdraw.ts`

**添加最低门槛验证**：
```typescript
// 🔒 P0级修复：持币生息最低门槛验证
const MIN_BALANCE_FOR_EARNINGS = 10000;

// 只有余额 >= 10,000 RAT 才能计算增量收益
if (balance >= MIN_BALANCE_FOR_EARNINGS) {
  incrementalEarnings = balance * TOKEN_PRICE * (dailyRate / 100) * daysElapsed;
} else {
  incrementalEarnings = 0;
  console.warn(`[Withdraw] ⚠️ 用户余额 ${balance.toFixed(2)} RAT < ${MIN_BALANCE_FOR_EARNINGS} RAT，不计算增量收益`);
}

// 🔒 P0级修复：验证 baseEarnings 的合法性
let validBaseEarnings = baseEarnings;
if (balance < MIN_BALANCE_FOR_EARNINGS && baseEarnings > 0) {
  console.error(`[Withdraw] 🚨 检测到非法收益（脏数据）: 用户 ${addr}`);
  console.error(`   当前余额: ${balance.toFixed(2)} RAT < ${MIN_BALANCE_FOR_EARNINGS} RAT`);
  console.error(`   历史收益（脏数据）: ${baseEarnings.toFixed(6)} USDT`);
  console.error(`   🔒 安全措施: 将 baseEarnings 视为 0，不允许提现非法收益`);
  validBaseEarnings = 0;
}

const realTimeEarnings = validBaseEarnings + incrementalEarnings;
```

### 3. 增强错误信息

**在提现金额不足时，提供更详细的说明**：
```typescript
if (availableUsdt < amount) {
  let errorMsg = `USDT not enough (available ${availableUsdt.toFixed(6)}, need ${amount})`;
  
  if (balance < MIN_BALANCE_FOR_EARNINGS) {
    errorMsg += `. Note: Your balance (${balance.toFixed(2)} RAT) is below the minimum requirement (${MIN_BALANCE_FOR_EARNINGS} RAT) for earning rewards. You need at least ${MIN_BALANCE_FOR_EARNINGS} RAT to generate earnings.`;
  } else if (hasExternalTransfer) {
    errorMsg += `. Note: External transfer tokens do not generate historical earnings. You can only withdraw your existing earnings (${validBaseEarnings.toFixed(6)} USDT).`;
  }
  
  throw new ApiError('USDT_NOT_ENOUGH', errorMsg, 400);
}
```

---

## ✅ 修复效果

### 修复前

- ❌ 余额 < 10,000 RAT 的用户仍然可以显示和提现"非法收益"
- ❌ 没有验证收益的合法性
- ❌ 允许提现脏数据

### 修复后

- ✅ 余额 < 10,000 RAT 的用户无法产生收益
- ✅ 验证收益的合法性（拒绝脏数据）
- ✅ 不允许提现非法收益
- ✅ 明确的错误信息，告知用户需要达到最低门槛

---

## 📊 修复后的逻辑

### 场景 1：余额 < 10,000 RAT（无外部转账）

**用户情况**：
- 当前余额：2,575.30 RAT
- 历史收益（脏数据）：50 USDT

**修复后的处理**：
```
incrementalEarnings = 0  // 余额不足，不计算增量收益
validBaseEarnings = 0   // 余额不足，历史收益视为脏数据，拒绝
realTimeEarnings = 0     // 总收益为 0
可提现金额 = 0 USDT
```

**错误信息**：
```
USDT not enough (available 0.000000, need X). 
Note: Your balance (2,575.30 RAT) is below the minimum requirement (10,000 RAT) for earning rewards. 
You need at least 10,000 RAT to generate earnings.
```

### 场景 2：余额 < 10,000 RAT（有外部转账）

**用户情况**：
- 系统记录余额：2,575.30 RAT
- 外部转账：7,841.30 RAT
- 当前余额：10,416.60 RAT
- 历史收益（脏数据）：50 USDT

**修复后的处理**：
```
incrementalEarnings = 0  // 检测到外部转账，时间差 = 0
validBaseEarnings = 0     // 系统记录余额 < 10,000，历史收益视为脏数据，拒绝
realTimeEarnings = 0      // 总收益为 0
可提现金额 = 0 USDT
```

**说明**：
- 虽然当前余额 >= 10,000 RAT，但系统记录余额 < 10,000 RAT
- 历史收益是基于系统记录余额计算的，属于脏数据
- 外部转账的代币不计算历史收益
- 用户需要等待一段时间，让外部转账的代币产生收益后再提现

### 场景 3：余额 >= 10,000 RAT（正常用户）

**用户情况**：
- 当前余额：50,000 RAT
- 历史收益：100 USDT（合法）

**修复后的处理**：
```
incrementalEarnings = 50,000 × 0.01 × (4% / 100) × daysElapsed
validBaseEarnings = 100  // 余额 >= 10,000，认可历史收益
realTimeEarnings = 100 + incrementalEarnings
可提现金额 = realTimeEarnings - Pending 提现
```

---

## 🎯 数据清理建议

### 识别脏数据

**SQL 查询**：
```sql
-- 查找余额 < 10,000 RAT 但 usdt_total > 0 的用户（脏数据）
SELECT 
  address,
  rat_balance_wei,
  usdt_total,
  energy_total
FROM users
WHERE 
  (rat_balance_wei IS NULL OR CAST(rat_balance_wei AS NUMERIC) / 1e18 < 10000)
  AND usdt_total > 0;
```

### 清理策略

**选项 1：保留但标记为无效**
- 不删除数据，但在代码中拒绝使用
- 优点：保留历史记录，便于审计
- 缺点：数据库中存在脏数据

**选项 2：清零脏数据**
- 将 `usdt_total` 设置为 0
- 优点：数据库干净
- 缺点：丢失历史记录

**建议**：采用选项 1（代码中拒绝使用），保留历史记录用于审计。

---

## 📝 相关代码

**文件**:
- `rabbit-ai-backend/src/services/earnings.ts`
- `rabbit-ai-backend/src/services/withdraw.ts`

**关键修改**:
1. 添加 `MIN_BALANCE_FOR_EARNINGS = 10000` 常量
2. 在计算增量收益前检查余额
3. 验证 `baseEarnings` 的合法性
4. 使用 `validBaseEarnings` 而不是 `baseEarnings`
5. 增强错误信息和日志

---

## ✅ 修复完成

**修复时间**: 2026-01-09  
**修复内容**:
1. ✅ 添加持币生息最低门槛验证（10,000 RAT）
2. ✅ 验证收益的合法性（拒绝脏数据）
3. ✅ 不允许提现非法收益
4. ✅ 增强错误信息和日志

**测试建议**:
1. 测试余额 < 10,000 RAT 的用户（应该显示 0 收益）
2. 测试余额 < 10,000 RAT 但数据库中有收益的用户（应该拒绝提现）
3. 测试余额 >= 10,000 RAT 的正常用户（应该正常工作）
4. 验证错误信息的准确性

---

**文档维护**：如有更新，请及时更新本文档。

