# Rabbit AI 后端 API 接口文档

**版本**: v1.0  
**更新日期**: 2026-01-09  
**Base URL**: `https://your-api-domain.com`  
**维护人员**: 开发团队

---

## 📋 目录

1. [通用说明](#通用说明)
2. [用户相关 API](#用户相关-api)
3. [资产相关 API](#资产相关-api)
4. [空投领取 API](#空投领取-api)
5. [管理员 API](#管理员-api)
6. [系统 API](#系统-api)
7. [访问统计 API](#访问统计-api)
8. [错误码说明](#错误码说明)

---

## 通用说明

### 请求格式

- **Content-Type**: `application/json`
- **字符编码**: UTF-8

### 响应格式

**成功响应**：
```json
{
  "ok": true,
  "data": { ... }
}
```

**错误响应**：
```json
{
  "ok": false,
  "code": "ERROR_CODE",
  "message": "错误描述"
}
```

### 认证方式

**管理员 API**：
- 需要在请求头中携带 `Authorization: Bearer {ADMIN_API_KEY}`
- 所有管理员 API 都需要认证

**用户 API**：
- 无需认证（公开 API）

---

## 用户相关 API

### 1. 获取用户信息

**接口**: `GET /api/user/info`

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| address | string | 是 | 用户钱包地址（0x开头） |

**请求示例**：
```bash
GET /api/user/info?address=0x1234...
```

**响应示例**：
```json
{
  "ok": true,
  "address": "0x1234...",
  "energyTotal": 100,
  "energyLocked": 10,
  "inviteCount": 5,
  "referrerAddress": "0x5678...",
  "createdAt": "2026-01-01T00:00:00.000Z"
}
```

---

### 2. 获取团队奖励

**接口**: `GET /api/user/team-rewards`

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| address | string | 是 | 用户钱包地址 |

**请求示例**：
```bash
GET /api/user/team-rewards?address=0x1234...
```

**响应示例**：
```json
{
  "ok": true,
  "totalRewards": "100.5",
  "totalEnergy": 50,
  "downlines": [
    {
      "address": "0xabcd...",
      "totalRewards": "50.25",
      "totalEnergy": 25,
      "inviteTime": "2026-01-01T00:00:00.000Z"
    }
  ]
}
```

---

### 3. 获取领取历史

**接口**: `GET /api/user/claims`

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| address | string | 是 | 用户钱包地址 |

**请求示例**：
```bash
GET /api/user/claims?address=0x1234...
```

**响应示例**：
```json
[
  {
    "txHash": "0xabcd...",
    "amount": "100.5",
    "blockNumber": 12345678,
    "blockTime": "2026-01-01T00:00:00.000Z",
    "createdAt": "2026-01-01T00:00:00.000Z"
  }
]
```

---

### 4. 获取推荐历史

**接口**: `GET /api/user/referrals`

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| address | string | 是 | 用户钱包地址 |

**请求示例**：
```bash
GET /api/user/referrals?address=0x1234...
```

**响应示例**：
```json
[
  {
    "downlineAddress": "0xabcd...",
    "amount": "50.25",
    "energyReward": 3,
    "blockTime": "2026-01-01T00:00:00.000Z",
    "isFirstClaim": true
  }
]
```

---

### 5. 获取用户通知

**接口**: `GET /api/user/notifications`

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| address | string | 是 | 用户钱包地址 |

**请求示例**：
```bash
GET /api/user/notifications?address=0x1234...
```

**响应示例**：
```json
[
  {
    "id": "uuid",
    "title": "通知标题",
    "content": "通知内容",
    "isRead": false,
    "createdAt": "2026-01-01T00:00:00.000Z"
  }
]
```

---

### 6. 标记通知为已读

**接口**: `POST /api/user/notifications/read`

**请求体**：
```json
{
  "address": "0x1234...",
  "notificationId": "uuid"
}
```

**响应示例**：
```json
{
  "ok": true,
  "message": "Notification marked as read"
}
```

---

### 7. 标记所有通知为已读

**接口**: `POST /api/user/notifications/read-all`

**请求体**：
```json
{
  "address": "0x1234..."
}
```

**响应示例**：
```json
{
  "ok": true,
  "message": "All notifications marked as read"
}
```

---

### 8. 删除通知

**接口**: `POST /api/user/notifications/delete`

**请求体**：
```json
{
  "address": "0x1234...",
  "notificationId": "uuid"
}
```

**响应示例**：
```json
{
  "ok": true,
  "message": "Notification deleted"
}
```

---

## 资产相关 API

### 1. 申请提现

**接口**: `POST /api/asset/withdraw/apply`

**请求体**：
```json
{
  "address": "0x1234...",
  "amount": "100.5"
}
```

**响应示例**：
```json
{
  "ok": true,
  "id": "withdrawal_id",
  "status": "Pending",
  "amount": "100.5",
  "time": "2026-01-01 00:00:00"
}
```

**错误响应**：
```json
{
  "ok": false,
  "code": "ENERGY_NOT_ENOUGH",
  "message": "Energy not enough (need >= 1000, available 500)"
}
```

---

### 2. 获取提现历史

**接口**: `GET /api/asset/withdraw/history`

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| address | string | 是 | 用户钱包地址 |

**请求示例**：
```bash
GET /api/asset/withdraw/history?address=0x1234...
```

**响应示例**：
```json
[
  {
    "id": "withdrawal_id",
    "amount": "100.5",
    "status": "Completed",
    "time": "2026-01-01 00:00:00",
    "createdAt": "2026-01-01T00:00:00.000Z",
    "energyCost": 1000
  }
]
```

---

### 3. 获取收益信息

**接口**: `GET /api/asset/earnings`

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| address | string | 是 | 用户钱包地址 |

**请求示例**：
```bash
GET /api/asset/earnings?address=0x1234...
```

**响应示例**：
```json
{
  "pendingUsdt": "100.5",
  "dailyRate": 4,
  "currentTier": 2,
  "holdingDays": 30
}
```

**字段说明**：
- `pendingUsdt`：可领收益（USDT，6位小数）
- `dailyRate`：日利率（百分比，例如 4 表示 4%）
- `currentTier`：VIP 等级（0-4）
- `holdingDays`：持币天数（取整）

---

### 4. 获取 RAT 余额

**接口**: `GET /api/asset/rat-balance`

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| address | string | 是 | 用户钱包地址 |

**请求示例**：
```bash
GET /api/asset/rat-balance?address=0x1234...
```

**响应示例**：
```json
{
  "balance": "50000.00"
}
```

**说明**：
- 余额从链上读取（实时）
- 如果 RPC 调用失败或超时，返回 `"0.00"`

---

## 空投领取 API

### 1. 验证空投领取

**接口**: `POST /api/mining/verify-claim`

**请求体**：
```json
{
  "address": "0x1234...",
  "txHash": "0xabcd...",
  "referrer": "0x5678..."
}
```

**响应示例**：
```json
{
  "ok": true,
  "txHash": "0xabcd...",
  "amount": "100.5",
  "unit": "RAT",
  "blockNumber": 12345678,
  "blockTime": "2026-01-01T00:00:00.000Z",
  "attempt": 1
}
```

**说明**：
- `referrer` 参数仅在新用户首次绑定时有效
- 如果用户已有推荐人，系统会忽略传入的 `referrer` 参数（推荐人终身制）

**错误响应**：
```json
{
  "ok": false,
  "code": "TX_NOT_FOUND",
  "message": "Transaction not found"
}
```

---

## 管理员 API

**认证方式**：所有管理员 API 都需要在请求头中携带 `Authorization: Bearer {ADMIN_API_KEY}`

### 1. 验证管理员密钥

**接口**: `GET /api/admin/auth/verify`

**请求头**：
```
Authorization: Bearer {ADMIN_API_KEY}
```

**响应示例**：
```json
{
  "ok": true,
  "message": "Admin key verified successfully",
  "timestamp": "2026-01-01T00:00:00.000Z"
}
```

---

### 2. 获取 KPI 数据

**接口**: `GET /api/admin/kpis`

**响应示例**：
```json
{
  "ok": true,
  "totalUsers": 1000,
  "pendingWithdrawals": 10,
  "totalAirdropFees": "1000.5",
  "totalRatHoldings": "5000000.00"
}
```

---

### 3. 获取待处理提现列表

**接口**: `GET /api/admin/withdrawals/pending`

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| limit | number | 否 | 返回数量（默认 50，最大 200） |

**请求示例**：
```bash
GET /api/admin/withdrawals/pending?limit=50
```

**响应示例**：
```json
{
  "ok": true,
  "withdrawals": [
    {
      "id": "withdrawal_id",
      "address": "0x1234...",
      "amount": "100.5",
      "status": "Pending",
      "createdAt": "2026-01-01T00:00:00.000Z",
      "energyCost": 1000
    }
  ],
  "total": 10
}
```

---

### 4. 拒绝提现

**接口**: `POST /api/admin/withdrawals/:id/reject`

**请求体**：
```json
{
  "reason": "拒绝原因（可选）"
}
```

**响应示例**：
```json
{
  "ok": true,
  "message": "Withdrawal rejected"
}
```

---

### 5. 完成提现

**接口**: `POST /api/admin/withdrawals/:id/complete`

**请求体**：
```json
{
  "payoutTxHash": "0xabcd..."
}
```

**响应示例**：
```json
{
  "ok": true,
  "message": "Withdrawal completed"
}
```

---

### 6. 获取用户信息

**接口**: `GET /api/admin/users/:address`

**响应示例**：
```json
{
  "ok": true,
  "address": "0x1234...",
  "energyTotal": 100,
  "energyLocked": 10,
  "usdtTotal": "100.5",
  "usdtLocked": "50.25",
  "inviteCount": 5,
  "referrerAddress": "0x5678...",
  "ratBalance": "50000.00",
  "createdAt": "2026-01-01T00:00:00.000Z"
}
```

---

### 7. 获取用户团队

**接口**: `GET /api/admin/users/:address/team`

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| limit | number | 否 | 返回数量（默认 50，最大 200） |
| offset | number | 否 | 偏移量（默认 0） |

**响应示例**：
```json
{
  "ok": true,
  "downlines": [
    {
      "address": "0xabcd...",
      "energyTotal": 50,
      "inviteCount": 2,
      "createdAt": "2026-01-01T00:00:00.000Z"
    }
  ],
  "total": 10,
  "limit": 50,
  "offset": 0
}
```

---

### 8. 获取用户收益

**接口**: `GET /api/admin/users/:address/earnings`

**响应示例**：
```json
{
  "ok": true,
  "pendingUsdt": "100.5",
  "dailyRate": 4,
  "currentTier": 2,
  "holdingDays": 30
}
```

---

### 9. 调整用户能量

**接口**: `POST /api/admin/users/:address/energy`

**请求体**：
```json
{
  "delta": 100,
  "reason": "调整原因（可选）"
}
```

**响应示例**：
```json
{
  "ok": true,
  "message": "Energy adjusted",
  "oldValue": 100,
  "newValue": 200
}
```

---

### 10. 调整用户 USDT

**接口**: `POST /api/admin/users/:address/usdt`

**请求体**：
```json
{
  "delta": "50.5",
  "reason": "调整原因（可选）"
}
```

**响应示例**：
```json
{
  "ok": true,
  "message": "USDT adjusted",
  "oldValue": "100.5",
  "newValue": "151.0"
}
```

---

### 11. 设置用户结算时间

**接口**: `POST /api/admin/users/:address/settlement-time`

**请求体**：
```json
{
  "settlementTime": "2026-01-01T00:00:00.000Z"
}
```

**响应示例**：
```json
{
  "ok": true,
  "message": "Settlement time updated"
}
```

**说明**：
- 用于处理通过直接转账获得代币的情况
- 确保收益从正确的时间开始计算

---

### 12. 获取用户列表

**接口**: `GET /api/admin/users/list`

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| limit | number | 否 | 返回数量（默认 50，最大 200） |
| offset | number | 否 | 偏移量（默认 0） |
| search | string | 否 | 搜索关键词（地址） |
| sortBy | string | 否 | 排序字段（energy_total/usdt_total/created_at） |
| sortOrder | string | 否 | 排序方向（asc/desc） |

**响应示例**：
```json
{
  "ok": true,
  "users": [
    {
      "address": "0x1234...",
      "energyTotal": 100,
      "usdtTotal": "100.5",
      "createdAt": "2026-01-01T00:00:00.000Z"
    }
  ],
  "total": 1000,
  "limit": 50,
  "offset": 0
}
```

---

### 13. 获取能量配置

**接口**: `GET /api/public/energy-config`

**说明**：公开 API，无需认证

**响应示例**：
```json
{
  "ok": true,
  "config": {
    "withdraw_energy_ratio": 10,
    "claim_self_reward": 1,
    "claim_referrer_first": 3,
    "claim_referrer_repeat": 1
  }
}
```

---

### 14. 更新能量配置

**接口**: `PUT /api/admin/energy-config/:key`

**请求体**：
```json
{
  "value": 10,
  "reason": "调整原因（可选）"
}
```

**响应示例**：
```json
{
  "ok": true,
  "oldValue": 5,
  "newValue": 10
}
```

---

### 15. 获取能量配置历史

**接口**: `GET /api/admin/energy-config/history`

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| key | string | 否 | 配置键（可选） |
| limit | number | 否 | 返回数量（默认 50） |

**响应示例**：
```json
{
  "ok": true,
  "history": [
    {
      "id": "uuid",
      "key": "withdraw_energy_ratio",
      "oldValue": 5,
      "newValue": 10,
      "changedBy": "admin",
      "changeReason": "调整提现能量比例",
      "createdAt": "2026-01-01T00:00:00.000Z"
    }
  ]
}
```

---

### 16. 获取 VIP 等级配置

**接口**: `GET /api/vip/tiers`

**说明**：公开 API，无需认证

**响应示例**：
```json
{
  "ok": true,
  "tiers": [
    {
      "level": 1,
      "name": "🌱 新手",
      "min": 10000,
      "max": 49999,
      "dailyRate": 2
    },
    {
      "level": 2,
      "name": "🌿 进阶",
      "min": 50000,
      "max": 99999,
      "dailyRate": 4
    }
  ]
}
```

---

## 系统 API

### 1. 健康检查

**接口**: `GET /api/health`

**响应示例**：
```json
{
  "ok": true,
  "time": "2026-01-01T00:00:00.000Z"
}
```

---

### 2. 索引器健康检查

**接口**: `GET /api/health/indexer`

**响应示例**：
```json
{
  "ok": true,
  "chainSync": {
    "id": "bsc_airdrop",
    "lastBlock": 12345678,
    "updatedAt": "2026-01-01T00:00:00.000Z"
  },
  "time": "2026-01-01T00:00:00.000Z"
}
```

---

### 3. 获取系统链接

**接口**: `GET /api/system/links`

**响应示例**：
```json
{
  "ok": true,
  "whitepaper": "https://...",
  "audits": "https://...",
  "support": "https://..."
}
```

---

### 4. 获取倒计时配置

**接口**: `GET /api/system/countdown-config`

**响应示例**：
```json
{
  "ok": true,
  "targetTime": "2026-01-01T00:00:00.000Z",
  "enabled": true
}
```

---

## 访问统计 API

### 1. 记录页面访问

**接口**: `POST /api/analytics/visit`

**请求体**：
```json
{
  "pagePath": "/",
  "walletAddress": "0x1234...",
  "referrer": "0x5678...",
  "language": "zh",
  "isMobile": true,
  "sessionId": "session_id"
}
```

**响应示例**：
```json
{
  "ok": true,
  "message": "Visit recorded"
}
```

**说明**：
- 公开 API，无需认证
- 有 Rate Limit 保护（防止滥用）

---

## 错误码说明

| 错误码 | HTTP 状态码 | 说明 |
|--------|------------|------|
| `INVALID_REQUEST` | 400 | 请求参数无效 |
| `NOT_FOUND` | 404 | 资源不存在 |
| `TX_NOT_FOUND` | 404 | 交易不存在 |
| `TX_FAILED` | 400 | 交易失败 |
| `TX_TO_MISMATCH` | 400 | 交易接收方不匹配 |
| `TX_FROM_MISMATCH` | 400 | 交易发送方不匹配 |
| `EVENT_NOT_FOUND` | 400 | 事件未找到 |
| `USDT_NOT_ENOUGH` | 400 | USDT 余额不足 |
| `ENERGY_NOT_ENOUGH` | 400 | 能量值不足 |
| `UNAUTHORIZED` | 401 | 未授权（管理员 API） |
| `INTERNAL_ERROR` | 500 | 服务器内部错误 |
| `DATABASE_ERROR` | 500 | 数据库错误 |
| `CONFIG_ERROR` | 500 | 配置错误 |
| `RPC_TIMEOUT` | 500 | RPC 调用超时 |

---

## 注意事项

### 1. 推荐人地址终身制

- 推荐关系一次绑定，终身有效
- 如果用户已有推荐人，系统会忽略前端传入的 `referrer` 参数
- 只有新用户（没有推荐人）才接受前端传入的参数

### 2. Lazy Settle 机制

- 查询收益时：只计算，不更新数据库
- 提现时：固化收益到数据库
- 这样可以减少数据库写入，提高性能

### 3. RPC 超时保护

- RPC 调用超时时间：8-10 秒
- 如果超时，使用默认值 0（不会阻塞请求）

### 4. 能量配置动态调整

- 能量配置支持动态调整（无需重启）
- 配置缓存 60 秒，自动刷新

### 5. 并发安全

- 所有关键操作都使用数据库行锁和原子更新
- 防止高并发场景下的数据不一致

---

**文档维护**：如有更新，请及时更新本文档。

