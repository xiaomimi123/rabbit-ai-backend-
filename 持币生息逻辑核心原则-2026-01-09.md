# 持币生息逻辑核心原则（最终版）

**日期**: 2026-01-09  
**目的**: 明确持币生息逻辑的核心原则，避免未来反复修改  
**状态**: ✅ 最终版本，请严格遵循

---

## 🎯 核心原则（必须遵循）

### 原则1：`last_settlement_time` 的唯一含义

**定义**：
- `last_settlement_time` = **用户首次达到 10,000 RAT 的时间点**
- 或者：用户首次领取时间（如果从未达到 10k）

**关键**：
- 这是收益计算的起点
- 一旦设置，除非特殊情况，**不应该被修改**

### 原则2：收益计算公式

```
增量收益 = RAT余额 × 代币价格($0.01) × 日利率(%) × 持币天数
持币天数 = (当前时间 - last_settlement_time) / (24 × 3600 × 1000)
```

**关键**：
- 增量收益基于 **RAT余额**，与 USDT 无关
- `last_settlement_time` 是收益计算的起点

### 原则3：最低门槛

**规则**：
- 只有余额 >= 10,000 RAT 才能产生收益
- 如果余额 < 10,000 RAT：增量收益 = 0
- 如果系统记录余额 < 10,000 RAT：历史收益视为脏数据，拒绝使用

---

## 📋 `last_settlement_time` 更新规则（最终版）

### ✅ 允许更新的情况（仅4种）

| # | 情况 | 更新为 | 代码位置 | 原因 |
|---|------|--------|---------|------|
| 1 | **首次达到10k** | 首次累计余额达到10k的时间 | `earnings.ts` | 初始化收益计算起点 |
| 2 | **领取空投** | 领取时间 | `settle_earnings_on_claim` | 固化之前的收益，新代币从领取时间开始计算 |
| 3 | **检测到外部转账** | 当前时间 | `earnings.ts`, `withdraw.ts` | 防止 Sybil Attack |
| 4 | **管理员手动设置** | 管理员指定的时间 | `admin.ts` | 特殊情况处理 |

### ❌ 不允许更新的情况（必须遵守）

| # | 情况 | 代码位置 | 状态 |
|---|------|---------|------|
| 1 | **管理员赠送USDT** | `admin.ts` | ✅ 已修复（不更新） |
| 2 | **用户提现** | `withdraw.ts` | ✅ 已修复（不更新） |

---

## 🔧 代码实现检查清单

### ✅ 已修复的问题

1. **`admin.ts` - 管理员赠送USDT**
   ```typescript
   const updateSettlementTime = false; // ✅ 不更新
   ```

2. **`withdraw.ts` - 用户提现**
   ```typescript
   const originalLastSettlementTime = (user as any)?.last_settlement_time || null;
   // ✅ 传递给数据库函数，保持不变
   ```

3. **`earnings.ts` - 收益计算**
   - ✅ 检测外部转账时更新（正确）
   - ✅ 初始化首次达到10k的时间（正确）
   - ✅ 不随意更新（正确）

4. **`settle_earnings_on_claim` - 领取空投**
   - ✅ 更新为领取时间（正确，需要固化之前的收益）

---

## 🎯 为什么之前会反复修改？

### 根本原因

1. **缺乏明确的设计原则**
   - 没有明确 `last_settlement_time` 的唯一含义
   - 没有明确的更新规则

2. **业务逻辑理解不完整**
   - 管理员赠送USDT时不应该更新（已修复）
   - 提现时不应该更新（已修复）
   - 但领取空投时应该更新（这是正确的）

3. **代码注释不够清晰**
   - 没有明确说明为什么更新或不更新

### 解决方案

1. ✅ **明确核心原则**（本文档）
2. ✅ **统一代码实现**（所有相关代码遵循相同规则）
3. ✅ **完善代码注释**（说明为什么更新或不更新）

---

## 📝 未来维护指南

### 修改代码前必须确认

1. **是否会影响 `last_settlement_time`？**
   - 如果是，是否属于允许更新的4种情况之一？
   - 如果不是，**不要更新**

2. **是否会影响收益计算？**
   - 如果是，是否遵循核心原则？
   - 增量收益 = RAT余额 × 代币价格 × 日利率 × 持币天数

3. **是否会影响最低门槛验证？**
   - 如果是，是否检查余额 >= 10,000 RAT？
   - 是否检查系统记录余额 >= 10,000 RAT？

### 代码审查检查点

- [ ] `last_settlement_time` 的更新是否符合规则？
- [ ] 收益计算是否遵循公式？
- [ ] 最低门槛验证是否正确？
- [ ] 脏数据过滤是否正确？
- [ ] 外部转账检测是否正确？

---

## ✅ 总结

### 核心原则（必须遵循）

1. **`last_settlement_time` 的唯一含义**：用户首次达到10k的时间（或首次领取时间）
2. **更新规则**：只有在4种特定情况下才更新
3. **不更新规则**：管理员赠送USDT、用户提现时不应该更新

### 当前状态

- ✅ 所有关键问题已修复
- ✅ 代码逻辑已统一
- ✅ 设计文档已完善

### 未来维护

- ✅ 遵循本文档的核心原则
- ✅ 任何修改都要参考本文档
- ✅ 代码审查时参考本文档

---

**重要提示**：这是持币生息逻辑的最终设计文档。任何修改都要参考本文档并遵循核心原则。

