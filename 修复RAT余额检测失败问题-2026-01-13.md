# 修复 RAT 余额检测失败问题

## 📋 问题描述

**错误信息**：
```
[Earnings] Failed to fetch RAT balance for 0x591325a8fe68e3abbae1867097ac44a9fdc47fd5: could not detect network (event="noNetwork", code=NETWORK_ERROR, version=providers/5.8.0), using default 0
```

**问题表现**：
- 用户链上实际持有 1 万多枚 RAT 代币
- 系统检测到的余额为 0.00 RAT
- 导致不计算增量收益（余额 < 10000 RAT 阈值）

**根本原因**：
- `ethers.providers.JsonRpcProvider` 在创建时没有显式指定网络参数
- ethers.js 会尝试自动检测网络（调用 RPC 的 `eth_chainId` 或 `net_version`）
- 当 RPC 节点响应异常或网络不稳定时，自动检测失败，抛出 `NETWORK_ERROR`
- 导致后续的 `balanceOf` 调用失败，返回默认值 0

---

## 🔧 修复方案

### 修复 1: 在 RpcPool 中显式指定 BSC 主网网络 ✅

**文件**: `rabbit-ai-backend/src/infra/rpcPool.ts`

**修改内容**：
1. 创建 `BSC_MAINNET` 网络配置常量（Chain ID: 56）
2. 在创建 `JsonRpcProvider` 时显式传入网络参数

**代码变更**：
```typescript
// 新增：BSC 主网网络配置
export const BSC_MAINNET: ethers.providers.Network = {
  chainId: 56,
  name: 'bsc',
  ensAddress: undefined,
};

// 修改：创建 Provider 时显式指定网络
const provider = new ethers.providers.JsonRpcProvider({
  url,
  timeout: 30000,
}, BSC_MAINNET); // 🔒 显式指定 BSC 主网，避免自动检测失败
```

**效果**：
- ✅ 不再需要自动检测网络，直接使用指定的 BSC 主网配置
- ✅ 即使 RPC 节点的网络检测接口异常，也能正常工作
- ✅ 提高稳定性和可靠性

---

### 修复 2: 在 Admin Provider 中显式指定网络 ✅

**文件**: `rabbit-ai-backend/src/index.ts`

**修改内容**：
- 在创建 `adminProvider` 时也显式指定 BSC 主网网络

**代码变更**：
```typescript
if (config.adminRpcUrl) {
  const { BSC_MAINNET } = await import('./infra/rpcPool.js');
  adminProvider = new ethers.providers.JsonRpcProvider({
    url: config.adminRpcUrl,
    timeout: 30000,
  }, BSC_MAINNET); // 🔒 显式指定 BSC 主网
}
```

---

## ✅ 修复效果

### 修复前
- ❌ RPC 节点网络检测失败时，余额查询返回 0
- ❌ 用户实际持有代币但系统无法检测
- ❌ 导致增量收益计算错误

### 修复后
- ✅ 显式指定网络，不依赖自动检测
- ✅ 即使 RPC 节点网络检测接口异常，也能正常查询余额
- ✅ 用户余额能够正确检测，增量收益计算正常

---

## 🔍 技术细节

### ethers.js JsonRpcProvider 网络检测机制

**自动检测流程**：
1. 创建 `JsonRpcProvider` 时，如果不指定 `network` 参数
2. ethers.js 会调用 RPC 的 `eth_chainId` 方法
3. 如果 `eth_chainId` 失败，尝试调用 `net_version`
4. 如果都失败，抛出 `NETWORK_ERROR: could not detect network`

**问题场景**：
- RPC 节点响应慢或超时
- RPC 节点返回异常响应格式
- 网络不稳定导致请求失败
- RPC 节点临时不可用

**解决方案**：
- 显式指定网络配置，跳过自动检测步骤
- 直接使用已知的 BSC 主网配置（Chain ID: 56）
- 提高稳定性和可靠性

---

## 📝 相关文件

### 已修复文件
- ✅ `rabbit-ai-backend/src/infra/rpcPool.ts` - RPC 连接池
- ✅ `rabbit-ai-backend/src/index.ts` - 服务启动入口

### 建议修复文件（可选）
以下文件也创建了 `JsonRpcProvider`，建议也应用相同修复：
- `rabbit-ai-backend/scripts/sync-all-rat-balances.ts`
- `rabbit-ai-backend/scripts/backfill-claim-fees.ts`
- `rabbit-ai-backend/scripts/fix-user-claim.ts`
- `rabbit-ai-backend/scripts/manual-index-tx.ts`
- `rabbit-ai-backend/src/scripts/findFirst10kTime.ts`

**修复方法**：
```typescript
import { BSC_MAINNET } from '../src/infra/rpcPool.js';

const provider = new ethers.providers.JsonRpcProvider(
  rpcUrl,
  BSC_MAINNET // 显式指定网络
);
```

---

## 🚀 部署说明

### 部署步骤
1. ✅ 代码已修复并提交
2. ⏳ 部署到生产环境
3. ⏳ 验证用户余额查询是否正常
4. ⏳ 监控错误日志，确认不再出现 `NETWORK_ERROR`

### 验证方法
1. 查询之前失败的用户地址：`0x591325a8fe68e3abbae1867097ac44a9fdc47fd5`
2. 检查日志中是否还有 `could not detect network` 错误
3. 验证用户余额是否正确显示（应该显示 1 万多枚）
4. 验证增量收益是否正确计算

---

## 📊 影响范围

### 直接影响
- ✅ 所有通过 `RpcPool` 创建的 Provider（主要代码路径）
- ✅ Admin Provider（后台管理功能）
- ✅ 收益计算引擎（`earnings.ts`）
- ✅ 资产查询 API（`asset.ts`）

### 间接影响
- ✅ 所有依赖 RPC 查询余额的功能
- ✅ 所有依赖 RPC 查询链上数据的功能

---

## 🔒 风险评估

### 风险等级：低
- ✅ 修复是向后兼容的，不影响现有功能
- ✅ 只是显式指定了已知的网络配置
- ✅ 不改变任何业务逻辑

### 测试建议
1. 测试正常用户的余额查询
2. 测试边界情况（余额为 0、余额很大等）
3. 测试 RPC 节点异常时的降级处理
4. 验证收益计算是否正确

---

## 📅 修复时间

**修复日期**: 2026-01-13  
**修复人员**: AI Assistant  
**优先级**: 🔴 P0（紧急修复）  
**状态**: ✅ 已完成

---

## 📚 参考资料

- [ethers.js JsonRpcProvider 文档](https://docs.ethers.io/v5/api/providers/jsonrpc-provider/)
- [BSC 主网 Chain ID: 56](https://docs.binance.org/smart-chain/developer/rpc.html)
- 错误信息：`could not detect network (event="noNetwork", code=NETWORK_ERROR)`

