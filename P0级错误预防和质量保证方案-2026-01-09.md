# P0级错误预防和质量保证方案

**制定日期**: 2026-01-09  
**严重级别**: 最高优先级  
**目标**: 确保不再出现P0级错误，保护项目和市场

---

## 📋 本次P0错误的根本原因分析

### 已发生的P0级错误

1. **管道收益丢失（竞态条件）**
   - 原因：数据库并发写入导致丢失更新
   - 影响：用户能量值丢失，影响收益计算

2. **代币奖励分发错误**
   - 原因：使用事件中的referrer而不是claims表中的referrer
   - 影响：推荐奖励被错误分配，用户投诉

### 共同的根本原因

1. **缺乏并发安全考虑**
   - 没有使用数据库锁（FOR UPDATE）
   - 没有事务保护
   - 没有考虑高并发场景

2. **数据源不一致**
   - 多个数据源（事件、数据库表）没有统一
   - 没有验证数据一致性

3. **缺乏测试**
   - 没有并发测试
   - 没有边界条件测试
   - 没有数据一致性测试

4. **代码审查不足**
   - 没有从并发安全角度审查
   - 没有从数据一致性角度审查
   - 没有考虑极端场景

---

## 🛡️ 预防措施（立即实施）

### 1. 代码审查检查清单（强制）

#### 涉及金钱/积分的代码必须检查：

- [ ] **并发安全**
  - [ ] 是否使用了数据库锁（FOR UPDATE）？
  - [ ] 是否使用了事务（BEGIN/COMMIT）？
  - [ ] 是否考虑了高并发场景（100+请求同时打进来）？
  - [ ] 是否使用了原子更新（UPDATE ... SET field = field + value）？

- [ ] **数据一致性**
  - [ ] 数据源是否统一（只使用一个可信数据源）？
  - [ ] 是否验证了数据一致性（事件数据 vs 数据库数据）？
  - [ ] 是否处理了数据不一致的情况？

- [ ] **幂等性**
  - [ ] 是否检查了重复处理（ON CONFLICT）？
  - [ ] 是否处理了重复请求？
  - [ ] 是否使用了唯一约束？

- [ ] **边界条件**
  - [ ] 是否处理了NULL值？
  - [ ] 是否处理了空字符串？
  - [ ] 是否处理了零地址（0x0000...）？
  - [ ] 是否处理了负数？

- [ ] **恶意用户防护**
  - [ ] 是否检查了自推荐（自己推荐自己）？
  - [ ] 是否检查了双花攻击？
  - [ ] 是否检查了外部转账？

- [ ] **错误处理**
  - [ ] 是否捕获了所有异常？
  - [ ] 是否记录了详细的错误日志？
  - [ ] 是否返回了有意义的错误信息？

### 2. 数据库函数开发规范

#### 所有涉及金钱/积分的数据库函数必须：

```sql
-- ✅ 必须包含：
1. SECURITY DEFINER（确保权限）
2. SET search_path = public（防止SQL注入）
3. 行锁（FOR UPDATE）
4. 事务保护（BEGIN/COMMIT）
5. 幂等性检查（ON CONFLICT）
6. 数据验证（NULL检查、地址验证）
7. 错误处理（EXCEPTION WHEN OTHERS）
```

#### 示例模板：

```sql
CREATE OR REPLACE FUNCTION public.your_function_name(
  -- 参数定义
)
RETURNS jsonb
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public
AS $function$
DECLARE
  -- 变量定义
BEGIN
  -- 1. 参数验证
  IF p_param IS NULL OR p_param = '' THEN
    RETURN jsonb_build_object('status', 'error', 'reason', 'invalid_param');
  END IF;
  
  -- 2. 🔒 行锁（防止并发）
  PERFORM 1 FROM table_name WHERE id = p_id FOR UPDATE;
  
  -- 3. 数据验证
  -- 检查数据是否存在、是否有效
  
  -- 4. 业务逻辑（使用原子更新）
  UPDATE table_name
  SET field = field + value  -- ✅ 原子更新
  WHERE id = p_id;
  
  -- 5. 返回结果
  RETURN jsonb_build_object('status', 'success');
  
EXCEPTION
  WHEN OTHERS THEN
    RETURN jsonb_build_object('status', 'error', 'message', SQLERRM);
END;
$function$;
```

### 3. 后端代码开发规范

#### 所有涉及金钱/积分的后端代码必须：

```typescript
// ✅ 必须包含：
1. 使用数据库函数（而不是直接SQL）
2. 数据源统一（只使用一个可信数据源）
3. 数据验证（验证数据一致性）
4. 错误处理（try-catch）
5. 详细日志（记录所有关键操作）
```

#### 示例模板：

```typescript
async function processReward(params: {
  txHash: string;
  address: string;
  referrer: string;
}) {
  try {
    // 1. 参数验证
    if (!params.txHash || !params.address) {
      throw new ApiError('INVALID_PARAM', 'Missing required parameters', 400);
    }
    
    // 2. 数据源统一：使用数据库中的referrer，而不是事件中的referrer
    const referrerFromDb = params.referrer; // ✅ 使用数据库中的referrer
    
    // 3. 数据验证：验证事件数据与数据库数据是否一致
    if (eventReferrer && eventReferrer !== referrerFromDb) {
      console.warn(`[processReward] ⚠️ 推荐人不一致: 事件=${eventReferrer}, 数据库=${referrerFromDb}`);
      // 使用数据库中的referrer（可信数据源）
    }
    
    // 4. 使用数据库函数（带锁和事务保护）
    const { data, error } = await supabase.rpc('insert_reward_safe', {
      p_tx_hash: params.txHash,
      p_address: params.address,
      p_referrer: referrerFromDb, // ✅ 使用数据库中的referrer
    });
    
    if (error) {
      throw new ApiError('DATABASE_ERROR', error.message, 500);
    }
    
    // 5. 详细日志
    console.log(`[processReward] ✅ 成功处理奖励: ${params.txHash}`);
    
    return data;
  } catch (error) {
    // 6. 错误处理
    console.error(`[processReward] ❌ 处理失败:`, error);
    throw error;
  }
}
```

### 4. 测试要求（强制）

#### 所有涉及金钱/积分的功能必须测试：

- [ ] **并发测试**
  - [ ] 100个请求同时打进来
  - [ ] 验证数据一致性
  - [ ] 验证没有丢失更新

- [ ] **边界条件测试**
  - [ ] NULL值处理
  - [ ] 空字符串处理
  - [ ] 零地址处理
  - [ ] 负数处理

- [ ] **恶意用户测试**
  - [ ] 自推荐测试
  - [ ] 双花攻击测试
  - [ ] 外部转账测试

- [ ] **数据一致性测试**
  - [ ] 事件数据 vs 数据库数据
  - [ ] 多个数据源的一致性

### 5. 代码审查流程（强制）

#### 所有涉及金钱/积分的代码必须：

1. **提交前自检**
   - 使用检查清单自检
   - 运行所有测试
   - 检查日志输出

2. **代码审查**
   - 至少一人审查
   - 重点关注并发安全
   - 重点关注数据一致性

3. **测试环境验证**
   - 在测试环境运行
   - 模拟高并发场景
   - 验证数据一致性

4. **生产环境监控**
   - 监控错误日志
   - 监控数据一致性
   - 监控并发性能

---

## 🔍 质量保证检查点

### 每次代码提交前必须检查：

1. **并发安全**
   - ✅ 是否使用了数据库锁？
   - ✅ 是否使用了事务？
   - ✅ 是否使用了原子更新？

2. **数据一致性**
   - ✅ 数据源是否统一？
   - ✅ 是否验证了数据一致性？
   - ✅ 是否处理了数据不一致？

3. **幂等性**
   - ✅ 是否检查了重复处理？
   - ✅ 是否使用了唯一约束？

4. **错误处理**
   - ✅ 是否捕获了所有异常？
   - ✅ 是否记录了详细日志？

5. **测试**
   - ✅ 是否通过了所有测试？
   - ✅ 是否测试了并发场景？
   - ✅ 是否测试了边界条件？

---

## 📊 监控和告警

### 必须监控的指标：

1. **数据一致性**
   - 监控 `claims.referrer` 与 `referral_rewards.referrer_address` 的一致性
   - 监控能量值计算的准确性
   - 监控收益计算的准确性

2. **并发性能**
   - 监控数据库锁等待时间
   - 监控事务执行时间
   - 监控并发请求数

3. **错误率**
   - 监控数据库错误率
   - 监控业务逻辑错误率
   - 监控数据不一致错误率

### 告警规则：

- ⚠️ **P0级告警**：数据不一致、并发冲突、收益计算错误
- ⚠️ **P1级告警**：数据库锁等待时间过长、事务执行时间过长
- ⚠️ **P2级告警**：错误率上升、性能下降

---

## 🎯 行动计划

### 立即执行（今天）

1. ✅ 修复所有已知的P0级错误
2. ✅ 添加数据库锁和事务保护
3. ✅ 修复历史数据

### 本周执行

1. ⏳ 审查所有涉及金钱/积分的代码
2. ⏳ 添加数据一致性检查脚本
3. ⏳ 添加并发测试
4. ⏳ 添加监控和告警

### 持续执行

1. ⏳ 每次代码提交前使用检查清单
2. ⏳ 定期审查代码
3. ⏳ 定期运行数据一致性检查
4. ⏳ 定期进行并发测试

---

## 📝 总结

### 核心原则

1. **永远假设高并发**：100个请求同时打进来
2. **永远假设恶意用户**：自己刷自己、双花攻击
3. **永远假设网络延迟**：数据库写入慢了2秒
4. **永远使用数据库锁**：FOR UPDATE
5. **永远使用事务**：BEGIN/COMMIT
6. **永远验证数据一致性**：多个数据源必须一致
7. **永远记录详细日志**：便于问题排查

### 关键教训

1. **并发安全不是可选的**：涉及金钱/积分的代码必须考虑并发
2. **数据源必须统一**：只使用一个可信数据源
3. **测试必须全面**：包括并发测试、边界条件测试
4. **代码审查必须严格**：重点关注并发安全和数据一致性

---

**制定人**: AI Assistant  
**审核人**: 待定  
**生效日期**: 2026-01-09  
**状态**: ✅ 已生效

