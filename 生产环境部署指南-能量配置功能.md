# 生产环境部署指南 - 能量配置动态调整功能

**部署日期**: 2026-01-07  
**功能**: 能量配置动态调整  
**影响范围**: 数据库、后端 API、前端管理后台

---

## ⚠️ 重要提醒

**必须按照以下顺序部署，否则可能导致服务异常！**

---

## 📋 部署步骤

### 步骤 1: 执行数据库迁移（🔴 必须先做）

#### 1.1 登录 Supabase Dashboard
1. 访问 https://supabase.com/dashboard
2. 选择你的生产项目
3. 进入 **SQL Editor**

#### 1.2 执行脚本 1: 创建能量配置表
复制并执行 `rabbit-ai-backend/db/create_energy_config.sql` 的内容：

**脚本位置**: `rabbit-ai-backend/db/create_energy_config.sql`

**脚本说明**:
- ✅ 创建 `energy_config` 表（存储能量配置）
- ✅ 创建 `energy_config_history` 表（存储变更历史）
- ✅ 插入默认配置值（提现比例、奖励能量等）
- ✅ 创建 `get_energy_config` 和 `update_energy_config` 函数

**预期结果**: 
```
CREATE TABLE
CREATE TABLE
INSERT 4
CREATE FUNCTION
CREATE FUNCTION
```

#### 1.3 执行脚本 2: 更新 process_claim_energy 函数
复制并执行 `rabbit-ai-backend/db/update_process_claim_energy_dynamic_config.sql` 的内容：

**脚本位置**: `rabbit-ai-backend/db/update_process_claim_energy_dynamic_config.sql`

**脚本说明**:
- ✅ 更新 `process_claim_energy` 函数，支持动态能量参数
- ✅ 新增 3 个参数: `p_self_reward`, `p_referrer_first`, `p_referrer_repeat`

**预期结果**: 
```
CREATE OR REPLACE FUNCTION
```

#### 1.4 验证数据库迁移
在 SQL Editor 中执行以下验证查询：

```sql
-- 1. 验证表是否创建成功
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
  AND table_name IN ('energy_config', 'energy_config_history');
-- 预期结果: 返回 2 行

-- 2. 验证默认配置是否插入成功
SELECT key, value, description 
FROM energy_config 
ORDER BY key;
-- 预期结果: 返回 4 行配置数据

-- 3. 验证函数是否更新成功
SELECT routine_name, routine_type 
FROM information_schema.routines 
WHERE routine_schema = 'public' 
  AND routine_name IN ('process_claim_energy', 'get_energy_config', 'update_energy_config');
-- 预期结果: 返回 3 行函数信息
```

**✅ 验证通过后才能继续下一步！**

---

### 步骤 2: 推送代码到 Git 仓库

#### 2.1 检查代码状态
```bash
cd E:\cursor软件开发\rabbitAIdifi
git status
```

#### 2.2 添加所有修改的文件
```bash
git add .
```

#### 2.3 提交代码
```bash
git commit -m "feat: 新增能量配置动态调整功能

- 后端: 新增能量配置管理服务和API
- 后端: 修改提现和领取逻辑使用动态配置
- 数据库: 新增 energy_config 表和相关函数
- 前端: 新增能量配置管理页面
- 前端: 新增 API 调用函数和路由配置
"
```

#### 2.4 推送到远程仓库
```bash
git push origin main
```

或者如果你的分支是 `master`:
```bash
git push origin master
```

---

### 步骤 3: 检查 Render 后端环境变量

#### 3.1 登录 Render Dashboard
1. 访问 https://dashboard.render.com
2. 选择你的后端服务（例如：`rabbit-ai-backend`）
3. 进入 **Environment** 标签页

#### 3.2 确认必需的环境变量
**✅ 本次功能不需要新增环境变量！**

只需确认以下现有环境变量已正确配置：
- `SUPABASE_URL` - Supabase 项目 URL
- `SUPABASE_KEY` - Supabase Service Role Key
- `ADMIN_API_KEY` - 管理员 API 密钥
- `SENTRY_DSN` - Sentry 错误监控 (可选)
- `SENTRY_ENABLED` - 是否启用 Sentry (可选)
- `RPC_URL` - BSC RPC 节点 URL
- `AIRDROP_CONTRACT_ADDRESS` - 空投合约地址
- `PRIVATE_KEY` - 管理员钱包私钥（用于手动发放提现）

**注意**: 本次功能使用的都是现有的环境变量，无需添加新的配置。

---

### 步骤 4: 等待 Render 自动部署

#### 4.1 自动触发部署
- Render 会自动检测到 Git 仓库的新提交
- 自动开始构建和部署流程

#### 4.2 查看部署日志
1. 在 Render Dashboard 中，进入 **Logs** 标签页
2. 等待部署完成（通常 3-5 分钟）
3. 查看是否有错误信息

**预期日志**:
```
==> Building...
==> Installing dependencies...
==> Building TypeScript...
Build succeeded
==> Starting server...
[启动] Rabbit AI Backend Server
[启动] ✅ Connected to Supabase
[启动] ✅ Server running on port 3000
```

---

### 步骤 5: 部署前端到生产环境

#### 5.1 构建前端
```bash
cd E:\cursor软件开发\rabbitAIdifi\rabbit-ai-admin
npm run build
```

#### 5.2 部署到你的前端托管服务
- 如果使用 Vercel/Netlify: 推送代码后自动部署
- 如果使用自己的服务器: 上传 `dist` 目录

---

### 步骤 6: 测试生产环境功能

#### 6.1 后端 API 测试
使用 curl 或 Postman 测试：

```bash
# 替换为你的生产环境 URL 和 ADMIN_API_KEY
export API_URL="https://your-backend.onrender.com"
export ADMIN_KEY="your-admin-api-key"

# 测试 1: 获取能量配置
curl -X GET "$API_URL/api/admin/energy-config" \
  -H "x-admin-api-key: $ADMIN_KEY"

# 测试 2: 更新配置
curl -X POST "$API_URL/api/admin/energy-config/update" \
  -H "x-admin-api-key: $ADMIN_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "key": "claim_self_reward",
    "value": 2,
    "reason": "测试生产环境部署"
  }'

# 测试 3: 查看变更历史
curl -X GET "$API_URL/api/admin/energy-config/history?limit=5" \
  -H "x-admin-api-key: $ADMIN_KEY"
```

**预期结果**: 返回 `{ "ok": true, ... }` 的 JSON 数据

#### 6.2 前端界面测试
1. 访问你的管理后台 URL
2. 登录后，点击侧边栏"⚡ 能量配置"菜单
3. 验证以下功能：
   - ✅ 配置列表正确加载
   - ✅ 修改配置并保存成功
   - ✅ 查看变更历史正常
   - ✅ 清除缓存功能正常

#### 6.3 业务逻辑测试
**测试提现逻辑是否使用动态配置**:
1. 在能量配置页面，修改"提现能量消耗比例"为 `15`
2. 让一个测试用户尝试提现 1 USDT
3. 验证用户能量是否扣除了 15（而不是原来的 10）

**测试领取逻辑是否使用动态配置**:
1. 在能量配置页面，修改"领取空投自身奖励"为 `3`
2. 让一个测试用户领取空投
3. 验证用户能量是否增加了 3（而不是原来的 1）

---

## 🔄 回滚方案（如果出现问题）

### 数据库回滚
如果需要回滚数据库迁移：

```sql
-- 1. 删除新增的表
DROP TABLE IF EXISTS public.energy_config_history CASCADE;
DROP TABLE IF EXISTS public.energy_config CASCADE;

-- 2. 删除新增的函数
DROP FUNCTION IF EXISTS public.get_energy_config() CASCADE;
DROP FUNCTION IF EXISTS public.update_energy_config(text, jsonb, text, text) CASCADE;

-- 3. 恢复 process_claim_energy 函数的原始版本
-- (需要从你的备份或 Git 历史中获取原始 SQL)
```

### 代码回滚
```bash
# 回退到上一个提交
git revert HEAD
git push origin main
```

---

## 📊 生产环境配置检查清单

### 数据库配置
- [ ] 已执行 `create_energy_config.sql`
- [ ] 已执行 `update_process_claim_energy_dynamic_config.sql`
- [ ] 已验证表和函数创建成功
- [ ] 默认配置数据已插入

### 后端配置
- [ ] 代码已推送到 Git 仓库
- [ ] Render 环境变量已确认（无需新增）
- [ ] Render 自动部署已完成
- [ ] 后端服务启动成功，无错误日志

### 前端配置
- [ ] 代码已推送到 Git 仓库
- [ ] 前端构建成功
- [ ] 前端已部署到生产环境
- [ ] 能量配置页面可正常访问

### 功能测试
- [ ] 后端 API 测试通过
- [ ] 前端界面测试通过
- [ ] 提现逻辑测试通过（使用动态配置）
- [ ] 领取逻辑测试通过（使用动态配置）

---

## 🎯 部署完成标志

当以下所有条件都满足时，部署即为成功：

1. ✅ 数据库表和函数创建成功
2. ✅ 后端服务启动无错误
3. ✅ 前端能量配置页面可访问
4. ✅ API 调用返回正确数据
5. ✅ 配置修改后立即生效
6. ✅ 业务逻辑使用动态配置

---

## 🆘 常见问题

### Q1: Render 部署失败，提示 TypeScript 编译错误
**A**: 检查是否有未删除的 `energyConfigRoutes.ts` 文件，确保已执行：
```bash
rm rabbit-ai-backend/src/api/energyConfigRoutes.ts
```

### Q2: 后端 API 返回 404
**A**: 确认 Render 部署日志中没有错误，检查路由是否正确注册在 `src/api/routes/admin.ts` 中。

### Q3: 前端调用 API 返回 401 Unauthorized
**A**: 检查管理员密钥是否正确配置，前端是否正确传递 `x-admin-api-key` header。

### Q4: 数据库函数调用失败
**A**: 确认已执行所有 SQL 迁移脚本，并验证函数创建成功。

### Q5: 配置修改后不生效
**A**: 点击页面右上角"清除缓存"按钮，强制刷新后端配置缓存。

---

## 📞 技术支持

如果部署过程中遇到任何问题，请：
1. 检查 Render 部署日志
2. 检查 Supabase 数据库是否执行成功
3. 检查浏览器控制台是否有 JavaScript 错误
4. 联系技术支持并提供详细的错误信息

---

**部署指南版本**: v1.0  
**最后更新**: 2026-01-07  
**适用环境**: Render (后端) + Vercel/Netlify (前端) + Supabase (数据库)

---

## 🎉 祝你部署顺利！

按照以上步骤操作，你的能量配置动态调整功能将在几分钟内上线生产环境！💚✨

