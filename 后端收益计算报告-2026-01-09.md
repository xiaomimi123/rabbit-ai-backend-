# Rabbit AI 后端收益计算报告

**版本**: v1.0  
**更新日期**: 2026-01-09  
**维护人员**: 开发团队

---

## 📋 目录

1. [收益计算概述](#收益计算概述)
2. [核心算法](#核心算法)
3. [VIP 等级系统](#vip-等级系统)
4. [安全机制](#安全机制)
5. [边界情况处理](#边界情况处理)
6. [性能优化](#性能优化)
7. [调试和监控](#调试和监控)

---

## 1. 收益计算概述

### 1.1 业务模型

Rabbit AI 采用"持币生息"模式：
- 用户持有 RAT 代币即可获得 USDT 收益
- 收益按日计算，实时累积
- 提现时结算收益（Lazy Settle）

### 1.2 收益来源

**主要来源**：
- 持币生息收益（根据 VIP 等级和持币天数计算）

**收益计算时机**：
- 查询收益时（实时计算）
- 提现时（固化收益）

---

## 2. 核心算法

### 2.1 收益计算公式

```
增量收益 = Balance × TOKEN_PRICE × (dailyRate / 100) × daysElapsed
实时总收益 = baseEarnings + incrementalEarnings
可领收益 = max(0, 实时总收益)
```

**参数说明**：
- `Balance`：当前 RAT 余额（从链上读取）
- `TOKEN_PRICE`：代币价格（固定 $0.01 per RAT）
- `dailyRate`：日利率（百分比，例如 2 表示 2%）
- `daysElapsed`：从上次结算到现在的天数（精确到毫秒）
- `baseEarnings`：已固化收益（来自数据库 `users.usdt_total`）
- `incrementalEarnings`：增量收益（实时计算）

### 2.2 计算流程

#### 步骤 1：读取链上余额

```typescript
// 从链上读取 RAT 余额
const ratContract = new ethers.Contract(config.ratTokenContract, ERC20_ABI, provider);
const balanceWei = await ratContract.balanceOf(userAddress);
const balance = parseFloat(ethers.utils.formatUnits(balanceWei, decimals));
```

**超时保护**：
- RPC 调用超时时间：8 秒
- 如果超时，使用默认值 0（不会阻塞请求）

#### 步骤 2：查询首次领取时间

```typescript
// 查询用户最早的一条 claims 记录
const { data: firstClaim } = await supabase
  .from('claims')
  .select('created_at')
  .eq('address', addr)
  .order('created_at', { ascending: true })
  .limit(1);
```

**用途**：
- 计算持币天数（用于显示）
- 确定账户创建时间（用于安全验证）

#### 步骤 3：读取用户数据

```typescript
// 读取用户数据（包括 last_settlement_time 和 usdt_total）
const { data: userRow } = await supabase
  .from('users')
  .select('usdt_total, last_settlement_time, created_at')
  .eq('address', addr)
  .maybeSingle();
```

**关键字段**：
- `usdt_total`：已固化收益（基准收益）
- `last_settlement_time`：上次结算时间（用于计算增量收益）

#### 步骤 4：确定 VIP 等级和日利率

```typescript
// 根据余额确定 VIP 等级和日利率
const { dailyRate, tier: currentTier } = getVipTierByBalance(balance);
```

**VIP 等级规则**：
- 从高到低查找匹配的等级
- 如果余额 >= 最低余额，且 <= 最高余额（或 null），则匹配

#### 步骤 5：检测外部转账（Sybil Attack 防护）

```typescript
// 计算系统记录的累计代币（claims + referral_rewards）
let systemRecordedBalance = 0;
// ... 累加所有 claims 和 referral_rewards

// 检测外部转账：如果链上余额 > 系统记录余额 × 1.1（差异超过10%）
const EXTERNAL_TRANSFER_THRESHOLD = 1.1;
if (systemRecordedBalance > 0 && balance > systemRecordedBalance * EXTERNAL_TRANSFER_THRESHOLD) {
  hasExternalTransfer = true;
  // 使用当前时间作为起息日，防止多支付利息
}
```

**安全措施**：
- 如果检测到外部转账，使用当前时间作为起息日
- 防止用户通过外部转账获得代币后，系统错误地从账户创建时间开始计算收益

#### 步骤 6：初始化或修正 last_settlement_time

**情况 A：从未固化过收益**
- 如果余额 >= 10k，且 `usdt_total = 0`，且 `last_settlement_time` 是首次领取时间
- 查询所有 claims 和 referral_rewards，找到首次累计余额达到 10k 的时间点
- 更新 `last_settlement_time` 为首次达到 10k 的时间

**情况 B：已固化过收益**
- 检查 `last_settlement_time` 是否合理
- 如果通过系统记录无法达到 10k，但当前余额 >= 10k，说明通过其他方式获得代币
- 如果检测到外部转账，使用当前时间作为起息日

#### 步骤 7：计算增量收益

```typescript
// 计算从上次结算到现在的天数（不取整，保留小数）
const timeElapsedMs = now - lastSettlementTime;
const daysElapsed = timeElapsedMs / (24 * 3600 * 1000);

// 计算增量收益
const TOKEN_PRICE = 0.01; // $0.01 per RAT
let incrementalEarnings = balance * TOKEN_PRICE * (dailyRate / 100) * daysElapsed;
```

**精度**：
- 天数精确到毫秒（不取整）
- 收益精确到 6 位小数

#### 步骤 8：最大收益熔断限制

```typescript
// 理论最大值 = 当前余额 × 最高利率(10%) × (当前时间 - 账户创建时间)
const accountCreatedTime = new Date(firstClaim.created_at).getTime();
const maxPossibleDays = (now - accountCreatedTime) / (24 * 3600 * 1000);
const MAX_DAILY_RATE = 0.10; // 最高日利率 10%（VIP 4）
const theoreticalMaxEarnings = balance * TOKEN_PRICE * MAX_DAILY_RATE * maxPossibleDays;

// 如果计算的收益超过理论最大值，限制为理论最大值
if (incrementalEarnings > theoreticalMaxEarnings) {
  incrementalEarnings = Math.min(incrementalEarnings, theoreticalMaxEarnings);
}
```

**安全措施**：
- 防止时间回溯错误或攻击
- 如果计算的收益超过理论最大值，说明可能存在计算错误

#### 步骤 9：计算实时总收益

```typescript
// 基准收益（已固化的收益，来自数据库）
const baseEarnings = Number(userRow?.usdt_total || 0);

// 实时总收益 = 基准收益 + 增量收益
const grossEarnings = baseEarnings + incrementalEarnings;

// 可领收益 = max(0, 实时总收益)
const netEarnings = Math.max(0, grossEarnings);
```

#### 步骤 10：返回结果

```typescript
return {
  pendingUsdt: netEarnings.toFixed(6),        // 可领收益（6位小数）
  dailyRate: dailyRate * 100,                 // 日利率（百分比）
  currentTier,                                 // VIP 等级（0-4）
  holdingDays: Math.floor(daysHolding),       // 持币天数（取整）
  balance: balance.toFixed(2),                // 当前余额（2位小数）
  grossEarnings: grossEarnings.toFixed(6),    // 历史总收益（6位小数）
  totalWithdrawn: '0',                        // 已提现总额（已废弃）
};
```

---

## 3. VIP 等级系统

### 3.1 VIP 等级配置

**默认配置**（存储在 `vip_tiers` 表）：

| 等级 | 名称 | 最低余额 | 最高余额 | 日利率 |
|------|------|---------|---------|--------|
| 0 | 未达标 | 0 | 9,999 | 0% |
| 1 | 🌱 新手 | 10,000 | 49,999 | 2% |
| 2 | 🌿 进阶 | 50,000 | 99,999 | 4% |
| 3 | 🌳 资深 | 100,000 | 199,999 | 6% |
| 4 | 💎 核心 | 200,000 | 无上限 | 10% |

### 3.2 VIP 等级确定逻辑

```typescript
export function getVipTierByBalance(balance: number): { dailyRate: number; tier: number } {
  const tiers = getVipTiers();
  
  // 从高到低查找匹配的等级
  for (let i = tiers.length - 1; i >= 0; i--) {
    const tier = tiers[i];
    if (balance >= tier.minBalance) {
      // 检查是否有上限
      if (tier.maxBalance === null || balance <= tier.maxBalance) {
        return { dailyRate: tier.dailyRate, tier: tier.level };
      }
    }
  }
  
  // 未达到任何等级
  return { dailyRate: 0, tier: 0 };
}
```

**规则**：
- 从高到低查找匹配的等级
- 如果余额 >= 最低余额，且 <= 最高余额（或 null），则匹配
- 如果未达到任何等级，返回 `{ dailyRate: 0, tier: 0 }`

### 3.3 VIP 配置缓存

**缓存策略**：
- 启动时加载到内存
- 缓存有效期：60 秒
- 过期后异步刷新（不阻塞请求）

---

## 4. 安全机制

### 4.1 外部转账检测（Sybil Attack 防护）

**问题**：
- 用户通过外部转账获得代币后，系统可能从账户创建时间开始计算收益
- 这会导致多支付利息，造成经济损失

**解决方案**：
```typescript
// 计算系统记录的累计代币（claims + referral_rewards）
let systemRecordedBalance = 0;
// ... 累加所有 claims 和 referral_rewards

// 检测外部转账：如果链上余额 > 系统记录余额 × 1.1（差异超过10%）
const EXTERNAL_TRANSFER_THRESHOLD = 1.1;
if (systemRecordedBalance > 0 && balance > systemRecordedBalance * EXTERNAL_TRANSFER_THRESHOLD) {
  hasExternalTransfer = true;
  // 使用当前时间作为起息日，防止多支付利息
  lastSettlementTime = now;
}
```

**阈值**：
- 差异超过 10% 视为外部转账
- 只有系统记录余额 > 0 时才进行检测（避免误报）

### 4.2 最大收益熔断限制

**问题**：
- 时间回溯错误或攻击可能导致收益计算异常
- 计算的收益可能超过理论最大值

**解决方案**：
```typescript
// 理论最大值 = 当前余额 × 最高利率(10%) × (当前时间 - 账户创建时间)
const theoreticalMaxEarnings = balance * TOKEN_PRICE * MAX_DAILY_RATE * maxPossibleDays;

// 如果计算的收益超过理论最大值，限制为理论最大值
if (incrementalEarnings > theoreticalMaxEarnings) {
  incrementalEarnings = Math.min(incrementalEarnings, theoreticalMaxEarnings);
}
```

**安全措施**：
- 如果计算的收益超过理论最大值，记录错误日志
- 限制增量收益为理论最大值

### 4.3 RPC 超时保护

**问题**：
- RPC 调用可能无限等待，导致请求阻塞

**解决方案**：
```typescript
// 使用 Promise.race 实现超时保护
const balancePromise = ratContract.balanceOf(userAddress);
const timeoutPromise = new Promise<ethers.BigNumber>((_, reject) => {
  setTimeout(() => {
    reject(new Error(`RPC_TIMEOUT: balanceOf call exceeded 8 seconds`));
  }, 8000);
});

balanceWei = await Promise.race([balancePromise, timeoutPromise]);
```

**超时时间**：
- 余额查询：8 秒
- 如果超时，使用默认值 0（不会阻塞请求）

---

## 5. 边界情况处理

### 5.1 用户从未领取过空投

**处理**：
```typescript
if (!firstClaim || !firstClaim.created_at) {
  return {
    pendingUsdt: '0',
    dailyRate: 0,
    currentTier: 0,
    holdingDays: 0,
    balance: balance.toFixed(2),
    grossEarnings: '0',
    totalWithdrawn: '0',
  };
}
```

**返回**：
- 所有收益相关字段为 0
- 余额从链上读取（可能 > 0）

### 5.2 RPC 调用失败

**处理**：
```typescript
try {
  // RPC 调用
} catch (error: any) {
  console.warn(`[Earnings] Failed to fetch RAT balance: ${errorMsg}, using default 0`);
  balance = 0;
}
```

**降级策略**：
- 使用默认值 0
- 记录警告日志
- 不抛出异常（避免阻塞请求）

### 5.3 首次达到 10k 初始化

**处理**：
```typescript
// 查询所有 claims 和 referral_rewards，找到首次累计余额达到 10k 的时间点
let cumulativeBalance = 0;
for (const event of allEvents) {
  cumulativeBalance += event.amount;
  if (cumulativeBalance >= 10000 && !firstReached10kTime) {
    firstReached10kTime = event.timestamp;
    break;
  }
}

// 更新 last_settlement_time
if (firstReached10kTime) {
  await supabase
    .from('users')
    .update({ last_settlement_time: firstReached10kIso })
    .eq('address', addr);
}
```

**逻辑**：
- 合并所有代币来源（claims + referral_rewards）
- 按时间排序
- 计算累计余额，找到首次达到 10k 的时间点
- 更新 `last_settlement_time`

### 5.4 检测到外部转账

**处理**：
```typescript
if (hasExternalTransfer) {
  // 使用当前时间作为起息日，防止多支付利息
  lastSettlementTime = now;
  
  // 更新数据库
  await supabase
    .from('users')
    .update({ last_settlement_time: nowIso })
    .eq('address', addr);
}
```

**安全措施**：
- 使用当前时间作为起息日
- 防止从账户创建时间开始计算收益

---

## 6. 性能优化

### 6.1 Lazy Settle 机制

**问题**：
- 频繁更新 `usdt_total` 会导致数据库写入压力大

**解决方案**：
- 查询收益时：只计算，不更新数据库
- 提现时：固化收益到数据库（Lazy Settle）

**优势**：
- 减少数据库写入次数
- 提高查询性能
- 收益计算实时准确

### 6.2 VIP 配置缓存

**问题**：
- 每次查询收益都需要查询数据库获取 VIP 配置

**解决方案**：
- 启动时加载到内存
- 缓存有效期：60 秒
- 过期后异步刷新

**优势**：
- 减少数据库查询
- 提高响应速度

### 6.3 RPC 超时保护

**问题**：
- RPC 调用可能无限等待

**解决方案**：
- 使用 `Promise.race` 实现超时保护
- 超时时间：8 秒
- 超时后使用默认值 0

**优势**：
- 避免请求阻塞
- 提高系统稳定性

---

## 7. 调试和监控

### 7.1 日志记录

**关键日志**：
```typescript
console.log(`[Earnings] User ${addr}: baseEarnings=${baseEarnings.toFixed(6)}, incrementalEarnings=${incrementalEarnings.toFixed(6)}, grossEarnings=${grossEarnings.toFixed(6)}, netEarnings=${netEarnings.toFixed(6)}`);
```

**警告日志**：
```typescript
console.warn(`[Earnings] 🚨 检测到外部转账（Sybil Attack 风险）: 用户 ${addr}`);
console.warn(`   系统记录余额: ${systemRecordedBalance.toFixed(2)} RAT`);
console.warn(`   当前链上余额: ${balance.toFixed(2)} RAT`);
console.warn(`   外部转账金额: ${externalTransferAmount.toFixed(2)} RAT`);
```

**错误日志**：
```typescript
console.error(`[Earnings] 🚨 收益计算异常：增量收益超过理论最大值`);
console.error(`   计算的增量收益: ${incrementalEarnings.toFixed(6)} USDT`);
console.error(`   理论最大值: ${theoreticalMaxEarnings.toFixed(6)} USDT`);
```

### 7.2 Sentry 监控

**自动捕获**：
- 未处理的错误
- 错误堆栈和上下文

**过滤规则**：
- 过滤已知错误（如数据库函数检查时的预期错误）

### 7.3 调试查询

**查询用户收益数据**：
```sql
SELECT 
  address,
  usdt_total,
  last_settlement_time,
  energy_total,
  created_at
FROM users
WHERE address = '0x...';
```

**查询用户领取记录**：
```sql
SELECT 
  tx_hash,
  amount_wei,
  block_time,
  created_at
FROM claims
WHERE address = '0x...'
ORDER BY created_at ASC;
```

**查询推荐奖励记录**：
```sql
SELECT 
  tx_hash,
  amount_wei,
  block_time,
  created_at
FROM referral_rewards
WHERE referrer_address = '0x...'
ORDER BY created_at ASC;
```

---

## 8. 收益计算示例

### 8.1 正常用户示例

**用户信息**：
- 地址：`0x1234...`
- 余额：50,000 RAT
- VIP 等级：2（进阶，日利率 4%）
- 首次领取时间：2026-01-01 00:00:00
- 上次结算时间：2026-01-05 00:00:00
- 已固化收益：10 USDT

**计算过程**：
```
当前时间：2026-01-09 12:00:00
距离上次结算：4.5 天

增量收益 = 50,000 × 0.01 × (4 / 100) × 4.5
         = 50,000 × 0.01 × 0.04 × 4.5
         = 90 USDT

实时总收益 = 10 + 90 = 100 USDT
可领收益 = 100 USDT
```

### 8.2 外部转账检测示例

**用户信息**：
- 地址：`0x5678...`
- 链上余额：100,000 RAT
- 系统记录余额：10,000 RAT（仅通过系统获得）
- 差异：90,000 RAT（外部转账）

**检测结果**：
```
系统记录余额：10,000 RAT
链上余额：100,000 RAT
差异比例：900%

检测结果：外部转账（差异超过 10%）
安全措施：使用当前时间作为起息日
```

---

## 9. 常见问题

### 9.1 收益计算不准确

**可能原因**：
1. `last_settlement_time` 设置不正确
2. 检测到外部转账，使用了当前时间作为起息日
3. RPC 调用失败，余额读取为 0

**解决方案**：
1. 检查 `last_settlement_time` 是否正确
2. 查看日志，确认是否检测到外部转账
3. 检查 RPC 节点是否正常

### 9.2 首次达到 10k 未初始化

**可能原因**：
1. 用户通过外部转账直接达到 10k（没有系统记录）
2. 数据库查询失败

**解决方案**：
1. 检查是否有系统记录（claims + referral_rewards）
2. 如果检测到外部转账，会自动使用当前时间作为起息日

### 9.3 RPC 调用超时

**可能原因**：
1. RPC 节点响应慢
2. 网络问题

**解决方案**：
1. 配置多个 RPC 节点（自动轮换）
2. 使用 `ADMIN_RPC_URL` 配置专用节点
3. 超时后使用默认值 0（不会阻塞请求）

---

**文档维护**：如有更新，请及时更新本文档。

